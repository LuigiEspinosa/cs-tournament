# Deferred Work

Tracked follow-ups surfaced during reviews. Each item names its origin and its intended home.

## Deferred from: code review of story-5.6 (timeline feed read — FR-31) (2026-07-28)

_Baseline `f067362`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8) + reviewer verification against the mock + migrations. No AC violated in a way that ships an incorrect or leaking feed. 2 decision-needed + 3 patches handled in-story; the two items below are real but not actionable now._

- [ ] **`resolveCurrentTournament` binds "current" to `max(id)`, not `state`.** `[lib/feed/read.ts:44-49]` The read is `order by id desc limit 1`, so any second `tournament` row (a prematurely-created future event, next season's) shadows the intended live one — the layout pill, the Ceremonia lock, the feed read, and the registered count all bind to the wrong tournament. **Intended home:** whenever a second tournament is introduced — replace the newest-row heuristic with an explicit "current" flag or a state-based selector. **Reason deferred:** deliberate, dev-flagged single-private-event v1 assumption confirmed by Cuatro; there is no schema singleton constraint, so this is an accepted scope boundary, not a defect in the first tournament.
- [ ] **A transient roster/player read outage is indistinguishable from "all players removed."** `[lib/feed/read.ts:144-146]` If the `roster_entry` batch read errors or returns zero rows, `resolveNames` returns an empty map and every card renders `Jugador retirado`, with only a `console.error` to distinguish a real outage from a legitimately all-removed roster. **Intended home:** whenever the read layer is next touched (5.7/5.8) — surface a distinct degraded signal (e.g. an error state or a "names unavailable" marker) rather than reusing the removed-player label. **Reason deferred:** the graceful soft-fail is by design (an approved, published result must never blank the feed) and the "measure don't assume" trap is partly mitigated by the log line; a distinct signal is a refinement, not a correctness fix for this story.

## Deferred from: code review of story-5.2 (weird demo-only stat derivation — FR-19) (2026-07-21)

_Baseline `3b22fa0`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8) + reviewer verification against the real files, an independent re-run of the mutation set, and a re-run of THE BAR. **No AC and no spec prohibition violated.** 1 decision-needed (post-round kills COUNT — resolved by Cuatro, pinned in the handler comment), 9 patches applied, 3 dismissed. The five items below are real but not actionable in this story._

- [ ] **Pre-5.2 `stat_row` rows keep `NULL`, not `0`, in the weird five.** `[worker/db/db.go:131-136]` AC2's "recorded as zero, not omitted" holds only for rows written after this build; every Epic-3/4 row keeps `NULL` in `knife_kills`…`blind_kills` until its demo is re-parsed through `RunReparse`. `NULL` propagates through `SUM`/`ORDER BY`/comparison, so a player with only pre-5.2 demos renders blank rather than `0` and is silently excluded by any `where knife_kills > 0`. **Intended home:** Story 5.5 (the leaderboard view — it must coalesce; the story's Dev Notes already name it as the owner). **Reason deferred:** deliberate and explicitly not a backfill — the same widening pattern 4.6a and 5.1 already set; re-parse is the sanctioned repair path.
- [x] **🔴 RESOLVED same day by Story 5.2a — `AttackerBlind` IS NOT POPULATED; `blind_kills` was a dead column, MEASURED not suspected.** `[worker/ingest/parse.go:318]` **Resolution:** `blind` now derives from the live `Killer.IsBlinded()`, passed into the still-pure `classifyWeirdKill` as a bool; Story 5.2 AC1 amended for this one column. THE BAR now reports **blind 4** (was 0) with exact reconciliation. See [5-2a-blind-kills-live-source-amendment.md](5-2a-blind-kills-live-source-amendment.md). Kept here as the record of how it was found. **Superseded reasoning:** both this story and my first review pass explained `blind_kills = 0` away as "proven by mode — `AttackerBlind` needs a TEAMMATE to flash you, and 1v1 duels have no teammates." **That premise is false**: in a 1v1 the ENEMY flashes you and you kill them anyway (classic "blind justice"), and self-flashing also counts. Cuatro confirmed 2026-07-21 that the whole first tournament is 1v1 on wingman maps, so the mode excuse had to be tested rather than assumed. A probe over the same 14 demos measured: **172 `FlashExplode` events, 67 `PlayerFlashed`, and 4 non-warmup kills where the killer was genuinely blind at trigger time** (`Player.IsBlinded()`, i.e. `FlashDurationTimeRemaining() > 0` — not the stale `FlashDuration` field), the strongest being a `M4A4` kill with **2.13s of flash still remaining**. `e.AttackerBlind` was **`false` on all four**. The other CS2 flags do fire on this same data (wallbang 8, smoke 2), so this is specific to `AttackerBlind`, and it contradicts the story's Dev Note that "these four flags are CS2-only fields [that] are populated for Source 2 demos". **Consequence:** `blind_kills` will read 0 for every player for the entire tournament — an award track with no possible winner, and no error anywhere to reveal it. **Intended home:** was a decision + follow-up story before Story 5.5 publishes the awards — **Cuatro chose the live-source fix on 2026-07-21 and Story 5.2a shipped it.** The deviation from AC1's "native `events.Kill` field, never a heuristic" was made explicit as a scoped amendment rather than a silent patch: `Killer.IsBlinded()` is the parser's own tracked flash state, not a heuristic, and it is permitted only for a column whose `events.Kill` field is *demonstrated* dead. **No longer deferred.**
- [ ] **`no_scope_kills` has no live data yet, and unlike `blind_kills` that is unremarkable.** `[worker/ingest/parse.go:317]` The same probe found only **16 scoped-weapon kills (AWP/SSG08/G3SG1/SCAR-20) across 218 non-warmup kills**, none of them no-scopes. A no-scope requires both a sniper and a specific play, so zero out of 16 is an entirely ordinary sample — there is no positive counter-evidence of the kind that convicts `AttackerBlind`. **Intended home:** re-check once the tournament produces AWP volume. **Reason deferred:** cannot distinguish "works but rare" from "broken" without a demo containing a real no-scope; the classifier branch is mutation-verified, and nothing suggests a defect.
- [ ] **A truncated demo's final round silently drops its weird five.** `[worker/ingest/parse.go:556]` If the last `RoundEnd` never fires (truncated/aborted demo, or the fail-closed `recover()` path), that round's scratch is discarded whole. **Intended home:** whenever the parser is next touched (5.3/5.4) — a `log.Printf` warn when `cur` is non-empty at `ParseToEnd` return would make the loss visible. **Reason deferred:** pre-existing, shared identically with `kills`/`deaths`, and self-consistent (`final` excludes the round too), so it degrades quietly rather than tripping the conservation gate. Nothing currently logs it.
- [ ] **`assertDerivedStats` uses `t.Fatalf`, so one dropped field masks the other eleven.** `[worker/ingest/cli_test.go:150]` The helper is now a twelve-field net and every branch fatals, so a single dropped field aborts before the remaining eleven, the second player, and the FR-16 conservation check are examined — you learn "one field broke" when you needed "was the whole literal replaced?". **Intended home:** 5.3 or 5.4, when this helper is next widened. **Reason deferred:** pre-existing from Story 5.1's `assertCoreSeven`, not introduced by 5.2; the fix is `t.Errorf` in the helper with the `FailNow` decision left to the caller.
- [ ] **`worker/cmd/qa52/` is the throwaway THE BAR harness and is still in the tree.** `[worker/cmd/qa52/main.go]` Kept through review so the live gate could be re-run (it was, twice — pre- and post-patch, reproducing identical numbers). **Intended home:** deletion. **Reason deferred:** committed with the story so the harness that produced every THE BAR number stays recoverable in git history; delete in a follow-up rather than losing it to an untracked `rm`.

## Deferred from: code review of 4-8 (manual score override — manual side of FR-16) (2026-07-19)

_Baseline `07ec695`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8) + reviewer verification against the real files. **AC1/AC2 substantively satisfied.** 1 decision-needed (the `match_manual_override_audited` later-edit hole — a **patch/decision** on the story, not deferred; see the story's Review Findings), 3 patches, 4 dismissed (two Blind-Hunter false positives the code disproves — the Section-F stat-row assertion is meaningful because `bind_match_demo` links `stat_row.match_id`, and `bind.test.ts:167` covers the `format_not_declared` positive path — plus the `demo_id`↔`state` correlation, verified graceful, and the untested route status-table, consistent with the sibling thin-route pattern). The two items below are documentation/reasoning corrections that do not block the story._

- [ ] **`pending → manual_resolved` is an undocumented lifecycle edge in `ARCHITECTURE-SPINE.md`.** `[supabase/migrations/0019_manual_score.sql]` DECISION A accepts `pending` as the audited-override source state (`state in ('declared','live','pending')`), which is necessary or AD-5's override half is unreachable — but the SPINE match lifecycle (`:299-317`) lists only `Live → ManualResolved` and `Pending → Resolved` (Aprobar); there is no `Pending → ManualResolved`. **Intended home:** a SPINE doc update adding the edge (whoever next touches the lifecycle diagram, or Epic-5's console story that owns the manual-entry UX). **Reason deferred:** a documentation gap, not a code defect — the transition is deliberate and correct; the diagram simply lags it.
- [ ] **Story 4.8's Task 6 / Completion-Notes regression rationale misidentifies the real protective condition (no actual regression).** `[_bmad-output/implementation-artifacts/4-8-manual-score-override-manual-side-of-fr-16.md]` The story attributes the 0010/0012/0017 `admin_manual` fixtures' safety to the trigger being `after update`-only vs "raw-INSERT fixtures", but two of them set `score_source='admin_manual'` via **UPDATE** (`0012_format_lock_test.sql:346`, `0017_aprobar_publish_test.sql:436`) — they stay green because both set `manual_override=false` (so the trigger's `new.manual_override=true` `when` never matches), NOT because of the INSERT/UPDATE distinction. **Intended home:** a one-line correction to the story's Completion Notes / this note, so the real invariant is on record. **Reason deferred:** the sweep outcome is correct (no redness); this is a latent trap for a FUTURE fixture that sets `manual_override=true` via UPDATE without a `manual_score` audit row (it WOULD then trip IC907), recorded so that surprise is pre-explained rather than debugged.

## Deferred from: dev-story of 4-8 (manual score override — manual side of FR-16) (2026-07-19)

_Surfaced while IMPLEMENTING 4-8 (`manual_resolve_match` migration 0019 + the `match_manual_override_audited` trigger + `lib/match/manual-score.ts` + `POST /api/admin/manual-score`; baseline `07ec695`). This story **CLOSED** one item homed here (recorded so a reviewer can reconcile the `- [ ]` box): the **`lib/match/bind.ts` "maps EVERY 23514 to `format_not_declared`" item** (code review of story-4-6a, `:33` below) — closed by DECISION I: `bind.ts` now discriminates the constraint name (`match_live_requires_locked_format` → `format_not_declared`; `score_source_guard` → `write_failed`; unknown → `write_failed`). ⚠ Confirmed by analysis that the `score_source_guard` path stays UNREACHABLE via bind: `bind_match_demo`'s `not_bindable` guard refuses anything not `declared`/`live`, and a `manual_resolved` match — the only thing 4.8 sets `score_source` on — is neither, so no bind ever touches a `score_source`-set row. Robustness closing the box, not a live-bug fix. The three items below are left open._

- [ ] **The manual-resolution INVERSE (undo of a mis-entered manual score) has no path — DECISION H deferred it.** `[supabase/migrations/0019_manual_score.sql]` `manual_resolve_match` lands a match in terminal `manual_resolved`, and `rollback_match` (4.7) refuses `manual_resolved` with `not_resolved` (0018:144-147, DECISION C) — deliberately handing the inverse to this story, which the epic's AC1/AC2 (the WRITE) did not spec. So a mis-entered manual score currently has **no undo path** (the same shape as the `forfeit`-no-undo gap the 4-7 review flagged). **Intended home:** widen `rollback_match`'s state guard + target-state mapping to accept `manual_resolved` (reverting to `declared`/`live`, clearing the score, un-seating dependent advances via its existing two-pass/AD-8 machinery), or a sibling RPC — most naturally Story 4.9 or a small Epic-4 follow-up. **Reason deferred:** a distinct concern from the manual WRITE the epic ACs cover; refusing is still correct (a terminal `manual_resolved` is not silently mutable), it is the *missing forward tool* that is deferred.
- [ ] **`manual_resolve_match` has no ceremony-lock guard — Story 6.2 must add it (the same seam as `approve_match`/`rollback_match`).** `[supabase/migrations/0019_manual_score.sql]` AD-15 (`ARCHITECTURE-SPINE.md:153`) requires the ceremony lock to block event-mutating admin actions; a manual resolution after ceremony-lock is the same class of concern as an approve/rollback after it (`deferred-work.md`, the 4.6b/4.7 ceremony items). Neither `ceremony` nor `tournament.ceremony_locked_at` exists until Epic 6, so `manual_resolve_match` ships without the check. **Intended home: Story 6.2** — add a `ceremony_locked` typed refusal before the first write (alongside `not_manual_resolvable`), to `manual_resolve_match` too (now three RPCs: approve, rollback, manual-score). **Reason deferred:** the state it guards does not exist yet.
- [ ] **Story 4.9's audited-route list omits `manual-score` (DECISION G).** `epics.md:812`'s route list — *"generate bracket, advance, mark walkover, approve, re-parse, rollback, declare format, start ceremony, grant role"* — does NOT name the manual-score route this story shipped at `POST /api/admin/manual-score`. This is the same omission 4.6a flagged for the accept-anomaly route. **Intended home: Story 4.9** (the audited command-route helper) — its route list should be corrected to include `manual-score`. **Reason deferred:** 4.9 owns the shared command-route helper + its route enumeration; 4.8 flags the gap rather than editing 4.9's spec.

## Deferred from: code review of 4-7 (rollback an Approved match) (2026-07-19)

_Baseline `e2a0ad9`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8) + reviewer verification against the real files. The review's flagship finding — the AD-8 flag omitting `forfeit` — is a **patch** on the story, not deferred (see the story's Review Findings). The one deferred item below is a pre-existing gap that patch surfaces._

- [ ] **A committed `forfeit` (or `live`/`pending`/`awaiting_grace`) downstream can strand a rollback — there is no tool to undo a `forfeit` leaf-first.** `[supabase/migrations/0018_rollback_match.sql]` Once `rollback_match`'s AD-8 flag is corrected to refuse (`downstream_active`) on a downstream `forfeit` node (the 4-7 review patch), the admin is instructed to roll that downstream match back first (leaf-first). But `rollback_match` handles only `resolved` (→ `not_resolved` on a `forfeit`), and Story 4.5's `resume_match` handles only `awaiting_grace` — so a committed `forfeit` has **no undo path**, and a rollback blocked behind one cannot proceed. Refusing is still strictly correct (auto-reverting corrupts a decided match — AD-8's named hazard); this is about the *missing* forward tool, not the refusal. **Intended home:** a forfeit-rollback path — most naturally Story 4.9 (audited admin command routes) or a small Epic-4/future slice, and it should share `rollback_match`'s two-pass/AD-8 machinery (widen it to accept `forfeit`, reverting to the pre-forfeit seating, or add a sibling RPC). **Reason deferred:** pre-existing — 4.5 shipped `forfeit` with no undo; 4.7's flag correctly refuses rather than corrupts, but the leaf-first instruction it gives has no tool to satisfy it yet. ⚠ Note: this also means the 4-7 completion-note claim that the `mark_walkover` NULL-loser item (`:below`) was "NOT made reachable" is wrong for the `forfeit` (not just `awaiting_grace`) case — the patch closes the corruption path, but the reachability analysis in the story's Completion Notes should be read with that correction.

## Deferred from: dev-story of 4-7 (rollback an Approved match) (2026-07-18)

_Surfaced while IMPLEMENTING 4-7 (`rollback_match` migration 0018 + `lib/match/rollback.ts` + `POST /api/admin/rollback`; baseline `e2a0ad9`). This story **CLOSED** two items homed here (recorded so a reviewer can reconcile the `- [ ]` boxes): **`bind_match_demo`'s idempotent short-circuit vs a `rolled_back` match (`:21` below)** — closed FOR FREE, because DECISION A lands the target state at `pending`, not `rolled_back`, so a rolled-back match is indistinguishable from a fresh `pending` one and 4.6a's short-circuit already handles it; no `rolled_back` state is ever created, so the ambiguous `{ok:true, state:'rolled_back'}` reply cannot arise. And **`mark_walkover`'s NULL-loser (`:40` below)** — NOT made reachable, because DECISION F refuses (`downstream_active`) before un-seating anything that touches an `awaiting_grace` node, so the un-seat path never leaves a downstream forfeit one-seated. The two below are left open._

- [ ] **The correcting `timeline_feed` entry is not written — a rollback posts NOTHING to the public feed (DECISION D).** `[supabase/migrations/0018_rollback_match.sql]` `0017:115`'s comment says a rollback *"posts a CORRECTING entry"*, but `timeline_feed.entry_type` is `('match_result','bracket_advance','award_reveal')` — **none fits a rollback** — and **no reader exists** (Story 5.6 owns the feed read, unbuilt). The rollback IS logged, via the AD-17 `rollback` audit row (FR-14's *"logged"* requirement). **Intended home:** the SAME uniform feed-writer that 4.6b DECISION D deferred the `bracket_advance` entries to (most naturally Story 5.6 / a small Epic-5 slice) — it should post BOTH `bracket_advance` AND rollback-correction entries in one place, and will need the `entry_type` enum widened with a rollback value (e.g. `match_rolled_back`). **Reason deferred:** the append-only correction only matters once the feed is read, and posting one now writes to a feed nobody reads; the audit row already carries the correction (before/after + the reverted plan).
- [ ] **`rollback_match` has no ceremony-lock guard — Story 6.2 must add it (the same seam as `approve_match`).** `[supabase/migrations/0018_rollback_match.sql]` AD-15 (`ARCHITECTURE-SPINE.md:153`) requires the ceremony lock to block event-mutating admin actions; a rollback after ceremony-lock is the same class of concern as an approve after it (`deferred-work.md:9`). Neither `ceremony` nor `tournament.ceremony_locked_at` exists until Epic 6, so `rollback_match` ships without the check. **Intended home: Story 6.2** — add a `ceremony_locked` typed refusal before the first write (alongside `not_resolved`), to BOTH `approve_match` and `rollback_match`. **Reason deferred:** the state it guards does not exist yet.

## Deferred from: dev-story of 4-6b (Aprobar atomic publish) (2026-07-18)

_Surfaced while IMPLEMENTING 4-6b (`approve_match` + `timeline_feed` + the emit; baseline `ad47f40`). This story **CLOSED** five items that were homed here, and leaves the two below open. Items closed (recorded so a reviewer can reconcile the `- [ ]` boxes in the older sections): the **AD-23 `resolved→forfeit` half** (code review of 4-5) — shipped as the surgical `IC904` rule with a `lives_ok` pinning `resolved→pending` so Story 4.7 stays unblocked; **`bind_match_demo` does not verify the demo's players are the competitors** (dev-story of 4-6a) — shipped as the `wrong_demo` guard; **data-integrity M5 (`stat_row.demo_id = match.demo_id` at approve time)** — shipped as the `demo_mismatch` guard; the **`score_source_guard` score-with-no-provenance hole** — shipped as the tightened `(score_a is null) = (score_source is null)` conjunct (the 0010/0012 test fixtures that set a source without a score were corrected to real behaviour); and the **NULL-`rounds_won` bind** (code review of 4-6a) — refused at approve time by the `bad_score` guard (which now tests for NULL, not merely an absent row). The `bracket_advance` feed-writer (DECISION D) is **already** homed — see the `create-story of 4-6` entry below; 4-6b provisioned the `entry_type` enum with all three FR-31 values and writes only `match_result`, exactly as decided._

- [ ] **Aprobar does not check the ceremony lock — Story 6.2 must add it to `approve_match`.** `[supabase/migrations/0017_aprobar_publish.sql]` AD-15 (`ARCHITECTURE-SPINE.md:153`) requires the ceremony lock to *"block further approves"*, and data-integrity **M3** (`review-data-integrity.md:226-258`) wants `tournament.ceremony_locked_at` checked **by Aprobar**. Neither `ceremony` nor that column exists until Epic 6, so `approve_match` ships without the check. **Intended home: Story 6.2** — it must add a `ceremony_locked` guard to the shipped `approve_match` (a typed refusal before the first write, alongside `not_pending`). **Reason deferred:** the state it guards does not exist yet; building the check now would gate on a column no migration has created. Flagged at contexting and re-confirmed in build.
- [ ] **`approved_by` becomes anon-readable the moment Aprobar writes it — accepted, not fixed.** `[supabase/migrations/0017_aprobar_publish.sql]` `0009:35` grants `select` on `stat_row` to `anon, authenticated` column-blind, and `stat_view` exposes every `status='approved'` row — so writing `approved_by` (the acting admin's steamid64) makes it anon-readable. **Decision (per Cuatro):** accepted for this casual private friends' group (the standing "no privacy over-engineering" stance); the acting admin's identity on an approved result is not sensitive here. **Intended home:** only revisit if the group's privacy posture changes (e.g. a public deployment) — then either drop `approved_by` from the viewer projection or add a column-scoped grant. **Reason deferred:** a deliberate acceptance, recorded so it is a choice and not an oversight.

## Deferred from: code review of story-4-6a (2026-07-16)

_Baseline `13dc05f`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8) + reviewer verification of every contested claim against the real files. **AC1–AC3 substantively satisfied**; the scope-boundary grep was independently re-run and holds; every load-bearing spec citation resolves; DECISION L verified sound. 2 decision-needed (both resolved by Cuatro 2026-07-16 — the re-parse↔bind collision RE-HOMED to 4-6b, see the `:124` item below; the NULL-tally bind deferred), 9 patches, 9 dismissed (incl. three the Blind Hunter raised that the codebase disproves — `format_locked` and `validation_state` are both NOT NULL, and the tournament is 1v1 per `prd.md:95`, so the conservation invariant is correct). Six items deferred below._

- [ ] **A demo whose `rounds_won` is NULL on every row binds cleanly and lands the match `pending` with no derivable score.** `[supabase/migrations/0016_bind_and_score.sql:218-220]` `bind_match_demo`'s `no_stats` guard counts row EXISTENCE (`select count(*) … where demo_id = p_demo_id`), never the tally. Every `stat_row` in the DB has `rounds_won IS NULL` the moment 0016 applies — the migration's own column comment says so (*"NULL = not derived (e.g. parsed before Story 4.6a)"*) — so binding any pre-4.6a demo returns a healthy-looking `{ok:true, stat_rows_bound:2}`, moves the match to `pending`, and the admin *"eyeballs the derived score"* (`EXPERIENCE.md:209`) on NULL. The `no_stats` guard exists precisely to refuse a demo that *"decided nothing"*; a demo with rows but no tally decided nothing **scoreable**, and is not refused. **Intended home: Story 4-6b**, whose `bad_score` guard (*"a `stat_row.rounds_won` missing for either competitor"*) already refuses the approve — so nothing publishes wrong; it fails late rather than at the guard whose job this is. **Reason deferred (Cuatro, 2026-07-16):** consistent with this story's own reasoning at the 4-6a dev-story item below — adding an unspecced guard to a story already split for size is the exact scope creep the split existed to prevent; Task 2's guard list is explicit and does not include it. Also self-limiting: a re-parse re-derives any pre-4.6a demo. [blind+edge]
- [ ] **No runtime gate on `rounds_won` — `validate.go` has three gates and none covers the new derived field.** `[worker/ingest/validate.go:27-67]` The gates are conservation (Σkills==Σdeaths), empty_stats and unreconciled. Story 4-6a added a derived field with a headline invariant (`Σrounds_won == rounds_played`) and no counterpart gate, so every path that breaks it flows silently into `stat_row.rounds_won` with `validation_state='pending'` (clean), passes every `bind_match_demo` guard, and lands the match in `pending` for the admin to "eyeball": the unbounded-fold inflation (the story's own review patch), an empty `winners` slice (`parse.go:157-164` stores it — indistinguishable from a draw), and a winner who disconnects at round end. ⚠ **This is the gate that would have caught DECISION L's +1 in production, forever** — the defect that only a manual 14-demo run caught. **Intended home:** alongside the Σkills==Σdeaths conservation item below, or Epic 5's rich-stat derivation. **Suggested shape:** gate on `Σrounds_won > rounds_played` (the IMPOSSIBLE over-count direction) rather than strict `==` — trap 3 makes `<` legitimate on a drawn round, and this is exactly the asymmetry the conservation-gate item argues for on kills/deaths. **Reason deferred:** an unspecced gate, and the strict-`==`-vs-`>` question is the same one Cuatro already ruled DEFER on for kills/deaths (2026-07-11) — deciding them together, once, beats splitting the ruling. [edge+blind]
- [ ] **The `events.RoundEnd` derivation — the only code in 4-6a that computes a score — has no automated test at any layer.** `[worker/ingest/parse.go:150-167]` `DemoinfocsParser.Parse` is never invoked by any test: `parse_test.go` only widens a `FakeParser` echo, and `cli_test.go`/`reparse_test.go` drive `cannedParse()`, a hand-written constant. `cli_test.go`'s *"FR-16's conservation, at the assembly seam"* asserts `16 + 8 == 24` over three numbers typed twenty lines above with nothing but a struct copy between them — it cannot fail. pgTAP Section H is an INSERT/SELECT echo over the same constants (0016 adds a nullable column; it does not compute `rounds_won`). So AC1's derivation rests entirely on live-QA assertion, and DECISION L exists *because* the obvious implementation was silently wrong on 14/14 demos — with no regression guard against it returning, while `ParserVersion` is pinned *"in LOCKSTEP"*, i.e. an upgrade is anticipated. **Intended home:** Epic 5's rich-stat derivation (which rewrites this handler anyway), or a worker test-harness decision. **Reason deferred:** matches the worker's established convention (fakes cover orchestration, live-QA covers the real parser — the same posture as `RecordParse` in 3.3/3.4); `demos/` is gitignored so the 14-demo harness cannot be checked in as-is. ⚠ But the convention now guards a **score**, not a K/D tally — a checked-in minimal `.dem`, or a synthetic event-stream seam, deserves a think. [blind]
- [ ] **The parser now creates `stat_row`s for 0-kill round-winners — an unrecorded change to Story 3.3's row set that can newly trip 3.4's gate.** `[worker/ingest/parse.go:175-181]` The fold calls `getByID`, which CREATES on first sight; before 4-6a, `stats` was populated ONLY from the `events.Kill` handler. Now any SteamID64 in `WinnerState.Members()` at a live `RoundEnd` produces a row, and `validate.go:53-64` Gate 3 flags every parsed id absent from the ACTIVE roster → `anomalous` → 4-6a's own DECISION H then refuses the bind. A demo that ingested clean before 4-6a can become un-bindable. It also silently (partly) resolves the open 3.3 deferral below (*"a 0-kill/0-death player gets no row"*) without recording it, while the story explicitly flags the *sibling* K/D item as out of scope — and it contradicts the story's *"you add `rounds_won` and nothing else"* / *"Scope boundaries held."* **Intended home:** annotate or close the 3.3 row-set item; Epic 5's rich-stat derivation owns the handler. **Reason deferred:** low real reachability (a 1v1 player almost always gets a kill over 15 rounds), and creating the row is arguably MORE correct — a round-winner did play. Worth recording rather than fixing. [auditor, reviewer-confirmed against validate.go]
- [ ] **`lib/match/bind.ts` maps EVERY `23514` to `format_not_declared`, but `score_source_guard` is a second `23514` source on the very UPDATE the bind performs.** `[lib/match/bind.ts:35,100-102]` `score_source_guard` (`[supabase/migrations/0010_match.sql:124-128]`) is `score_source is null or (demo_derived and demo_id is not null) or (admin_manual and (demo_id is null or manual_override = true))`. A row with `score_source='admin_manual'`, `manual_override=false`, `demo_id IS NULL` is **legal today** (third disjunct) and becomes **illegal the instant the bind sets `demo_id`** — yielding a 23514 reported to the admin as *"declare the format first"* on a match whose format is already locked. 0016's claim *"score_source_guard is satisfied: score_source stays NULL"* is true only *while* it is NULL; the RPC does not guard it. **Intended home:** Story 4.8 (the `admin_manual` writer). **Fix then:** match the constraint name in `error.message`, or add `if v_m.score_source is not null` to the RPC. **Reason deferred:** unreachable today — nothing writes `score_source` until 4.8, and 4-6a is explicitly scoped out of the manual score. [edge, reviewer-confirmed against 0010] _(✅ RESOLVED in Story 4.8 via DECISION I — `bind.ts` now discriminates the constraint name in `error.message` (`match_live_requires_locked_format`→`format_not_declared`, `score_source_guard`→`write_failed`, unknown→`write_failed`). Path re-confirmed UNREACHABLE via bind, so this is robustness, not a live-bug fix.)_
- [ ] **`bind_match_demo`'s idempotent short-circuit precedes the safety guards — a post-bind `anomalous` demo, or a bound `rolled_back` match, returns `{ok:true}`.** `[supabase/migrations/0016_bind_and_score.sql:172-175]` The short-circuit fires before the state guard (`:194`) and the `anomalous` guard (`:211`). DECISION M's ordering is CORRECT for the retry case it was designed for (a dropped HTTP response must not become a permanent `not_bindable`), but its justification enumerates only *"a bound match is `pending` (or `resolved`)"*, while the `not_bindable` clause refuses five states. So: (a) a demo flagged `anomalous` by a re-parse AFTER binding still returns `{ok:true, idempotent:true}` — DECISION H's *"a held demo does not publish"* guards the transition, never the state; (b) a retry against a bound-but-`rolled_back` match returns `{ok:true, idempotent:true, state:'rolled_back'}` → HTTP 200. Mitigated: `state` is returned, so a caller CAN discriminate, and nothing publishes until 4-6b. **Intended home:** Story 4.7 (which creates `rolled_back` and owns the re-point). **Reason deferred:** unreachable until 4.7 lands; and re-ordering the short-circuit would re-break the retry case it exists to fix — 4.7 should decide the interaction with its un-bind path in one go. [blind]

## Deferred from: dev-story of 4-6a (bind demo→match + demo-derived score) (2026-07-16)

_Surfaced while IMPLEMENTING 4-6a (not at review). Two were found by the story's own mandated gates — the real-demo verification and the live-QA BAR — which is precisely what those gates are for._

- [ ] **⭐ `bind_match_demo` does NOT verify that the demo's players ARE the match's competitors.** `[supabase/migrations/0016_bind_and_score.sql]` The RPC binds `(match, demo)` on the admin's say-so and checks the match state, the format lock, the anomaly flag and the stat-row count — but never that the demo's `stat_row.steamid64`s correspond to `match.competitor_a`/`competitor_b`. A mis-click binds *Ana vs Bea's* demo to *Carla vs Diana's* match and it succeeds: the match goes `pending` carrying a score derived from two people who never played it. **Found at THE BAR:** the live-QA's random draw separated the demo's two real duelists, and binding their demo to an unrelated match was accepted — the run only became faithful after the draw was re-seeded to pair them. **Intended home: Story 4-6b**, and it is close to forced there — 4-6b maps `stat_row.rounds_won` onto `score_a`/`score_b` *via the competitor seats*, so it must resolve each seat's player anyway; if the demo's ids aren't the seats, that mapping has no answer and 4-6b cannot write a correct score. The guard is nearly free at that point (`every stat_row.steamid64 ∈ {seat_a.steamid64, seat_b.steamid64}`), and it is the natural place for the data-integrity **M5** assertion (`review-data-integrity.md:285-314`) too. **Reason deferred:** 4-6a's guard list is spec'd explicitly in the story's Task 2 and does not include it; adding an unspecced guard to the story that was *already split for size* is exactly the scope creep the split existed to prevent. ⚠ Nothing is wrong today — no score is written until 4-6b — but 4-6b **must not** assume the binding is semantically valid just because it exists.
- [ ] **`Kills`/`Deaths` are inflated by MatchZy's discarded pre-match round — `rounds_won` is not.** `[worker/ingest/parse.go]` Every real demo opens with a round that is played, won, and then thrown away when MatchZy restarts the game (`MatchStartedChanged true→false→true`, `ScoreUpdated 1→0`), and critically `IsWarmupPeriod()` is **false** for it, so the Kill handler's warmup skip does not catch it. Story 4-6a's `rounds_won` is immune by construction (DECISION L keys the tally on the game's own round counter, which the restart rewinds), but the 3.3 kill/death tally still counts that round. **Directly observable in the 4-6a live-QA rows:** `cuatro-luisito` → `Σkills = 16` over **15** real rounds, while `Σrounds_won = 15`. This is also the true explanation of the "`16==16` balanced" reading recorded at the 3.3 live-QA. **Intended home:** Epic 5's rich-stat derivation (FR-18: the story that must make K/D, ADR, HS% and KAST *accurate*, and which already rewrites this handler), or a small `worker/ingest` touch. It also bears directly on **deferred-work's Σkills==Σdeaths conservation-gate item** (3.4) — the discarded round perturbs both sides of that gate. **Reason deferred:** out of 4-6a's scope by explicit instruction ("you add `rounds_won` and nothing else"); changing K/D would alter shipped Story-3.3 behaviour and its live-QA baseline, which is a decision for the story that owns those stats. Low harm today: K/D is not yet consumed by any published surface (the leaderboard is Epic 5).
- [ ] **The live-QA bracket fixture cannot host a real demo without tripping the anomaly gate.** `[supabase/fixtures/live-qa-bracket-seed.sql]` It rosters 11 *synthetic* players (`…886xx`), but every real demo in `demos/` is played by real SteamID64s — so a real ingest against it is (correctly) flagged `ANOMALOUS/unreconciled` by the 3.4 gate, and 4-6a's DECISION H then refuses to bind it. 4-6a's BAR worked around this by rostering the demo's two real duelists before generation (13 players — still inside the 8..16 range, still not a power of two). **Intended home:** a fixture touch whenever the next story needs a real demo on a real bracket (4-6b's Aprobar BAR will need exactly this). **Reason deferred:** the workaround is two INSERTs and the fixture's own header warns it must not be re-run after generation; folding a demo-aware roster into the shared fixture deserves its own think (which demos? which seeds?) rather than a drive-by edit.

## Deferred from: create-story of 4-6 "Aprobar" atomic publish (2026-07-16)

_Story 4.6 was contexted whole (3 parallel research agents: architecture / PRD-UX / codebase, all Opus 4.8), measured ~2-3x any prior story, and **split by Cuatro** into **4-6a** (parser + `stat_row.rounds_won` + `bind_match_demo`, which lands `pending`) and **4-6b** (`approve_match` + `timeline_feed` + the emit). The split un-does the Story-3.7 absorption cleanly — 4-6a essentially IS Story 3.7, finally buildable now that `match` exists. Three of the four §8 effects turned out not to be integration work at all; the analysis of record is the `4-6b` inline entry in `sprint-status.yaml`. Two decisions taken at contexting create the follow-ups below._

- [ ] **The `bracket_advance` feed entry has no writer — forfeit and bye advances post NOTHING to the timeline.** FR-31 (`prd.md:415`) is explicit: *"An Approved Match result, **a Bracket advance**, and an Award reveal **each post an entry** to the feed"*, and FR-7 (`prd.md:188`) says *"Advancing a Match updates both the Winners/Losers routing and the Timeline feed."* The mock renders them as two distinct cards with different rail colors (a blue advance + a green result, `mock-home.html:565-591`), and `DESIGN.md:99` provisions a gold `railNodeReveal` for the third type. **Decision (Cuatro, 2026-07-16):** Story 4-6b posts **one `match_result` entry only** — §8 (`SOLUTION-DESIGN.md:374`) and the epic AC (`epics.md:751`) both say *"entry"* singular, and the ACs are the contract. 4-6b provisions the `entry_type` enum with all three FR-31 values but writes one. **Intended home:** a story that can land the `bracket_advance` writer **uniformly** — it must also cover Story 4.3's walkover cascades and Story 4.5's forfeit/bye advances, which shipped *before the table existed* and therefore post nothing; bolting it onto Aprobar alone would cover only the Aprobar path and leave the other two silently missing. Most naturally Story 5.6 (the feed read surface, the first consumer that will notice the gap) or a small Epic-5 slice. **Reason deferred:** the entry type is not 4-6b's to own — 4-6b owns the *Aprobar* post (`epics.md:757-759`), and `epics.md:167` maps all FR-31 posting to 4.6 only because no one noticed the table did not exist. ⚠ Award reveals are in the same position: no Epic-6 story mentions the feed, yet FR-31 and the gold rail token both require a reveal entry.
- [ ] **The accept-anomaly (`Anomalous → Pending`) route is ORPHANED between Epic 3 and Epic 4.** `0008_demo_validation.sql:18-19` assigns it by name: *"OUT OF SCOPE — do NOT add here: the admin **accept-anomaly (`Anomalous → Pending`)** + Aprobar publish command routes — **Epic 4** (audited admin routes, AD-8; **Aprobar = Story 4.6**)."* But **no Epic-4 story AC mentions it**, and Story 4.9's route list (`epics.md:812`) — *"generate bracket, advance, mark walkover, approve, re-parse, rollback, declare format, start ceremony, grant role"* — **omits it**. Story 3.4's AC2 (`epics.md:531`) requires it: *"an admin must explicitly accept the anomaly (logged) to advance it to `Pending`."* **Decision (Cuatro, 2026-07-16):** Story 4-6a **refuses** to bind an `anomalous` demo (a one-line guard — `demo.validation_state` and `stat_row.status` are deliberately orthogonal, `0008:32`) and does **not** build the accept path. **Intended home:** Story 4.9 (audited admin command routes — the natural owner, and its route list should be corrected to include it), or a small Epic-3 follow-up. **Reason deferred:** it is a distinct audited admin decision with its own AC in Story 3.4, not part of the Aprobar transaction; folding it into 4-6a would smuggle an unspecced admin action into a story that already needed splitting. ⚠ Two related traps for whoever builds it: (1) the admin mock renders the anomaly banner, the `Pendiente` pill and an **enabled** `Aprobar` **simultaneously** (`mock-admin.html:318-415`) — a state 3.4's two-step gate says cannot exist unless the anomaly was already accepted; (2) the mock grants a **second, undocumented override branch** — *"o confirma que Vale estuvo ausente-inactivo"* (`:397`) → *"marcado como ausente-inactivo y descalificado"* (`:459`) — i.e. accepting sets `idle_dq`, which is FR-21 / Story 5.4 territory (`epics.md:893-895`), not anything Epic 4's ACs cover.

## Deferred from: code review of 4-5-bye-and-forfeit-walkover-with-grace-timer (2026-07-15)

- [ ] **`mark_walkover` trusts the both-seats-filled invariant — a NULL competitor yields a NULL loser.** `[supabase/migrations/0015_walkover_grace.sql:302-308]` The `bad_winner` guard uses `is distinct from`, so if `competitor_a IS NULL` and `p_winner_entry = competitor_b`, the guard passes and `v_loser := …else competitor_a` = NULL — the forfeit advances with a NULL loser and the audit row records `forfeiting_entry = NULL`. **Intended home:** fold into a future `match` hardening touch, or Story 4.7 (which adds the unseat/rollback path that would first make this reachable). **Reason deferred:** unreachable today — only `begin_match_grace` enters `awaiting_grace` and it enforces both seats (`:164`); no unseat path exists. Cheap defense-in-depth: re-assert both seats non-NULL under the lock.
- [ ] **`advance_match`'s bare `P0001` raises map to `write_failed`/500, not `advance_refused`/409.** `[lib/match/walkover.ts:165-175]` + `[supabase/migrations/0013_advance.sql:711,721,782]` Only advance's `{ok:false}` RETURN is converted to IC902 → `advance_refused`; its `P0001` raise paths (dangling edge / void destination / hop-cap / pass-2 taken) fall through to 500 even though the forfeit was rolled back. **Intended home:** Story 4.9 (the shared command-route helper + central SQLSTATE map, which already owns advance_match's five bare P0001s — deferred-work.md:139). **Reason deferred:** those raise paths are genuine bracket corruption (not the `slot_taken` the story targets), so a 500 is defensible; centralizing them is 4.9's job, not 4.5's.
- [ ] **`resume_match` on HTTP `DELETE` requires a JSON body.** `[app/api/admin/match/grace/route.ts:98-113]` The `DELETE` verb parses `{ match_id }` from the body; a proxy/gateway that strips `DELETE` bodies would make resume unreachable (400 `invalid_json`). **Intended home:** revisit if a body-stripping hop is ever introduced — switch resume to POST or carry `match_id` as a query param. **Reason deferred:** low-risk — `fetch()` sends DELETE bodies fine and the deploy is Vercel with no exotic proxy (DECISION F explicitly left route shape to the dev's discretion).
- [ ] **`mark_walkover`'s `not_awaiting` guard (and the grace-gate NULL fail-closed branch) have no pgTAP coverage.** `[supabase/migrations/0015_walkover_grace.sql:278-280,290-293]` The three *mandated* mutations (grace gate / terminal guard / {ok:false} raise) all redden, but these two guards would not. **Intended home:** a future `plan(32→33)` bump on `0015_walkover_grace_test.sql`. **Reason deferred:** low value — `not_awaiting` is trivial and `resume_match`'s identical guard IS tested; the NULL branch is unreachable in a well-formed lifecycle.
- [ ] **AD-23's terminal guard is asymmetric — `resolved→forfeit` is not blocked by construction.** `[supabase/migrations/0015_walkover_grace.sql:109]` The guard treats only `bye`/`forfeit`/`void` as terminal, so it blocks `forfeit→resolved` (the direction a late demo hits) but the reverse (`resolved→forfeit`) is held today only by `mark_walkover`'s `not_awaiting` guard, not by construction. AD-23's "Resolved and Forfeit cannot coexist by construction" is therefore half-true until the demo/Aprobar side lands. **Intended home:** ~~Story 4.6~~ → **Story 4-6b** (re-homed 2026-07-16 when 4.6 split). **NOT 4-6a**, deliberately: 4-6a lands `pending`, never `resolved`, so the writer this guard constrains still does not exist — and the story's own scope boundary is explicit that the `IC904` half ships **with** the writer that can prove it, because a guard landed before its writer is an untested backstop (the exact `deferred-work.md` mutation-blindness lesson). ⚠ For whoever builds it: the obvious fix — adding `resolved` to 4.5's `IC901` terminal set — **would break Story 4.7**, whose rollback returns a match `Resolved → Pending` (SPINE:312, FR-14). It needs the surgical form (block only `resolved → bye|forfeit|void`, with a `lives_ok` pinning `resolved → pending`). **Reason deferred:** `resolved`/`manual_resolved` are 4-6b/4.8's states; correct for 4.5's and 4-6a's scope.

## Deferred from: code review of story-1.1 (2026-06-30)

- [ ] **First-build client-bundle leak scan (AC3 — deviation D1).** When the first Next.js app/build exists (Story 1.2+), run the production client-bundle scan to empirically confirm no server-only secret (Supabase service-role key, R2 write creds, worker↔MatchZy shared secret) is bundled. **Intended home:** Story 7.5 build-handoff checklist gate. **Reason deferred:** no app exists yet at Story 1.1; the structural guarantee (only `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` are client-exposed) stands in until the first real build.

## Deferred from: code review of story-1.2 (2026-06-30)

- [ ] **`granted_by` audit invariant unenforced (self-grant / grant-by-non-admin).** `app_role.granted_by` (`[supabase/migrations/0001_core_schema.sql:48]`) only checks existence in `player` via FK — nothing prevents `granted_by = steamid64` (self-grant) or a grantor who is not an admin. **Intended home:** **Story 2.4** (roles admin gating — the grant/revoke server route) _(re-homed 2026-07-01 from Story 1.3)_. **Reason deferred:** this is an authorization invariant on a *write*, and every `app_role` write goes through the service-role, which **bypasses RLS** — so RLS / `is_admin()` structurally cannot enforce it. Enforcement belongs to the Epic-2 grant/revoke route (Story 2.4), which re-verifies `is_admin()` server-side and can reject self-grants / non-admin grantors before writing `app_role`. **Confirmed by Story 1.3** (migration 0002, `done`): it deliberately added no CHECK/trigger for this — a CHECK can't cheaply prove "grantor is an admin," and a trigger is over-engineering for a casual private-friends event. [Story 1.3 Dev Notes "OUT of scope" + Completion Notes.]
- [ ] **Optional non-empty (`char_length > 0`) guards on text columns.** `display_name`, `season.name`, `tournament.name`, `avatar_url` (`[supabase/migrations/0001_core_schema.sql]`) are unbounded `text` with no non-empty CHECK, so `''` is a valid value. **Intended home:** future hardening pass (or fold into a later schema touch). **Reason deferred:** low value for a casual private-friends event; revisit only if empty names surface in the UI.

## Deferred from: second-opinion code review of story-1.2 (2026-07-01)

_A fresh independent review pass re-confirmed both story-1.2 deferrals above are still valid and added no new deferrals. The one new item it surfaced — missing `UNIQUE` on `season.name` / `tournament(season_id, name)` — is a **decision-needed**, not a defer; its disposition is tracked in the story file's "Review Findings — Second-Opinion Pass (2026-07-01)" section pending Cuatro's call. It also empirically **refuted** a claimed HIGH (trailing-newline regex bypass) against a live PostgreSQL 18.4 cluster — no action needed._

## Deferred from: code review of story-1.3 (2026-07-01)

- [ ] **No catalog-level guard enforcing the ENABLE+FORCE+grant convention for future tables.** `0002_rls_test.sql` Section A (`[supabase/tests/0002_rls_test.sql:37]`) hard-codes the four current tables by name when asserting `relforcerowsecurity = true`; a table added by a later migration that forgets `FORCE` (or its base-table grants) would pass this suite unnoticed, quietly breaking the "framework provably bites" guarantee (AC #5) for the forward roadmap. Add a generic catalog assertion — e.g. "no base table in `public` has `relforcerowsecurity = false`" — so the convention bites for every future table, not just the four. **Intended home:** Story 1.4 (audit/snapshot, migration 0003) or a shared pgTAP helper. **Reason deferred:** forward-looking framework hardening, not a defect in the four-table 1.3 slice. _(✅ RESOLVED in Story 1.4 — `0003_audit_snapshot_test.sql` Section A2 adds exactly this generic catalog `FORCE`-guard.)_

## Deferred from: code review of story-1.4 (2026-07-01)

- [ ] **`on delete cascade` on `tournament_id` can cascade-wipe the audit trail + snapshots.** `audit_log.tournament_id` (`[supabase/migrations/0003_audit_snapshot.sql:23]`) and `stat_snapshot.tournament_id` (`[…:35]`) use `on delete cascade`, so deleting a tournament silently erases its entire append-only audit trail and all snapshots — an indirect DELETE path that the no-UPDATE/DELETE-grant model does not cover, and which contrasts with `actor_steamid64`'s deliberate `NO ACTION`. **Intended home:** Epic 4+ (when/if tournament deletion becomes a reachable operation). **Reason deferred:** columns are verbatim from SOLUTION-DESIGN §3; Dev Notes state "tournaments are not deleted in v1," so no reachable path defeats append-only today. Revisit `cascade`→`restrict` on `audit_log` when tournament deletion is introduced.
- [ ] **Loose column domains on the append-only tables (no CHECK constraints).** `action` is enum-in-comment-only free text (`[supabase/migrations/0003_audit_snapshot.sql:25]`); `content_sha256` accepts non-hex / any length (`[…:37]`); `kills`/`rounds_played` allow negatives (`[…:49-50]`); `stats_int`/`h2h`/`detail` accept any `jsonb` shape (scalar/array/`'null'`). **Intended home:** future hardening pass (or fold into a later schema touch). **Reason deferred:** all columns are verbatim from AD-19/§3, which intentionally keeps the jsonb open so the offline verifier consumes the exact captured shape; the sole writer is a service-role server route that controls the vocabulary. Low value for a casual private-friends event.

## Deferred from: code review of 2-1-steam-openid-2-0-server-side-login (2026-07-02)

- [ ] **Nonce double-submit uses an identical value in the cookie and the URL, and carries no embedded expiry.** The signed CSRF nonce is set both as the `steam_openid_nonce` httpOnly cookie AND carried in `openid.return_to` (`[app/auth/steam/login/route.ts:22-34]`); Steam echoes the full URL back, so any leak of the callback URL (browser history, `Referer` to third-party resources on `/`, proxy/access logs) discloses the same value that is the cookie's "secret" half — weakening the double-submit guarantee the code comment claims. The token itself (`[lib/steam/nonce.ts]`) embeds no timestamp; only the 10-min cookie `maxAge` bounds freshness, and the value isn't tracked server-side as consumed (the cookie IS deleted after use, giving practical single-use). **Intended home:** auth hardening pass (Epic 7) or Story 2.4. **Reason deferred:** for a casual private group the httpOnly cookie + HMAC signature + Steam signing the full `return_to` + cookie-deletion-after-use make this a defense-in-depth gap, not an exploitable hole. Revisit by using a distinct cookie-half value or an embedded expiry if auth surface widens.
- [ ] **`ensureAuthUser` doesn't re-assert `app_metadata.steamid64` on the "already exists" path.** `[lib/auth/session.ts:46-56]` sets the identity claim only at `createUser`; on the exists branch it no-ops. A Supabase auth user that pre-exists WITHOUT the claim (created manually, by an earlier code version, or a partial prior run) would mint a session whose JWT has no `steamid64`, so `jwt_steamid64()` (migration 0002) returns null and RLS fails closed silently for that user. **Intended home:** fold into Story 2.2 (which already touches `app_metadata` for role) via an `updateUserById` claim-upsert on the exists path. **Reason deferred:** low probability in a fresh single-environment project where every user is created by this exact code path; becomes worth fixing when role binding (2.2) makes a claimless session actively confusing.
- [ ] **Over-broad "already exists" classification in `isAlreadyExists`.** `[lib/auth/session.ts:30-39]` treats any `error.status === 422` as duplicate-user, but Supabase returns 422 for a range of validation failures (disallowed email domain, weak input, etc.). A genuine non-duplicate `createUser` failure would be swallowed, then surface less precisely at the downstream `generateLink` call. **Intended home:** same live-QA pass that resolves the AC5 `verifyOtp` decision. **Reason deferred:** the correct narrowing (prefer `code === 'email_exists'`, drop the bare `status === 422`) depends on the live Supabase error shapes — resolve empirically alongside the AC5 session-mint QA rather than guessing statically.
- [ ] **`display_name` from the Steam persona is written unbounded and un-length/charset-checked.** `[lib/players.ts:42,68]` passes `personaname` (only end-trimmed) into `player.display_name`, a `text` column with no length CHECK (`0001_core_schema.sql`). Very long, RTL, control-char, or 4-byte-unicode persona names land verbatim. **Intended home:** viewer-surface hardening (Epic 5) or a schema touch. **Reason deferred:** cosmetic for a casual group; React escapes on render, so no XSS today; a length cap is reasonable when leaderboards render names.
- [ ] **`check_authentication` response could also assert the OpenID namespace.** `[lib/steam/openid.ts:90]` requires `parsed['is_valid'] === 'true'` but not `parsed['ns'] === OPENID_NS`. **Intended home:** auth hardening pass. **Reason deferred:** the endpoint is a hardcoded HTTPS constant (no SSRF/attacker-controlled URL), so the added assertion is belt-and-suspenders, not a fix for a reachable hole.
- [ ] **Home page depends on a remote Steam button image.** `[app/page.tsx:39-44]` loads Valve's "Sign in through Steam" asset from `community.fastly.steamstatic.com` at render. **Intended home:** self-host the asset (small static file) in a UI polish pass. **Reason deferred:** cosmetic — the `<a href="/auth/steam/login">` link still works if the image 404s, so login is never blocked.

## Deferred from: code review of 2-2-bind-identity-and-role-into-app-metadata (2026-07-02)

- [ ] **`app_role` role value is cast `as Role` with no runtime validation.** `resolveRole` returns `existing.role as Role` / `raced.role as Role` (`[lib/auth/roles.ts:47-57]`) — a blind TypeScript cast, not a runtime guard. **Intended home:** revisit when the role set expands (e.g. a Story 2.4/beyond `superadmin`) or if `app_role` ever gains a non-Data-API writer. **Reason deferred:** fully guarded TODAY by migration 0001's `CHECK (role in ('admin','viewer'))` plus the strict `is_admin()` (`= 'admin'` → any unexpected value fails closed to non-admin), so there is no reachable defect. The only obligation is to keep the `Role` union in lockstep with the DB CHECK; a runtime guard now has ambiguous forward-compat semantics (would it downgrade a future legit role?), so it's not worth adding until the role set actually changes.
- [ ] **`23505` race re-read swallows its own error + depends on the exact PostgREST `code` string.** In `resolveRole` the concurrent-first-login recovery path (`[lib/auth/roles.ts:64-74]`) destructures only `{ data: raced }` (drops `error`) and branches on `insertError.code === '23505'`; a fault during the re-read is reported as "insert failed / duplicate key", and if PostgREST ever surfaces the unique violation under a different `code`, a legit concurrent first-login throws. **Intended home:** auth hardening pass. **Reason deferred:** the race requires two concurrent first-logins of the SAME SteamID64 — effectively impossible for a private friends roster (one person per Steam account). It fails closed (user retries). Minor diagnostic/robustness nit, not a defect.
- [ ] **A fully-malformed non-empty `ADMIN_STEAMIDS` silently yields an empty allowlist (everyone `viewer`) with no operator signal.** `parseAdminAllowlist` (`[lib/auth/roles.ts:26-34]`) drops every non-17-digit entry; if the whole var is mis-formatted, no admin is ever bootstrapped and nothing warns. **Intended home:** a small `console.warn` when `ADMIN_STEAMIDS` is set but parses empty (ops nicety). **Reason deferred:** conformant with AC7 (fail-closed to `viewer` by design); the live-QA gate (Cuatro's login must show `admin`) is the backstop, and for a casual single-admin v1 the silence is low-risk.

## Deferred from: code review of 2-3-canonical-steamid64-capture-and-display-name-tolerance (2026-07-03)

- [ ] **`defaultGet`'s real transport (fetch + timeout + `!res.ok` throw + `res.json()`) is never exercised by any test.** Every `fetchSteamProfile` test injects a mock `get`, so the default transport (`[lib/players.ts:29-38]`) — including the `AbortSignal.timeout(10s)`, the non-200 throw, and the JSON parse — ships unverified; a regression there (e.g. someone drops the `!res.ok` throw) would pass green. **Intended home:** a thin transport test (mock global `fetch`) or an auth hardening pass. **Reason deferred:** thin-glue coverage; mirrors 2.1's equally-untested `HttpPost` default seam — consistent precedent, low value for a casual event.
- [ ] **An unhydrated FIRST login writes a permanent 17-digit placeholder as `display_name`.** A brand-new player who logs in during a Steam outage (or before `STEAM_API_KEY` is configured — `env.steamApiKey()` returns `''`) gets a placeholder row `{display_name: steamid64, avatar_url: null}` (`[lib/players.ts:108-111]`; `display_name` is NOT NULL so *some* value is forced). It self-heals only if that same human logs in again while Steam is healthy — otherwise the id-as-name persists and is user-facing. **Intended home:** **Story 2.6** (unreconciled-SteamID64 list / roster-side reconciliation surface). **Reason deferred:** an accepted AC6 design consequence (login must produce a row; the null-path DO-NOTHING is what protects *existing* good values); heals on the next healthy login, and a backfill/reconciliation path is squarely 2.6's job, not 2.3's.
- [ ] **`ignoreDuplicates:true` makes a DO-NOTHING no-op indistinguishable from a real insert.** On the unhydrated path (`[lib/players.ts:108-111]`), `ON CONFLICT DO NOTHING` returns `error: null` whether it inserted a new placeholder row or silently skipped an existing one, and `upsertPlayer` returns `void` either way — so a genuinely-skipped write is reported as success. **Intended home:** revisit if/when a second UNIQUE constraint is added to `player`. **Reason deferred:** benign today — `player` has exactly one key (`steamid64` PK), so the only conflict target is the intended one; becomes a latent silent-skip footgun only the moment a second unique constraint on `player` lands.
- [ ] **AC6 avatar-only partial-200 clobber (sub-case of the resolved decision).** A Steam **200** with a *present* persona but a *missing* `avatarfull` (`[lib/players.ts:70]`) yields a truthy profile with `avatarUrl: null`, so the normal UPDATE path overwrites a stored good `avatar_url` with `null`. The code-review decision (Cuatro, 2026-07-03) chose **Option A** — fix the visibly-bad *name* clobber now (blank persona → `null`), and defer this rarer avatar-only case. **Intended home:** Epic 5 (viewer-surface hardening, alongside the `display_name` length/charset cap). **Reason deferred:** a nulled avatar is far less bad than a name becoming a 17-digit number, and a present-persona/missing-avatar 200 is essentially a malformed response; per-field preserve (Option B) needs a read-modify-write / DB-side COALESCE that this casual-event slice does not warrant. Revisit when avatars render on leaderboards.

## Deferred from: code review of 2-4-roles-admin-gating-and-revoke-invalidates-session (2026-07-05)

- [ ] **`findAuthUserIdByEmail` silently no-ops the mirror-heal past the 10,000-user pagination cap.** The lookup pages `listUsers` at `LIST_USERS_PER_PAGE=200` up to `LIST_USERS_MAX_PAGES=50` (`[lib/auth/roles.ts:184-197]`); a target sitting on page 51+ falls through to `return null`, and `healRoleMirror` treats `null` identically to "no auth user yet — deliberate no-op success" (`[lib/auth/roles.ts:168-169]`). So a real, logged-in user whose JWT mirror needs re-minting is indistinguishable from one who never logged in — the refresh-convergence half of a grant/revoke silently never happens, with no error and no log. **Intended home:** revisit if the auth-user count ever approaches 5-figures (or when a `getUserByEmail`/indexed lookup becomes available in `@supabase/auth-js`). **Reason deferred:** 10,000 users is orders of magnitude beyond this private friends event and the cap is a documented backstop; enforcement is unaffected (Option-A `requireAdmin` re-reads `app_role` regardless of the mirror), so the only residual is delayed `is_admin()` convergence on RLS-read paths for an out-of-scale user. Optional cheap hardening when touched: `console.warn` when the loop exhausts `MAX_PAGES` without a match, so a silent miss becomes visible.

## Deferred from: code review of 2-5-registration-window-and-roster-management (2026-07-05)

- [ ] **Roster add/remove + self-enroll lock is a read-then-write state gate, not a `WHERE`-clause DB-write guard.** `adminAddPlayer`/`removePlayer` gate on `tournament.state` via a prior `requireMutableTournament` SELECT, then issue the `roster_entry` write with no cross-table state predicate (`[lib/roster.ts:250-303]`); `enrollSelf` relies on the route's earlier `resolveOpenTournament` read with no guard on its own upsert (`[lib/roster.ts:214-217]`). Only `setRegistrationOpen` carries the real `.in('state', ROSTER_MUTABLE_STATES)` guard on its own-table UPDATE. AC7 literally requires the lock "enforced both by the route's state check **and** by the write's `WHERE`-clause state guard" — a TOCTOU window exists between the state read and the roster write. **Intended home:** the Epic-4 bracket-generation slice (add a correlated `WHERE EXISTS (… tournament.state IN (…))` / an RPC when a concurrent `bracket_live` writer first exists). **Reason deferred:** the dev knowingly deferred the `removePlayer` half (story Completion Notes); until Epic 4 ships bracket generation there is NO concurrent writer able to flip `tournament.state`, so the window is currently unexploitable and the route+read gate blocks the ordinary locked case. Extend the same deferral note to cover `enrollSelf`.
- [ ] **pgTAP `0004_roster_test.sql` has no reactivation round-trip proving `removed → active` via the upsert `ON CONFLICT DO UPDATE status='active'`.** The suite proves `active → removed` (soft-delete) and the grant matrix (service_role holds UPDATE), and the Vitest suite asserts the upsert shape/onConflict key (`[lib/roster.test.ts:170-178]`), but no SQL-layer test flips a `removed` row back to `active` — the exact behavior `enrollSelf`/`adminAddPlayer` depend on for re-enroll. **Intended home:** fold a `plan(38)→plan(39)` assertion into `0004_roster_test.sql` (or a later roster touch). **Reason deferred:** the behavior is covered across the unit + grant layers; a dedicated SQL round-trip is proof-completeness hardening, not a correctness fix, and adding it now perturbs the story's green `plan(38)` baseline for low marginal value.

## Deferred from: create-story of 2-6-unreconciled-steamid64-list-roster-side (2026-07-06)

- [ ] **Story 2.6 (unreconciled-SteamID64 list, roster side) re-sequenced out of Epic 2 into Epic 3 — build AFTER Story 3.4.** During `bmad-create-story` for 2.6 (the last Epic 2 story), exhaustive analysis (3 research subagents: architecture / PRD-UX / codebase) established the story has **no data source in the current schema**. AD-4 defines the "unreconciled" list as a derived **left-join**, not a stored table (`ARCHITECTURE-SPINE.md:98` — *"lands in an 'unreconciled' list (left-join)"*; `SOLUTION-DESIGN.md:144` — `stat_row.steamid64` is deliberately **NOT a FK** so unmatched IDs are insertable), and the table it left-joins over — `stat_row` — is not created until **Story 3.3** and is only populated/flagged by **Story 3.4** (Epic 3, all `backlog`; `stat_row` is written solely by the Go worker per AD-2). A SQL view over a non-existent relation is impossible, and reconciliation is *mechanical* — adding a SteamID64 to the roster auto-attaches its orphaned stats via the shared FK-less join key (no rewrite; largely already delivered by Story 2.5's `adminAddPlayer`). Story 3.4 already names 2.6 by number as its destination (`epics.md:535`). Confirmed by grep: no `unreconciled`/`stat_row`/`match` table exists in migrations 0001–0004. **Decision (Cuatro, 2026-07-06):** DEFER — `2-6` is set to status `deferred` in sprint-status (documented in that file's STATUS DEFINITIONS) so it is not auto-picked as "next"; build it immediately after Story 3.4 when `stat_row` + the ingest producer exist. **Intended home:** Epic 3, post-Story-3.4 (or fold the roster-side reconcile surface into a 3.x slice). **Reason deferred:** building now would require either a view over a missing table (impossible) or pulling Epic 3's single-writer `stat_row`/`match` schema (AD-2) forward into Epic 2, guaranteeing rework when Story 3.3 lands the real stat schema — over-engineering for a casual private event. Epic 2 is otherwise complete (5/6: Stories 2.1–2.5 done). No story file, migration, or code was produced. [Analysis sources: epics.md#Story 2.6 (lines 422–438) + #Story 3.4 (517–537); FR Coverage Map epics.md:138 (FR-2 = E1/E2/E3); prd.md:142 (unreconciled = parsed *stat rows*, showing SteamID64 + source Match, excluded from leaderboards until reconciled); AD-4 epics.md:82; UX-DR57 = EXPERIENCE.md "Identity reconciliation" State Pattern (one-tap manual linkage), spine-only, no mockup.]

- [ ] **The Story-2.3 placeholder-`display_name` deferral rides along to 2.6's new home.** The 2.3 review item — *"an unhydrated FIRST login writes a permanent 17-digit placeholder as `display_name` … Intended home: Story 2.6 (roster-side reconciliation surface)"* (see the 2-3 deferral above) — moves with Story 2.6 to Epic 3 / post-Story-3.4. It is a roster-side identity-completeness concern, adjacent to (but distinct from) the unreconciled-stat-rows list; resolve it wherever the roster-side reconciliation surface is ultimately built.

## Deferred from: code review of story-3.1 (2026-07-06)

_Severity calibrated to the private-tournament threat model: every ingest endpoint is shared-secret or `requireAdmin` gated, feeding from the operator's own MatchZy server — no public/anonymous surface. The one substantive hardening item (ingest size / decompression-bomb caps) was surfaced as a **decision-needed** (tracked in the story's Review Findings), not a defer. All 7 ACs PASS._

- [ ] **Presign endpoint shares `WORKER_MATCHZY_SHARED_SECRET` across two trust boundaries.** `[worker/ingest/server.go:113]` `/ingest/presign` (mints an R2-writable presigned PUT URL) is gated by `authOK` using the exact same secret as the MatchZy server-to-server receiver. Per the README the admin *browser* is the intended presign consumer — which would require exposing that secret to client context, while it is simultaneously the credential a rented MatchZy CS2 server uses. **Intended home:** Epic 5 (the admin manual-upload UI slice, where the presign consumer is actually built). **Reason deferred:** no browser consumer exists in this slice (Task 9 QA calls presign via `curl`); the admin-upload UI is explicitly Epic-5-deferred. When it lands, route presign through an app-side `requireAdmin` proxy (secret stays server-side) or issue a separate presign secret so the two trust boundaries don't share one credential.
- [ ] **`/api/ingest/register` trusts an unverified client-supplied `storage_key` (orphan / mismatch / browser-PUT-then-never-register).** `[lib/ingest.ts:32,54; app/api/ingest/register/route.ts:47]` `parseRegisterBody` only checks `storage_key` is a non-empty string; `recordManualUpload` inserts it verbatim with no confirmation the object exists in R2, was actually PUT, or was the key minted by `/ingest/presign` for this `match_id`. A mismatched/never-uploaded key → a `demo` row pointing at the wrong/absent object; a completed browser PUT with no subsequent register → an orphan R2 object with no row. **Intended home:** Epic 5 (admin-upload UI). **Reason deferred:** `requireAdmin`-gated (operator-trusted, so realistically an operator-error not an attack); verifying the key would need the app to HEAD R2 (it deliberately holds no R2 creds — the bypass-Vercel invariant) or to bind the registered key to a worker-issued presign token — design work that belongs with the real Epic 5 flow. The worker paths (MatchZy/CLI) already store-before-record and are unaffected.
- [ ] **Shared secret carried in the `?token=` query is logged.** `[worker/ingest/server.go:47]` The query-param carrier is a deliberate fallback (the MatchZy docs snapshot shows no auth-header cvar), but a secret in the URL lands in worker/proxy/CDN access logs and the CS-server config. **Intended home:** Epic 7 (deploy hardening). **Reason deferred:** deploy-time/operational, and the carrier is pinned at deploy — prefer the `Authorization` header if the deployed MatchZy build supports a configurable header, and rotate the secret if logs were exposed.
- [ ] **MatchZy gzip/zip paths don't re-validate the decompressed content is a `PBDEMS2` .dem.** `[worker/ingest/decompress.go:42,78]` The raw branch checks the `PBDEMS2` magic and the CLI enforces `checkDemHeader`, but the gzip branch streams whatever the gzip contains and the zip branch selects any `*.dem`-named entry — neither re-checks the decompressed magic, so a gzip of garbage / a zip whose `.dem` entry is arbitrary bytes is stored + recorded. **Intended home:** optional consistency hardening (fold into a 3.x ingest touch or Epic 7). **Reason deferred:** cheap but marginal — Story 3.2 hashing / 3.3 parsing detect non-demos downstream; requires the secret; low value for a casual event.

## Deferred from: live-QA (Task 9) of story-3.1 (2026-07-10)

- [ ] **The worker cannot reach a hosted Supabase DB from an IPv4-only host — no Supavisor-pooler support.** `buildDatabaseURL` (`[worker/config/config.go:90-95]`) derives the hosted DSN as `db.<ref>.supabase.co:5432`, which Supabase now resolves **IPv6-only** (direct-connection IPv4 was deprecated; IPv4 clients must use the Supavisor pooler `aws-0-<region>.pooler.supabase.com`, user `postgres.<ref>`, port 6543 transaction / 5432 session). Discovered during 3.1 live-QA: `db.<ref>.supabase.co` returned only an `AAAA` record and the QA host (Windows, IPv4-only) could not connect — so live-QA ran the worker's DB writes against the **local** Supabase stack instead (R2 was the real bucket). **Intended home:** Epic-3 infra-prep action item (#3, Cuatro — choose the worker deploy host) and/or Epic 7 (deploy hardening). **Reason deferred:** not a 3.1 acquisition-logic defect and it does not block the local-verify Deploy-posture decision; it becomes a hard requirement the moment the worker is deployed to talk to the hosted DB. Fix options: (a) add a pooler-DSN branch to `buildDatabaseURL` (accept a full `DATABASE_URL`/pooler host override) — the cleanest and also unblocks local-QA on IPv4-only dev boxes; (b) deploy the worker on an IPv6-capable host; (c) buy Supabase's dedicated-IPv4 add-on. The IPv4-only host also can't bind the local stack's default Postgres port when Hyper-V reserves 54322 — a `net stop winnat && net start winnat` (admin) frees it; note this for any local-QA runbook.

## Deferred from: code review of story-3.3 (2026-07-11)

_Baseline `e375b1f`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor. No Critical/High defects; AC1–AC5 satisfied, no out-of-scope leakage. Two items surfaced as **decision-needed** (SteamID64 non-17-digit whole-match abort; missing `recover()` around the parser) are tracked in the story's Review Findings pending Cuatro's call — not deferred here._

- [ ] **`parser_version` UPDATE has no `RowsAffected` check — silent no-op success on a non-existent `demoID`.** `PgxStatRecorder.RecordParse` (`[worker/db/db.go]`) runs `update demo set parser_version=$1 where id=$2` with no rows-affected assertion; on a **zero-row** parse whose `demoID` matches no `demo` row, the transaction commits and returns `nil` (success) though nothing was stamped and no such demo exists. (With non-empty rows the `demo_id references demo(id)` FK catches a bad id, so only the empty-rows + bad-id combination is silent.) **Intended home:** Story 3.8 (bounded async queue). **Reason deferred:** unreachable in the current CLI wiring — `acq.DemoID` is always a valid fresh PK from the same `Acquire` — so there is no reachable defect today; it becomes relevant only when 3.8's queue can pass a stale/wrong `demoID` decoupled from the acquire. Cheap future hardening: assert `RowsAffected() == 1` on the stamp.
- [ ] **`stat_row` is created only for players who appear in a live (non-warmup) `events.Kill` — a 0-kill/0-death player gets no row.** The `get()` helper that first materializes a `PlayerStat` runs only inside the Kill handler (`[worker/ingest/parse.go]`), so a connected player with zero kills AND zero deaths in live rounds produces no row — mildly inconsistent with AC2's "one row per SteamID64 that appeared." **Intended home:** Story 3.4 (validation / roster-reconciliation) + a Task-7 live-QA note. **Reason deferred:** matches the story's own prescribed parse-core sketch (`get()`-in-Kill-handler), near-zero real impact (every player in a real CS2 match dies ≥ once), and 3.4's `Σkills==Σdeaths` gate is unaffected (a 0/0 player is in neither sum). Evaluate Task 7's "count(*) matches real player count" with this in mind; if it matters, seed the player set from a connect/spawn event in Epic 5.

## Deferred from: code review of story-3.5 (2026-07-12)

_Baseline `a3b53b4`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8). **Clean review** — AC1–AC5 + full out-of-scope list PASS; both pgTAP `plan()` counts (0009 `plan(23)`, 0007 `plan(33)`) reconcile. No decision-needed, no patch. 7 findings dismissed as noise (notably the Blind Hunter's "anon evaluates `stat_admin` qual" false positive — `stat_admin` is `to authenticated` only, so anon never evaluates it; and a "`'...'` placeholder descriptions" finding that was an artifact of the diff summarization, not the real files). Two low-severity items deferred below._

- [ ] **Column-blind `grant select on public.stat_row to anon, authenticated` exposes admin-identity + internal columns to anonymous viewers.** The grant (`[supabase/migrations/0009_stat_pending_visibility.sql:35]`) has no column list, so for any `status='approved'` row that passes `stat_view` an anon client can read every column — including `approved_by` (the approving admin's canonical SteamID64) and `approved_at` (`[supabase/migrations/0007_stat_row.sql:48]`), plus all Epic-5 stat shell columns. **Intended home:** Story 4.6 (Aprobar — the story that first populates `approved_by`/`approved_at`) or an Epic-5 viewer-surface hardening pass; add a column-scoped `grant select (…)` or a viewer-facing view if the reviewer identity should stay private. **Reason deferred:** nothing is exposed today — `approved_by`/`approved_at` are null shells until Story 4.6 writes them; the table-wide grant is exactly what the 3.5 spec prescribes (DDL verbatim); and for this casual private-friends group the approving-admin's SteamID64 is not sensitive. Revisit when 4.6 lands the approval write.
- [ ] **`create policy stat_view` is non-idempotent — a migration re-apply raises `42710` instead of converging.** Bare `create policy` with no `drop policy if exists`/guard (`[supabase/migrations/0009_stat_pending_visibility.sql:30-31]`); a mid-migration failure + manual re-apply (or an out-of-band replay) dies on the policy line rather than no-op'ing. **Intended home:** a repo-wide migration-idempotency convention decision (or Epic 7 ops hardening). **Reason deferred:** this is the established pattern across every existing migration (0002/0005/0007/0008 all use bare `create policy`), so it is pre-existing convention, not a regression introduced by 3.5; Supabase's forward-only migration runner never re-applies a committed migration in normal operation.

## Deferred from: code review of story-3.4 (2026-07-11)

_Baseline `70d94bc`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8). AC1–AC5 + AD-2/AD-7/fail-closed verified satisfied, no scope creep. One item surfaced as **decision-needed** (below) → Cuatro chose defer-to-live-QA; the other four findings are small in-scope patches tracked in the story's Review Findings section. One finding dismissed as noise: fail-closed roster-read leaving a demo persisted-but-unvalidated on a rare roster-DB blip — by-design fail-closed, recovery is the already-deferred Story 3.6/3.8 re-parse, same pattern 3.3 established._

- [ ] **Conservation gate `Σkills == Σdeaths` false-positives on unattributed deaths.** `Validate` gate 1 (`[worker/ingest/validate.go:27]`) holds ANY match with `Σkills ≠ Σdeaths` as `anomalous`, but the 3.3 parser (`[worker/ingest/parse.go:85-95]`) increments a killer's `Kills` only for a real account (`get()` drops `nil` / `SteamID64==0` = world/bot) while ALWAYS incrementing the victim's `Deaths` — so a fall / suicide / world / bomb death (no attributable killer) yields `Σkills < Σdeaths` and holds a legitimate match. In CS2 `Σkills ≤ Σdeaths` always, so strict `==` conflates the IMPOSSIBLE over-count direction (`Σk > Σd` = a genuine parse double-count worth flagging) with the NORMAL under-count (`Σk < Σd` = unattributed deaths). **Intended home:** Task 6 live-QA verification (this story) → then a `validate.go` touch (or Epic 5 rich-stat derivation) if a code change is warranted. **Decision (Cuatro, 2026-07-11):** DEFER — keep the spec-mandated `==` (AC1 + PRD FR-12 both specify it); verify conservation behavior against real `.dem`s at the pending Task 6 live-QA (the 3.3 QA already showed `16==16` balanced on `cuatro-luisito.dem`). Relax to `Σkills > Σdeaths` (flag only the impossible over-count) ONLY if unattributed-death imbalance proves routine on real demos. **Reason deferred:** the empirical question — do this group's real matches balance? do they play with bots (which perturb both directions)? — is answered precisely by the pending live-QA human gate, not by static analysis; low risk to wait because an anomaly is a hold-for-review flag only and there is NO publish path in Epic 3 (rows are written regardless). [blind+edge, confirmed against parse.go.]

## Deferred from: code review of story-3.6 (2026-07-12)

_Baseline `aca5457`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8) + reviewer read-through. **AC1–AC5 + the headline delete-missing risk all SATISFIED** in code and covered by fakes-based tests. All layers converged on one substantive item — the re-hash-verify drain dependency (tracked as a Patch in the story's Review Findings, not deferred). One item surfaced as decision-needed (the destructive revert of approved rows on an empty/anomalous re-parse — Cuatro to decide). One finding dismissed as noise (`-match=<id>` single-dash `=` form, consistent with the sibling ingest parser). Three low-severity items deferred below._

- [ ] **delete-missing is scoped by `match_id`, not `demo_id` — a multi-demo match would lose the other demo's rows + collapse provenance.** `PgxDemoReader.DemoForMatch` returns the newest demo (`order by id desc limit 1`, `[worker/db/db.go]`) while `RecordReparse`'s delete is `where match_id=$1 and steamid64 <> all($2::text[])` (`[worker/db/db.go]`) and the upsert re-stamps every surviving `stat_row.demo_id` to that newest `ref.DemoID`. For a hypothetical >1-demo match this deletes steamids present only under the older demo and reassigns survivors' provenance. **Intended home:** Epic 4 (match→tournament scoping of the demo/roster reads). **Reason deferred:** dormant under AD-18 (v1 single-demo-per-match) and explicitly forward-scoped by the story ("re-parse the latest and note the forward-scope; do not build demo-selection logic"). [blind+edge+auditor]
  - **⭐⭐ RE-HOMED TO STORY 4-6b, AND NO LONGER DORMANT — Cuatro's call at 4-6a's code review (2026-07-16).** The "dormant under AD-18 (v1 single-demo-per-match)" premise above is **retired**, by 4-6a's own pgTAP fixture: it deliberately builds a second demo sharing D1's `matchzy_match_id` and states that this is *"NOT contrived… 0006's `unique(matchzy_match_id, demo_sha256)` explicitly permits it and a re-upload actually produces it"*. Multi-demo-per-`matchzy_match_id` is a **normal re-upload**, not a hypothetical. And 4-6a is what makes it bite: before it, `stat_row.match_id` was NULL on every row and nothing scored off these rows; now they decide a match. Three confirmed mechanisms, all in `[worker/db/db.go]`: **(a) DELETE** — `RecordReparse`'s `delete … where matchzy_match_id = $1 and steamid64 <> all($2)` wipes a *bound* match's stat rows when the re-parse resolves to the sibling demo, leaving a `pending` match with `demo_id` set and ZERO stat rows — and `bind_match_demo` then short-circuits idempotent (`{ok:true, stat_rows_bound:0}`) so the only existing path cannot repair it. **(b) RE-POINT** — `upsertStatRows` conflicts on `(matchzy_match_id, steamid64)` and sets `demo_id = excluded.demo_id`, so a same-players re-upload overwrites the bound rows in place: `match.demo_id = D_a` while every `stat_row.demo_id = D_b`. The score then comes from one demo while the evidence link names another. **(c) ORPHAN** — a re-parse discovering a NEW player takes the INSERT path (no conflict) with `match_id` NULL; 4-6a makes this likelier, since its `RoundEnd` fold now creates rows for 0-kill round-winners. ⚠ 4-6a's D5 fixture gives the probe a **different steamid**, which models a re-upload where the roster changed — impossible — so it proves the bind's `demo_id` scope while never exercising mechanism (b). **Intended home: Story 4-6b**, folded into the **M5** assertion (`stat_row.demo_id == match.demo_id` at approve time, `review-data-integrity.md:285-314`) alongside the competitor-identity guard — the same "4-6b must not trust that a binding is semantically valid" family, and M5 catches (b) exactly. **Reason deferred (not fixed in 4-6a):** fixing `RecordReparse` would change shipped Story-3.6 behaviour and its live-QA baseline, and 4-6a was already split for size; nothing publishes until 4-6b, so nothing is wrong today. [blind+edge+reviewer, 4-6a code review]
- [ ] **Empty-set delete-all depends on pgx encoding a non-nil empty `[]string` as `ARRAY[]::text[]`; not proven against the real driver.** `newIDs := make([]string,0,len(rows))` is non-nil so pgx encodes `'{}'` and `steamid64 <> all('{}')` is TRUE for every row (delete-all); a nil slice would encode as SQL `NULL` and silently delete nothing. Correct today and commented, but the only coverage is the in-memory `FakeStatRecorder` — the real SQL is exercised only at live-QA. **Intended home:** Task-6 live-QA delete-missing check (this story). **Reason deferred:** matches the worker's no-real-DB-harness convention (fakes cover orchestration, live-QA covers the SQL — same posture as `RecordParse` in 3.3/3.4); the pending Task-6 phantom-row delete directly exercises this branch. [blind+edge]
- [ ] **Manual-path (NULL `demo_sha256`) re-parse skips integrity verification with no `size_bytes` cross-check.** When `ref.SHA256==""` the re-hash-verify is skipped (`[worker/ingest/reparse.go]`, per AC4), so a silently-truncated/corrupted manual-path object parses to fewer/zero players and drives delete-missing with no fail-closed signal beyond a warn line. **Intended home:** Epic 7 ops hardening. **Reason deferred:** by design for v1 (the manual path has no recorded hash to verify against); a `size_bytes` cross-check is a narrow future hardening, not in the 3.6 slice. [edge]

## Deferred from: create-story of 3-7-demo-derived-score-from-ingest-ingest-side-of-fr-16 (2026-07-12)

- [ ] **Story 3.7 (demo-derived score, ingest side of FR-16) re-sequenced out of Epic 3 into Epic 4 — no ingest-side schema home exists until the `match` table lands.** ⚠ **PARTIALLY CLOSED by Story 4-6a (2026-07-16) — the INGEST half only. Do not read this item as fully discharged until 4-6b lands.** Corrected at 4-6a's code review, which found the story, its Change Log and `sprint-status.yaml` all claiming *"CLOSES deferred Story 3.7 IN FULL"* while this item and the sprint-status key both still read `deferred`. What 4-6a actually delivered: the **derivation** (`events.RoundEnd` → `stat_row.rounds_won`, per-player) and AC2's terminal-refusal half (`bind_match_demo` refuses a committed bye/forfeit/void). What remains **4-6b's**: 3.7's AC1 as `epics.md:594` literally states it — *"the match's `score_source` is `demo_derived`"* — which 4-6a never writes, correctly and by design: `SOLUTION-DESIGN:373` puts the score write inside the Aprobar transaction, and `score_source_guard` (`0010:124-128`) forbids a `demo_derived` score before `demo_id` is set, which is precisely what 4-6a made possible. So: **half closed, and the half that is closed is the half that was blocked.** ⚠ 4-6a also **CORRECTS this item's and the retro action's stated SHAPE** (DECISION B): they specify *"widen `ParseResult` with `score_a`/`score_b`"*, but `a`/`b` name the match's competitor seats and the worker cannot resolve them (at parse time no bracket row is associated with the demo at all; it holds only the external `matchzy_match_id`; `competitor_a` is a `roster_entry_id`). It emits a per-SteamID64 tally instead, and 4-6b maps the seats. **Flip this item CLOSED when 4-6b writes `score_source='demo_derived'`.** — Original create-story analysis follows: during `bmad-create-story` for 3.7, exhaustive analysis established that every target of its epic ACs lives on the **`match` table**, which is created only in **Epic 4** (the "match and audit_log migration slice", `epics.md:632`):
  - **AC1** ("compute the demo-derived score and set `score_source='demo_derived'`") targets `match.score_a` / `match.score_b` / `match.score_source` and the AD-5 `score_source_guard` CHECK — all defined on `match` in `SOLUTION-DESIGN.md §3 (lines 125–139)`. There is **no** score column on `demo` (0005) or `stat_row` (0007); a `grep -i score` over `supabase/migrations` returns **zero** hits.
  - **AC2** (AD-23 single-writer precedence: "a demo arriving for a committed `Forfeit`/`Bye` is archived for evidence but produces no `stat_row` and does not flip the match") is entirely a `match.state` transition guard (`SOLUTION-DESIGN.md §7 lines 362–364`; the `match.state` closed-set is Epic-4 DDL). With no `match` table there is nothing to gate — and the **"archived for evidence" half is already delivered** by demo retention (Stories 3.1/3.2: the `demo` row + immutable R2 object exist regardless of any later match state).
  - The architecture places the demo-derived score-setting **inside the Aprobar transaction**: `SOLUTION-DESIGN.md §8 (lines 372–373)` — *"(2) `match.score_source='demo_derived'`, `score`, `state='resolved'`"* — which is **Story 4.6**. FR Coverage Map (`epics.md:152`) splits FR-16 as **3.7 (demo-derived) · 4.8 (manual override)**, and 4.6's own AC already names `score_source='demo_derived'` (`epics.md:751`).

  This is the **direct analogue of the Story 2.6 deferral** (a story whose data source is created by a later story's migration). It is also the mirror of the pre-provisioning that DID exist for the two prior ingest slices: 3.5 shipped on the pre-provisioned `stat_row.status` shell (0007) and 3.6 on the pre-provisioned `demo.parse_generation` + `stat_row` DELETE grant (0005/0007). **No score-bearing column was pre-provisioned anywhere the single-writer worker can persist** — the architects deliberately kept score on `match` only — so 3.7 has neither a schema home nor a match to attach a score to today. The parser (`worker/ingest/parse.go`) currently computes only kills/deaths/`RoundsPlayed`; the **team round-win tally** (`score_a`/`score_b` from `events.RoundEnd` / final `GameState` team scores) is un-built and rides along with this deferral. **Decision (Cuatro, 2026-07-12):** DEFER — `3-7` is set to status `deferred` in sprint-status (not auto-picked as "next", per that file's STATUS DEFINITIONS). No story file, migration, or code was produced. **Intended home:** **Epic 4** — the demo-derived score computation + persistence lands in **Story 4.6 (Aprobar)**, which sets `match.score_source='demo_derived'` + `score_a`/`score_b` + `state='resolved'` in the one atomic publish transaction (§8); the AD-23 Forfeit/Bye precedence guard lands in the **match state machine (Stories 4.5/4.6)**; the manual-override sibling of FR-16 is already **Story 4.8**. The score-derivation parser extension (widen `ParseResult` with `score_a`/`score_b`) travels with 4.6. **Reason deferred:** building now would require either pulling Epic 4's `match` schema (AD-5/AD-23) forward into Epic 3 or persisting a demo-side score the architecture deliberately does not model — guaranteeing rework when Story 4.6's atomic Aprobar (which is the specified home for score-setting) lands. Over-engineering for a casual private event. [Analysis sources: `epics.md#Story-3.7` (lines 584–604) + `#Epic-4` (630–632) + FR Coverage Map (152); `ARCHITECTURE-SPINE.md#AD-5` (100–103), `#AD-23` (190–193), ERD `MATCH` (359–367); `SOLUTION-DESIGN.md §3` (110–139 match table + score_source_guard), `§7` (360–366 Bye/Forfeit precedence), `§8` (370–375 Aprobar sets score); `worker/ingest/parse.go` (kills/deaths/rounds only, no score); `grep -i score supabase/migrations` = 0 hits. Mirrors the Story 2.6 create-story deferral above.]

## Deferred from: code review of story-3.8 (2026-07-12)

- [ ] **`serve` graceful-drain (`defer runner.Close()`) is unreachable on a normal shutdown signal.** `runServe` starts the bounded parse-job `Runner` and `defer runner.Close()`s it, but `http.ListenAndServe` (`[worker/main.go:98]`) blocks and only returns on error, so on SIGTERM/Ctrl-C the deferred `Close()` never runs — in-flight and buffered parse jobs are NOT drained, weaker than Task 6's "the serve graceful-shutdown drains in-flight jobs" wording. The drain mechanism itself (`Runner.Close` → close `quit` → workers drain the buffered channel → `wg.Wait`) is correct and unit-tested; only the wiring to a real shutdown signal is missing. **Intended home:** Story 7.4 (ingest recovery hardening / ops slice) — add `signal.NotifyContext` + `http.Server.Shutdown` so the deferred `Close` (and future durable-recovery sweep) actually fire on a clean stop. **Reason deferred:** the story's own Regression note explicitly blesses the accepted loss ("or explicitly document the accepted loss; either is fine — R2 + Story 7.4 is the recovery backstop"); the ephemeral-queue design already treats a lost job as re-drivable from the retained R2 demo, and the worker has never had signal handling (pre-existing). [edge]
## Deferred from: code review of story-4.1 (2026-07-12)

- [ ] **`score_source_guard` permits a score with no provenance.** The CHECK (`[supabase/migrations/0010_match.sql:77-81]`) constrains `score_source` but never *requires* one, so `score_a = 16, score_b = 14, score_source = NULL` is accepted — precisely the row the AD-5 "single discriminator" exists to prevent. The missing half is `(score_a is null) = (score_source is null)`. **Intended home:** **Story 4.6** (Aprobar — the story that first populates `score_a`/`score_b`/`score_source`), with **4.8** (manual override) as the sibling. **Reason deferred:** the DDL is verbatim from SOLUTION-DESIGN §3 (110-139), which Story 4.1 Task 1 explicitly mandated copying; tightening it is a spec change, and no score is written anywhere until 4.6/4.8, so no reachable defect exists today. [blind]
- [x] ~~**`pg_temp.m(slot)` test helper resolves a match by `bracket_slot` alone — no tournament scope, no `LIMIT 1`.**~~ **RESOLVED, NOT DEFERRED (2026-07-12, same review).** The helper only existed because the story re-fixtured `0005`–`0009` with real `match` parent chains to satisfy an FK on `demo`/`stat_row.match_id`. Decision 2(c) moved that FK onto a **new, nullable** `match_id` (the ingest paths write `matchzy_match_id`), so those suites no longer need a match parent at all — the re-fixturing was **reverted to baseline** and `pg_temp.m()` no longer exists. Nothing to carry forward. [blind]
- [ ] **No CSRF defence on the state-mutating, cookie-authenticated admin POST routes.** `[app/api/admin/bracket/route.ts]` (and, identically, `registration` / `roster`) accept a cookie-authenticated POST with no CSRF token, origin check, or custom-header requirement beyond whatever `SameSite` the Supabase SSR cookie happens to carry. **Intended home:** auth/ops hardening pass (Epic 7). **Reason deferred:** **pre-existing across every admin route** — not introduced by Story 4.1, which faithfully mirrors the established `app/api/admin/registration/route.ts` pattern. For a private friends group behind Steam login the practical exposure is low; fix it once, uniformly, for all admin routes rather than piecemeal here. [blind]

## Deferred from: code review of story-4.2 (2026-07-13)

_Baseline `5b398ec`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8). **AC1, D2 and D3's column SATISFIED**; the headline `manual_override` trap correctly avoided; no scope creep; every gate independently re-verified green (pgTAP 497 / Vitest 190 / lint 0 / build 0) and every story citation accurate. **AC2 and D1 do NOT hold as shipped** — the trigger's override gate is a `PGC_USERSET` custom GUC that any role (including `service_role`, the exact writer D1 names) can set for itself, so the "silent edit" is reachable in two lines with zero audit rows; confirmed by EXECUTION against the live DB by two independent layers. That is a **decision-needed** (three candidate mechanisms, materially different trade-offs), tracked in the story's Review Findings pending Cuatro's call — not deferred here. Ten in-scope patches likewise tracked in the story. Three low-severity items deferred below._

- [ ] **`P0001` is PL/pgSQL's GENERIC exception code — `lib/match/format.ts`'s `already_locked` mapping will misfire the moment a second `match` trigger raises it.** `[lib/match/format.ts:89,136-138]` classifies **any** `P0001` from the `declare_match_format` RPC as `already_locked` → HTTP 409. `P0001` (`raise_exception`) is the default SQLSTATE for **every** `RAISE EXCEPTION` in every PL/pgSQL function in the database that does not set an explicit `errcode` — and the repo raises it in exactly that bare form elsewhere (`0011`'s `roster_entry_lock_guard`). **Intended home:** **Story 4.5** (the match state machine — the first story likely to add a second `BEFORE UPDATE` trigger on `match`). **Reason deferred:** verified not reachable today — within this RPC's blast radius `match_format_lock_guard` is the only `P0001` source, so no misclassification is currently possible, and the mapping itself is *correct* (it is 4.1's review lesson applied). It becomes a real defect the instant a sibling trigger lands: a write rejected for an unrelated reason would report "the format is frozen" (409) and the true error would be swallowed — the `console.error` sits in the other branch. Fix when the collision arrives: give the format-lock trigger a distinct SQLSTATE (e.g. a custom class) and match on that, or match the message. [blind]
- [x] ~~**Nothing in TypeScript maps `23514` — whoever lands the `declared → live` transition inherits an opaque `check_violation`.**~~ **CLOSED by Story 4-6a (2026-07-16), four stories after it was raised.** 4.5 declined it (`resume_match` goes to `declared`, so it never lands a played state). **4-6a lands `pending`** — the first played state any writer produces — so it inherited the obligation and discharged it both ways: `bind_match_demo` guards `format_locked` **explicitly** and RETURNS a typed `format_not_declared` *before any write* (so the CHECK is never even reached on the normal path), and `lib/match/bind.ts` maps `23514` → `format_not_declared` (409) as the honest report if a direct RPC call ever trips the constraint itself. The CHECK remains the backstop that binds every writer; the guard is the UX in front of it. Proven at both layers: pgTAP `0016` Section D (typed refusal + the match untouched, mutation-verified) and the 4-6a live-QA (a real unlocked-format match over supabase-js → PostgREST → RPC → `format_not_declared`, not a 500). ⚠ **Still open for `declared → live` itself**, which remains unowned (→ the Epic-5 admin console): whoever lands *that* transition inherits the same CHECK, but the map now exists to copy. [blind]
- [ ] **`app/api/admin/bracket/route.ts:52` has the same unbounded-integer gap as the new format route.** `Number.isInteger(1e21)` is `true`, so an out-of-`bigint`-range id passes the 400 gate, binds as a `bigint`, and PostgreSQL raises `22003` → mapped to `write_failed` → **HTTP 500**, where a malformed id should be a **400**. **Intended home:** a shared `isPositiveInt`/body-parse helper — most naturally **Story 4.9** (the reusable audited-command-route helper, which already owns de-duplicating this boilerplate across every admin route). **Reason deferred:** **pre-existing** (Story 4.1), not introduced by 4.2 — the new route inherited the pattern. The 4.2 route's own copy is being patched in-story; fix the sibling once, centrally, rather than piecemeal. Cheap guard: `&& v <= Number.MAX_SAFE_INTEGER`. [edge]

## Deferred from: code review of story-4.3 (2026-07-14)

_Baseline `7208608`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8). **AC1, AC2 and D1–D5 all hold as MECHANISMS**, and the two-pass design (invented during implementation, never specced) is sound — pass 1's approval condition is a strict **superset** of pass 2's predicate, so the asymmetry runs the safe way. But **three of the story's own load-bearing claims are FALSE, and each was proven by EXECUTION**: the lock order is **not** topological (the Winners Final and the Losers Final deadlock on the Grand Final — `40P01` reproduced against the real function); `match_place_competitor`'s contract is **not** "AC1's literal SQL" (it writes `winner_entry` outside the predicate, so it will re-crown a decided match or erase a winner — found by **all three** layers, reproduced as `service_role`); and although the story mutation-tested **AC1's** predicate and closed the hole it found with Section L, it shipped the **identical hole on AC2** — deleting the result-state gate leaves all **79 assertions green** while `advance_match` cheerfully double-routes a `pending` match. Those, plus a migration that **cannot apply to any database that already holds a bracket** (an immediate validated CHECK with no backfill — structurally invisible to `db reset`), are tracked as decision-needed/patch in the story's Review Findings, not here. Six low-severity items deferred below._

- [ ] **The `tournament:<id>` Broadcast topic is PUBLIC, and a public topic is client-WRITABLE.** `[supabase/migrations/0013_advance.sql:660]` `realtime.send(..., private => false)` per **AD-11**, which specifies `tournament:<id>` as the public channel. Any client holding the anon key can therefore *write* a `bracket.advanced` message to it as well as read one. **Intended home:** **Story 5.8** (realtime nudge + reconnect reconcile). **Reason deferred:** *decided by Cuatro at the 4.3 code review.* The blast radius is a **wasted read**: AD-11 makes Broadcast a **nudge, never the source of truth** — every viewer surface is reconstructable from a published read alone — so a spoofed nudge costs a viewer one unnecessary re-fetch and can forge no state. Locking it down means a private channel plus an RLS policy on `realtime.messages`, and that should land **uniformly across all three channels** in the story that owns realtime, rather than being bolted onto this one — which has no viewer surface to test it against until 5.7 anyway. [auditor]
- [ ] **AC1's guarantee binds one FUNCTION, not the TABLE — `service_role` can seat any competitor into any slot with a bare `UPDATE`.** `match` grants `select, insert, update` to `service_role` (`[supabase/migrations/0010_match.sql:227]`) and no trigger constrains `competitor_a`/`competitor_b`, so the AC1 predicate is a property of `advance_match` / `match_place_competitor` and **not an invariant of the bracket**. The suite's own Section J demonstrates it, as a *fixture*: `update match set competitor_b = pg_temp.re('TALT', 11) …`. This is the same **shape** as the 4.2 review's headline finding (a guard that binds one code path rather than every writer), and the repo already solved the analogue one migration earlier — 0012 answers it with the `match_format_audited` **constraint trigger** plus an audited-override seam. **Intended home:** **Story 4.7** (rollback / un-advance), with **4.9** as the sibling. **Reason deferred:** the naive fix — a write-once trigger on the competitor columns — would **block 4.7**, which must legitimately *un-seat* competitors to roll an advance back. The correct shape is 0012's audited-override seam, and it cannot be designed properly until the un-advance path exists to design against. Not reachable by any production caller today (D5: there is no advance route, and nothing else in the codebase writes `match`). [auditor]
- [ ] **A returned `{ok:false}` does NOT roll back the CALLER's transaction — and nothing requires the caller to.** `[supabase/migrations/0013_advance.sql:721-726]` The two-pass design fixes this **inside** the function (a refusal writes nothing — verified), but the same property re-creates the hazard **one level up**, at exactly the boundary this migration is built for: if Story 4.6's Aprobar writes the score/demo/state, calls `advance_match`, receives `slot_taken`, and **commits anyway**, the result is a resolved match with a score and no advance — the precise partial state the two-pass design set out to prevent. Note also that the `FOR UPDATE` locks pass 1 acquired **before** refusing are **held for the remainder of the caller's transaction**, blocking other advances even though nothing happened. **Intended home:** **Stories 4.5 / 4.6** (the first callers). **Reason deferred:** there is no caller today (D5), so nothing is broken — but the contract is currently **unwritten**, which is exactly how it gets missed. The migration header should state it outright (*"the caller MUST roll back on `ok:false`"*), and it is worth deciding whether `slot_taken` should instead `raise` with a distinct SQLSTATE so a caller **cannot** accidentally commit through it. [blind]
- [ ] **`realtime.messages` is day-partitioned by a Realtime BACKGROUND JOB, and `realtime.send` swallows its own errors — so in PRODUCTION a missing partition makes AC3 fail SILENTLY.** `[supabase/tests/0013_advance_test.sql:1690-1709]` The dev found this during Task 0 and the diagnosis is exactly right — but the conclusion drawn was *"make the suite's own precondition true"*: the **test** creates the partition it needs, goes green, and **production is left exactly as exposed as it was**. If the partition is missing there, `realtime.send` swallows the error, `advance_match` returns `ok:true`, and the nudge is lost **with no signal anywhere**. AD-11's tolerance (*"a client re-fetches on reconnect"*) covers a dropped event for a **reconnecting** client; it does not cover a **connected** viewer who never reconnects and therefore never learns the bracket moved. **Intended home:** **Story 5.8** (realtime nudge + reconnect reconcile — the story that owns delivery), plus an ops check in **Epic 7**. **Reason deferred:** not a defect in this story's logic, and there is no viewer surface to drop a nudge *to* until Epic 5.7. The honest scope of 4.3's AC3 — *the message is emitted, post-commit, on the right channel, with the right event* — is proven. Delivery is 5.8's. [auditor]
- [ ] **`advance_match` raises bare `P0001` in five places — against the story's own Dev Notes, which say "there is no reason to raise".** `[supabase/migrations/0013_advance.sql:368, 513, 545, 552, 600]` (bad side · hop cap · dangling edge · void destination · post-validation failure). The Dev Notes state *"You are **not** adding a raising trigger … **Keep it that way** — a `raise exception` in an advance would have to be given a distinct SQLSTATE"* — while the story's own **Task 3** *mandates* `raise exception` for the void node and the hop cap. The implementation correctly followed the tasks; the Dev Notes were the stale half. **Intended home:** **Story 4.5** (the match state machine), joining the existing `P0001`-ambiguity item above — they are the same defect and should be fixed together. **Reason deferred:** not reachable today (`[lib/match/format.ts:106]` maps `P0001` only on `declare_match_format`'s own response, and `advance_match` has **no TypeScript caller at all** — D5). It becomes real the moment 4.5/4.6 call the advance through a route that also talks to the format RPC, or when 4.9's shared command-route helper maps SQLSTATEs centrally. Fix: give these a distinct SQLSTATE (a custom class) rather than PL/pgSQL's default. [auditor]
- [ ] **`advance_match` never checks `tournament.state`.** `[supabase/migrations/0013_advance.sql:450-455]` It locks and re-reads the **match**, but never the **tournament** — so an advance will run against a tournament in *any* state whatsoever, including one that has been rolled back or archived. Contrast `generate_bracket`, which deliberately takes `FOR UPDATE` on the tournament row and gates on its state before writing anything. **Intended home:** **Story 4.7** (rollback) — the story that first makes *"a tournament in a state that should not accept advances"* a reachable condition. **Reason deferred:** no reachable defect today: there is no tournament state in which a match can be `resolved`/`forfeit`/`bye` **with a winner** and yet an advance be wrong, because nothing can un-live a bracket until 4.7 exists. Cheap to add when it does — one `for update` plus a state guard, mirroring `generate_bracket`. [blind]
- [ ] **The `advance` audit row carries no before/after, though AD-17's letter asks for one.** AD-17 (`[ARCHITECTURE-SPINE.md:163]`) specifies an `audit_log` row with *"actor SteamID64, timestamp, **before/after**"*, and `generate_bracket`'s audit row does carry `before`/`after`. `advance_match`'s `detail` is `{winner_entry, loser_entry, hops, champion}` — `hops` is a reasonable *"after"*, but there is no *"before"* (which seats were empty prior to the advance). **Intended home:** whichever story next revisits the audit shape — most naturally **4.7** (rollback, which must *read* an advance's audit row in order to undo it, and is therefore the first consumer that actually wants the "before"). **Reason deferred:** **story-compliant** — Story 4.3's D4 specified exactly `{winner_entry, loser_entry, hops}`, and the implementation added `champion` on top. This is a gap between the **story** and **AD-17**, not between the story and the code. Flagged rather than unilaterally widened, because 4.7 is the story that knows what shape it needs to consume. [auditor]

## Deferred from: code review of story-4.4 (2026-07-14)

_Baseline `eac5f51`; layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor (all Opus 4.8). **AC1, AC2, AC3 and DECISIONS A/B/C all hold, verified — not merely asserted.** Unlike 4.1/4.2/4.3, the story's own load-bearing claims survived scrutiny: the Auditor confirmed line-by-line that both `create or replace` functions ARE 0013's body copied forward verbatim except the three disclosed surgical changes; the "mutation-verified" claim is genuine (removing the GF-1 conditional turns four value-asserting TGFA assertions red, not shape-only ones); AC2 is proven literally (gf_order=1 and gf_order=2 winners are distinct rows with distinct winners); and `final_match_id` = "the last GF row played" holds on both paths and cannot latch a wrong row today. Two patches (a constraint-rigor tightening + a stale comment) are tracked in the story's Review Findings. One low-severity latent item deferred below. Three findings dismissed (a Blind-layer false positive on NULL-in-CHECK — already caught by `match_winner_edge_gf_order`, 0013:128-137, which the diff-blind layer could not see; the AD-21 seat-convention coupling, verified asserted end-to-end at 0013:414-421 + Vitest; and a cosmetic section-count label, now self-consistent at plan(89))._

- [ ] **`final_match_id = p_match_id` assumes the crown always happens at the ADVANCED row — latent if a future story makes a terminal row walkover-reachable.** `[supabase/migrations/0014_grand_final_reset.sql:587-591]` sets `final_match_id = p_match_id` whenever `v_champ` becomes non-null. But `v_champ` is *also* set inside the pass-1 cascade loop (`[…:471-473]`) for a walkover DESTINATION row that is terminal — in which case `p_match_id` is the cascade SOURCE, not the crowning row. **Correct today and verified unreachable:** the only terminal rows are grand_final gf_order=2, which generation forces to `declared` (never `bye`), and gf_order=1 now always carries the reset edge so it is no longer terminal — a walkover only continues into a `bye` node, so no terminal row is ever walkover-reachable, and `v_champ` can only be set for `p_match_id` itself. **Intended home:** whichever future story makes any terminal row walkover-reachable (e.g. a terminal `bye`, a 3rd-place match, or gf_order=2 ever becoming a `bye`). **Reason deferred:** not a defect in this slice — the invariant holds by construction for the 8..16 field range and the two-row GF. The robust shape is to carry the crowning row id alongside the champion (`v_champ_match := v_dest.id` where the loop sets `v_champ`, default `p_match_id`) and write `final_match_id = v_champ_match`. Cheap to add when a terminal-reachable-by-walkover row first exists to design against. [blind+edge]

## Deferred from: code review of story-5-1-core-stat-derivation (2026-07-19)

_Baseline `cc63e7a`; 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 4.8) over the working-tree diff. AC1/AC2/AC3 + scope discipline (no migration/SQL/app change) + bind-preservation (`status`/`match_id` out of both upsert lists) all CONFIRMED. All automated gates re-verified green by the reviewer; the two mapping sites + the KAST-survived branch mutation-tested (reddened, restored). One HIGH decision-needed (duplicate-`RoundEnd` idempotency regression) is tracked in the story's Review Findings, NOT here. The items below are real but not actionable in this slice — inherent to the KAST per-round model and/or invisible on the fixed-roster 1v1 tournament corpus._

- [x] **KAST per-round-at-RoundEnd model edge cases.** `[worker/ingest/parse.go:413-419]` — (a) a mid-round joiner in `Participants().Playing()` at RoundEnd with no death event banks a false "survived" KAST round; (b) a player who fragged then disconnected before RoundEnd is dropped from the KAST loop though their kill is folded (internally inconsistent row); (c) a trade completed in the next round's freeze-time (still inside the 5s window) is missed. **Reason deferred:** inherent to evaluating KAST over `cur` at RoundEnd; none can occur on a fixed 2-player 1v1 (no subs/mid-round joins, killer-survives-so-K). Revisit when 5v5 demos with substitutions land. [blind+edge]
- [x] **KAST "traded" credits an unattributed (world/suicide, `killer==0`) death of the killer as a trade.** `[worker/ingest/parse.go:141-146]` — a self-consistent convention choice (the boundary test asserts it as intended). **Reason deferred:** revisit only if the 5.5 leaderboard wants strict "killer was killed by a player" trades. [blind]
- [x] **Self-inflicted damage inflates ADR + UtilityDamage (no `attacker==victim` guard).** `[worker/ingest/parse.go:329-347]` — only `attacker==0` is filtered. **Reason deferred:** the PoC's unfiltered sum matched the in-game scoreboard golden values and THE BAR's ADR values were believable; add an `attacker==victim` skip if a 5v5 demo reads high vs scoreboard. [blind]
- [x] **Per-player ratio denominator uses match-wide `RoundsPlayed`.** `[worker/ingest/cli.go:140]` / `[worker/ingest/reparse.go:102]` — a substitute/late-joiner would read artificially low KAST%/ADR when Story 5.5's view divides by `RoundsPlayed`. **Reason deferred:** 5.5's concern (the ratio is computed there), inherited from `RoundsWon`, and fixed 2-player 1v1 rosters never trigger it. [edge]
- [x] **SQL `DO UPDATE` set-list not reachable by the in-memory fakes.** `[worker/db/db.go:349-361]` — dropping `assists = excluded.assists` (etc.) reddens no Go unit test. **Reason deferred:** disclosed by the dev (Known unit-test limit), consistent with the 4.6a `rounds_won` precedent; covered by a live re-parse (THE BAR family) + structural mirroring of the verified `rounds_won` line. [auditor]

## Deferred from: code review of story-5-3-derived-stat-derivation (2026-07-21)

_Baseline `3a99d8c`; 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 4.8) over the working-tree diff. AC1-AC4 PASS; AC5 PARTIAL. THE BAR was re-run independently by the reviewer and reproduced every headline number exactly (221 freeze-ends, Sum-entry 204 = Sum-opening 204, Sum-clutches 0, GATE 2 `T:1 CT:1` x221, weird five 1/8/2/0/4, blind `[3 5 9 13]`, exit 0). The `$1..$22` positional alignment and the `status`/`match_id` exclusions were counted by hand and are correct. Two decision-needed items (the Don Clutch award retirement overstepping the spec, and `Playing()` not being an aliveness filter) and eight patch items are tracked in the story's Review Findings, NOT here. The items below are real but not actionable in this slice._

- [x] **FR-20's bomb exclusion is implemented only as `killer == 0`.** `[worker/ingest/parse.go:222]` — `recordOpeningDuel` excludes world/bomb deaths solely by an unattributed killer. If demoinfocs attributes a C4-explosion kill to the PLANTER, the guard misses it and a bomb kill latches a false opening duel, poisoning both the numerator and the denominator of 5.5's entry-success ratio. **Reason deferred:** the reviewer instrumented all 14 demos and measured **0 bomb-explosion kills and 0 bomb-latched duels** — unreachable on current data, and a teammate-bomb-kill is impossible in 1v1. Revisit if the format goes 5v5 or if a bomb kill ever appears; the check is a weapon-type test alongside the existing three guards. [edge]
- [x] **A demo where `RoundFreezetimeEnd` never fires writes all-zero FR-20 columns with no anomaly.** `[worker/ingest/validate.go:27-67]` — `!rs.live` short-circuits every kill, the fold contributes nothing, `Validate` returns `Anomalous: false`, and the rows are approvable. This is the Story-5.2a dead-column shape, one library flag (Source-1 mimicry) away. The only detector is GATE 1 in `worker/cmd/qa53/main.go`, which the story says is deleted after sign-off. **Reason deferred:** the spec explicitly prohibits touching `validate.go` in this story ("If you find yourself editing `validate.go`, you have left the story") and pins its relaxation to Story 5.4. **Intended home: Story 5.4**, which opens `validate.go` — consider an FR-20 liveness gate there. [edge]
- [x] **Task 10b did not carry every qa52/qaflash measurement forward.** `[worker/cmd/qa53/main.go]` — `qa53` carries the weird-five totals and the blind-kill round indices as hard assertions, but not qa52's knife weapon-name breakdown (the only check that a new knife skin still normalizes to `EqKnife`), qa52's raw second-pass reconciliation of the weird five, or qaflash's `FlashExplode`/`PlayerFlashed` counters (the exact instrumentation that proved `AttackerBlind` dead and justified the 5.2a amendment). **Reason deferred:** Task 10b's literal requirement ("carry the weird-five regression checks forward") IS met and both reproduced exactly. Rebuild the flash instrumentation from git history if a future demoinfocs bump needs it. [blind]
- [x] **`blindRoundIdx` is collected from the unfiltered raw pass while `weirdTotals` comes from the fold.** `[worker/cmd/qa53/main.go:219-221]` — the raw handler appends blind-kill round indices with no `idx > final` filtering and no discarded-pre-match-round exclusion, then both are printed as one "weird-five regression net" and asserted against 5.2a's numbers. A MatchZy restart that shifts a blind kill into the discarded round would fire a spurious hard failure blamed on a 5.2a regression. **Reason deferred:** throwaway harness, currently agrees exactly on 14/14. [blind]
- [x] **EXPERIENCE.md's a11y reveal example says "21 muertes" for a kills award.** `[EXPERIENCE.md:145]` — the project's own mockup uses "muertes" for BOTH the kills award (`mock-leaderboards.html:486`, "habilidad - volumen - muertes") and the most-deaths comedy award (`:500`, "comedia - mas muertes"), so a screen-reader user cannot distinguish them. **Reason deferred:** **pre-existing** terminology ambiguity, not introduced by this change — the diff reused the mockup's existing label verbatim. **Intended home:** an Epic 6/7 award-copy pass; the fix is a distinct term (bajas/asesinatos) for kills across all three surfaces. [blind]
- [x] **The `clutchTracker` alive-snapshot does not survive a mid-round disconnect or a freeze-time death (5v5 only).** `[worker/ingest/parse.go:680-685]` — `Participants().Playing()` filters on `Team`, not aliveness, and `events.Kill` is the only thing that ever decrements `aliveOn` (there is no `PlayerDisconnected` handler in `Parse`). A disconnect on the decaying team means it never reaches exactly-1 and a genuine clutch is silently dropped; a disconnect on the other team inflates X (a real 1v1 published as `{"2":1}`, which 5.5's `clutches->>'1'` read would miss). **Reason deferred — EXPLICIT DECISION (Cuatro, 2026-07-21, during the 5.3 code review):** accepted as a documented 5v5 limitation. No `PlayerDisconnected` handler and no relaxation of the story's `IsAlive()`/`GetTeam()` prohibition — keeping `clutchTracker` free of every demoinfocs type is what makes AC2 table-testable at all, and the gap is unreachable on a 1v1 corpus (measured: `T:1 CT:1` on all 221 rounds, Sum-clutches 0). The misleading "LIVING" comments were corrected in-review so the code no longer claims a guarantee it lacks. **Intended home:** the first story that ingests a 5v5 or substitution-bearing demo — at which point `PlayerDisconnected` -> `ct.kill(sid)` is the cheap fix and stays pure. [blind+edge]

## Deferred from: code review of story-6-1-award-catalog-and-buckets (2026-08-03)

_Baseline `64990db`; 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 5) over the working-tree diff (~2,230 reviewable lines). AC1/AC2/AC4 PASS; AC3 PARTIAL (populated-ness measured for all 12, but the clone/ranking probe that killed three predecessors was never run over the three replacements). The Edge-Case Hunter reproduced its SQL claims by executing `curate_award_catalog` against the running local stack; the reviewer independently confirmed the two headline guard defects by reading the source. Two decision-needed items (the unprobed replacement awards, and `El Inofensivo`'s kill floor inverting for a `min` award) and eleven patch items are tracked in the story's Review Findings, NOT here. ⚠ Scope calibration: the admin route posts the server-side constant and never accepts a catalog from the body, so no malformed-payload defect below is reachable over HTTP — they are `curate_award_catalog` RPC-contract defects. The items below are real but not actionable in this slice._

- [x] **`award_tournament_priority_idx` duplicates the index the UNIQUE constraint already builds.** `[supabase/migrations/0023_award_catalog.sql]` — a `UNIQUE` constraint, deferrable or not, always builds a btree index on exactly those columns, so every write to `award` maintains two identical indexes and the planner will never choose the redundant one. The migration comment concedes the shape is the same and keeps it citing "the pgTAP shape assertions", but the suite only does `has_index(...)` on it — an assertion a duplicate index cannot fail meaningfully. **Reason deferred:** spec-mandated (Task 1 explicitly requires `Index award (tournament_id, priority)`, SOLUTION-DESIGN.md:260). Drop it only with an architecture amendment. [blind]
- [x] **`catalog_frozen` is a denylist read taken without a lock.** `[supabase/migrations/0023_award_catalog.sql:415]` — `state in ('ceremony','closed')` means any state added to `tournament.state` later is silently *curatable* by default, in a migration whose stated philosophy is making bad states unrepresentable; and the read takes no lock, so a concurrent transition to `ceremony` committing between the guard and the write lets a curation land on a tournament that is by then frozen. **Reason deferred:** the story pins this as the honest partial of AD-15 — **Story 6.2** replaces the state test with the real ceremony lock (retro Action Item #2). Make it an allowlist there. [blind]
- [x] **Two concurrent curations of an EMPTY catalog serialize on nothing.** `[supabase/migrations/0023_award_catalog.sql:415]` — `for update` over `award` locks zero rows when there is nothing to lock, so the first curation of a tournament is unserialized. Both transactions delete nothing and both insert; disjoint names dodge the immediate name UNIQUE while overlapping priorities trip the DEFERRABLE priority UNIQUE at COMMIT (23505), and the loser gets an opaque 500 — so the migration's "23505 is only a backstop for raw writers" claim does not hold on the app path. **Reason deferred:** single-admin casual tournament; curation is driven over HTTP by one person. Revisit if a second admin or an automated curator appears. No concurrency test exists. [edge]
- [x] **`~ '[^[:space:]]'` accepts zero-width and format characters.** `[supabase/migrations/0023_award_catalog.sql:138,339]` — verified: the regex correctly refuses U+00A0 (NBSP) and U+3000, but returns TRUE for U+200B / U+200E / U+FEFF, so an award named with a single zero-width space is accepted, stored, and renders as an empty card at the ceremony. **Reason deferred:** one codepoint class beyond the TAB bug this guard was written to close (4.2), and the only writer is a server-side constant. The fix is widening the character class in both the CHECK and the RPC guard together. [edge] ⛔⛔ **RE-CONFIRMED OPEN by Story 6.9a's CODE REVIEW (2026-08-09), and the record needs correcting rather than extending.** 6.9a claimed this debt was given teeth by the bundle's ASCII restriction (§9.5) — AC2 says so, and the claim was repeated at the refusal site in `publish_bundle`, in `canonical.ts`'s options doc and in the gate-4 vector's own `why` string. **It is false.** DECISION N drops `name` from the bundle entirely, so every published field is digits, snake_case identifiers, decimal strings or `inclusivcup/v1/…` labels: no producer-controlled value can ever be non-ASCII and that refusal is UNREACHABLE in production. It is worth keeping as defence-in-depth against a field added later, and it is what makes `octet_length = length` a sound ASCII test for the stored bytes — but it never touched this. All four false claims were corrected in place. **Home: 6.9b, where award names actually reach a viewer** (the verify strip and the feed), as an explicit reject-list for C0/C1 controls, U+200B/200C/200D/200E/200F/FEFF and the bidi overrides U+202A-202E / U+2066-2069, plus a length bound. ⚠ A byte-alphabet rule on a hashed document was never the right shape for this: the hazard is what a HUMAN sees, not what the hash covers.
- [x] **`name` has no length bound, and near-duplicate names both pass.** `[supabase/migrations/0023_award_catalog.sql:142,402]` — verified: `'A'` and `'A '` are both accepted (the payload dup check and the scoped UNIQUE are exact-text), so two visually identical prize categories can ship in one catalog and both get spun; `name text` is also unbounded, so a multi-KB name is stored and copied into every `audit_log` before/after payload. **Reason deferred:** admin posts a server-side constant that `catalog.test.ts` already pins to 12 distinct non-blank names. [edge]
- [x] **pgTAP Section D's bucket/direction/floor assertions test their own fixture.** `[supabase/tests/0023_award_catalog_test.sql]` — the rows under test come from `seed_payload`, a 12-element literal restated inside the same file, so `count(distinct bucket) = 4` "the clutch bucket is refilled after the Don Clutch retirement" can only fail if `curate_award_catalog` corrupts the column; the stated subject is a property of the *shipped* catalog, which this file deliberately cannot observe. **Reason deferred:** the shipped catalog's four-bucket property IS asserted against the real constant in `lib/awards/catalog.test.ts`, so the property is covered — only these test *names* overclaim. Rename them to what they measure (a column round-trip). [blind]
- [x] **`El Inofensivo` (least ADR) carries `floor_kills = 20`, excluding the least-harmful players by construction.** `[lib/awards/catalog.ts]` — the seed applies the kill floor by `class` (rate ⇒ 20) with no regard for `direction`, and #12 is the catalog's only `min` award. For a `max` rate award a kill floor is anti-farm; for a `min` award it is anti-*qualification* — the most harmless player is the one least likely to clear a kill floor, so the trophy goes to whoever cleared 20 kills with the lowest ADR. The code conforms to Task 5 as written ("rate awards carry `floor_kills = 20`"), so this is a product call, not a spec violation. **Reason deferred — DECISION (Cuatro, 2026-08-03, during the 6.1 code review):** `direction='min'` and its ties are already recorded as 6.4/6.5's first-class problem (Completion Note #2, "6.4's Stage 2 and 6.5's ladder must handle minimisation from their first line of code"); the floor interaction belongs in that same specified case rather than a seed-constant patch here. **Intended home: 6.4 / 6.5.** [blind]
- [x] ⛔ **The declared eligibility floors exclude the ENTIRE measured roster — 0/28 players clear either one.** `[supabase/migrations/0021_leaderboard.sql:50-52, lib/awards/catalog.ts]` — measured by an independent reviewer probe over all 14 demos: `rounds_played` per player is **min 10 / max 21** against `floor_rounds = 24`, and **0/28** players reach `floor_kills = 20`. Applied to the corpus as it stands, all twelve seeded awards have **ZERO eligible players** — a catalog in which every category has no possible winner. That is exactly the failure AC3 exists to prevent, reached from a direction the story never checked: not an empty stat, but an unclearable floor. ⚠ **Format-specific compounding:** in 1v1 wingman `kills ≈ rounds_won ≈ ½ rounds_played` (the same structural identity Task 0 measured), so `floor_kills = 20` implies ~40 rounds played — about three matches — before a player qualifies for **any rate award**; in 5v5 a single match clears it. Not a 6.1 defect: the floors live in 0021 and 6.1 correctly treats them as a value-parity duplicate it must not touch (its scope boundary says so explicitly). **Reason deferred — DECISION (Cuatro, 2026-08-03, during the 6.1 code review):** the corpus is a 28-player first round with exactly one match per player; bracket winners accumulate rounds across subsequent matches and are expected to clear the floors well before the ceremony. **Intended home: 6.4 / 6.5 — verify the 24/20 values against the REAL bracket shape rather than assuming it, because the assumption is currently untested and its failure mode is silent (an empty award, no error anywhere).** [reviewer probe]
- [x] **The all-zero-tie hazard on the thin weird stats, now quantified tournament-wide.** `[lib/awards/catalog.ts THIN_BUT_REAL]` — the story recorded this qualitatively; the reviewer's probe measured players-above-zero across all 14 demos: `knife_kills` **1/28**, `through_smoke_kills` **2/28**, `blind_kills` **3/28**, `wallbang_kills` **7/28**. If the single knife-kill holder fails the eligibility floors, award #6 becomes a **27-way tie at zero** that walks the FR-29 ladder to a shared trophy for the entire roster. **Reason deferred — DECISION (Cuatro, 2026-08-03):** already the story's own recorded consequence for 6.4/6.5 ("is a zero deciding value awardable at all?"); these numbers are the evidence those stories should meet the case with rather than rediscovering it live. **Intended home: 6.4 / 6.5.** [reviewer probe]

## Deferred from: code review of 6-2-fair-seed-freeze-and-immutable-snapshot-capture (2026-08-03)

_Baseline `01e3f5b`; 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 5) over the working-tree diff (~3,265 reviewable lines), plus a reviewer-independent mutation pass executed against a freshly reset local stack. AC1/AC3/AC5 MET; AC2 PARTIAL (`curate_award_catalog` still evaluates the lock without holding one; a hand-resolved final can leave the ceremony permanently unlockable); AC4 PARTIAL (the mandated verbatim rows are elided, no zero-stat player exists in the corpus, the per-player table was never produced, and the clone probe AC4's own "Given" clause names was not run). The reviewer reproduced `plan(94)` green, then found **5 survivors in 6 mutations the author did not author** — including the AD-19 rate-pair assertion going green on a pair missing `den`. Four decision-needed items and sixteen patch items are tracked in the story's Review Findings, NOT here. The items below are real but not actionable in this slice._

- [x] **One write-once snapshot row mixes tournament-blind and tournament-scoped aggregates.** `[supabase/migrations/0024_ceremony_lock_snapshot.sql:592]` — `public.leaderboard` has no tournament predicate (`0021:86-89` groups every approved non-idle `stat_row` by `steamid64`), while the capture's `own`, `dq` and `h2h_pair` CTEs all join `match` and filter `m.tournament_id`. A player with approved rows in two tournaments therefore gets a row whose `volume.kills`, `rounds_played`, `matches_played` and all four rate pairs are **global**, sitting beside a **scoped** `rounds_won`, `achievement_ts`, `idle_dq` and `h2h` — so `h2h[opp].rounds_played` cannot sum to `volume.rounds_played`, and 6.5 can cross-multiply a global numerator against a scoped denominator. `v_eligible` inherits the same blindness. The migration documents this as a "SEAM" at `:585-588`, but nothing refuses, warns, or records that the assumption held, and `stat_snapshot_row` is write-once. **Reason deferred:** AD-18 pins v1 to exactly one tournament, so today the two coincide exactly; the fix is to give `0021` the tournament scope, which is explicitly out of this story's scope. **Intended home: whoever adds tournament #2 — and it must be 0021, never a hand-rolled aggregate in the capture.** [edge]
- [x] **`ceremony.state` and `snapshot_id` are "immutable (AD-15)" by convention only.** `[supabase/migrations/0024_ceremony_lock_snapshot.sql:144,199]` — `snapshot_id` carries the comment "immutable (AD-15)" and `seed_demo_sha256` is the frozen seed, but `service_role` (the only writer, and BYPASSRLS) holds plain `UPDATE` on every column, there is no write-once trigger on either — contrast `tournament.fair_seed`, which got one — and `ceremony_state_valid` is a membership CHECK with no transition constraint. `update ceremony set state = 'not_started'` re-opens all four `ceremony_locked` guards *and* `catalog_frozen` after the snapshot is captured and (post-6.8) published. In a migration whose stated thesis is "not by convention, by mechanism", these two are convention. **Reason deferred:** 6.4 needs `UPDATE` for `state`/`spin_plan` and 6.8 for `complete`, so the guard must be a transition trigger designed against those stories' needs rather than a blanket revoke. **Intended home: 6.4 / 6.8.** [blind]
- [x] **6.11's golden vector cannot be projected from this capture.** `[_bmad-output/implementation-artifacts/6-2-fair-seed-freeze-and-immutable-snapshot-capture.md:452]` — the 14 demos are a 28-player single round and `generate_bracket` bounds the field at 8-16 (`0014:174-177`), so the corpus cannot produce a demo-backed grand final through a legal bracket. Task 5 drove the crowning through a `grand_final` `gf_order=2` row bound to a real demo, with the other 13 first-round matches published by the status flip. The freeze, the hash, the parse and the capture arithmetic are all real; the **bracket topology is a fixture**. AD-19 requires 6.11's end-to-end vector be projected from a *real* captured snapshot. Compounding: `achievement_ts` is wall-clock approval time, so a re-ingest of the same demos yields a different `content_sha256` in principle — the digest is reproducible from the stored rows, never from the corpus. **Reason deferred:** the story recorded it honestly and the corpus limitation is not 6.2's to fix. **Intended home: 6.11 — either capture over a corpus with a real final, or state the limitation on the vector's face.** [auditor]
- [x] **`STATUS_FOR`'s numeric values are untested across all four admin routes.** `[app/api/admin/ceremony/route.ts:37]` — the `Record<Extract<Result,{ok:false}>['reason'], number>` typing makes a *missing* reason a compile error, which is the property the comments claim, but says nothing about the number: `ceremony_locked: 404` would compile and ship. No test exercises any of the four `STATUS_FOR` maps — the lib tests assert only that the reason string is surfaced, and `vitest.config.ts:17` restricts collection to `lib/**`, so a test under `app/` silently would not run. **Reason deferred:** pre-existing pattern across every admin route, not introduced by 6.2; closing it needs a route-level test harness that does not exist yet. **Intended home: Epic 7 hardening.** [blind]
- [x] **`ceremony_locked` is not extended to the grace / bind RPCs.** `[supabase/migrations/0015_walkover_grace.sql:161,233; supabase/migrations/0016_bind_demo.sql:155]` — `begin_match_grace`, `resume_match` and `bind_match_demo` gate on *match* state only and carry no ceremony guard, so after the lock they still churn match state and push `timeline_feed` entries. Because the lock sets `tournament.state = 'ceremony'`, that means feed activity landing on the viewer **while the audience is watching the ceremony**, describing a bracket the frozen snapshot says is settled. **Reason deferred — DECISION (Cuatro, 2026-08-03, during the 6.2 code review):** `mark_walkover` was guarded in 6.2 because it is the one RPC that can move `final_match_id` and thereby falsify an already-committed seed; the remainder is a viewer-integrity concern rather than a correctness one. **Intended home: 6.8 — it owns the reveal surface and the judgement about what the audience may see mid-ceremony.** [edge]
- [x] ⛔ **FR-29 needs a deterministic TERMINAL rung — rung 4 provably ties for duel opponents.** `[prd.md:393]` — `approve_match` stamps `approved_at = now()` (the transaction timestamp) on every stat_row of a match in one statement, so both competitors of a 1v1 wingman duel receive a byte-identical value; `achievement_ts = min(approved_at)` therefore cannot separate any two players whose earliest match was against each other — precisely the pairs a head-to-head-adjacent ladder reaches rung 4 for. ⚠ **This is structural, not a source problem:** switching to the discarded alternative `min(match.id)` ties identically (both players share the match) and would additionally publish a bracket coordinate as a timestamp, since `generate_bracket` creates every match row at once. There are no per-event timestamps in the parser — the reason DECISION C exists at all. **Reason deferred — DECISION (Cuatro, 2026-08-03, during the 6.2 code review):** keep `min(approved_at)` and document it; the ladder is 6.5's. **Intended home: 6.5 — a NAMED BLOCKER, not a caveat. The ladder must carry a deterministic terminal rung (it has a seeded PRNG for exactly this), because rung 4 is guaranteed to fail for the most common tie shape in a 1v1 bracket.** [edge + auditor]
- [x] **`achievement_ts` tracks admin approval order.** `[supabase/migrations/0024_ceremony_lock_snapshot.sql:585]` — the value is a function of the order an admin presses Aprobar, inside a ceremony whose premise (FR-27) is that a skeptic can verify nobody influenced the outcome. DECISION C was approved on the grounds the proxy is "deterministic, monotone and fully verifiable" — all true, and orthogonal to operator-influenceability, which was not on the table at sign-off. **Reason deferred — DECISION (Cuatro, 2026-08-03):** exploiting it requires an admin to pre-plan approval order against a tie they cannot yet see, in a private friends'' tournament; the non-manipulable alternative costs chronological meaning and does not fix the tie anyway. Documented in the column comment rather than changed. **Intended home: revisit only if the ceremony ever becomes adversarial.** [blind + auditor]

## Deferred from: code review of 6-3-prng-core-hmac-sha256-counter-mode (2026-08-03)

_Baseline `eed5318`; 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 5) over the working-tree diff (~3,050 lines, ~1,016 of them vector JSON), plus a reviewer-independent mutation pass of 12 mutations. Gates re-measured and all match the author's claims exactly (lint 0 / Vitest 704 across 38 files / build 0 with every viewer route still dynamic / Go clean). AC1, AC2, AC4, AC5 MET; AC3 PARTIAL. All 13 block cases and all 135 uniform draws were recomputed from the spec by an independent implementation with 0 mismatches, and the AC4 external anchor was re-run verbatim. The mutation pass left 2 survivors and confirmed all three of the author's named blind spots are in fact covered. Four decision-needed items and twenty-two patch items are tracked in the story's Review Findings, NOT here. The items below are real but not actionable in this slice._

- [x] **THE BAR''s cross-language transcript cannot be re-derived from the committed tree.** `[_bmad-output/implementation-artifacts/6-3-prng-core-hmac-sha256-counter-mode.md:315]` — the story''s central claim is that Go, TS and Python produce a byte-identical 729-line transcript over the real frozen seed (SHA-256 `0AC84C1B…CC1881`), but both harnesses (`worker/cmd/qa63/main.go`, `lib/roulette/bar-qa63.test.ts`) were deleted before commit, so the single piece of evidence for cross-runtime *equivalence* is not in the repository and is not gated by CI. What remains gated is *conformance*: each suite independently matches `roulette/vectors/`, which is a strictly weaker property covering 13 block cases and 135 draws. Two consistency checks do pass — 729 = 1 + 13 x (1+2+40+12+1) exactly, and every value in the pasted excerpt matches the independently recomputed vector rows. **Reason deferred:** Task 5 explicitly mandated the harnesses be deleted before commit, so the author followed the story. **Intended home: 6.11 — it owns the vector suite and is the natural place for a committed, CI-gated cross-language equivalence run.** [blind + auditor]
- [x] **`crypto.subtle` is dereferenced with no feature detection, in the one module whose purpose is running in a browser.** `[lib/roulette/prng.ts:137,180]` — it is `undefined` in any non-secure browsing context (plain HTTP, some embedded webviews), so 6.9''s "Verificar la ceremonia" button fails as `TypeError: Cannot read properties of undefined` from inside `read()` rather than as a typed refusal. Every other precondition in this module gets a loud, named error; the only *environmental* one gets nothing. **Reason deferred:** the deployment context that determines whether this is reachable (and what the user-facing message should say) belongs to the story that ships the button. **Intended home: 6.9.** [blind] **🟡 STILL OPEN after Story 6.9a — CONFIRMED, not overlooked.** 6.9a ships the bundle but no browser path (`worker/ceremony.Run` and `publish_bundle` both have zero production callers), so `crypto.subtle` is still not reached from a browser. ⛔ It becomes live the moment 6.9b's `Verificar la ceremonia` button ships. **Home: 6.9b, its AC9.**
- [x] **A rejected `subtle.sign` leaves the stream partially consumed.** `[lib/roulette/prng.ts:154-165]` — if the HMAC call rejects mid-`read`, `#pos` has already advanced by the bytes read so far, with no way to observe or repair the partial consumption; Go''s `Read` cannot fail mid-loop, so the two runtimes cannot even fail identically. **Reason deferred:** the fix is the same code path as the async-reentrancy decision in the story''s Review Findings and should be made once, together with it. **Intended home: whichever option Cuatro picks for the reentrancy item.** [blind]
- [x] **Three of the four TS import-ban tests currently assert over an empty list.** `[lib/roulette/prng.test.ts:474-492]` — `labels.ts` and `prng.ts` have zero imports, so `importSpecifiers()` returns `[]` and the `server-only`, `node:` and third-party bans all pass with no executed assertion. They are correct forward-looking guards, but "the bans are tested" is not what happens today, and a regression in `importSpecifiers` itself would be invisible here. **Reason deferred:** they start biting the moment `lib/roulette` gains its first import, which 6.4 will bring. **Intended home: 6.4 — add a positive control when the first real import lands.** [blind]
- [x] **TS `#pos` loses precision past 2^53; neither language handles counter overflow at 2^64.** `[lib/roulette/prng.ts:94]` — Go''s `pos` is `uint64`, TS''s is a double, so the two runtimes have different (and unguarded) failure thresholds. Reaching either needs on the order of 2.8e14 HMAC calls. **Reason deferred:** unreachable by many orders of magnitude; recorded for completeness only. **Intended home: none unless the engine is ever reused outside a ceremony.** [edge]
- [x] **pgTAP 1155 / 25 files is the one gate resting entirely on an unshown run.** `[_bmad-output/implementation-artifacts/6-3-prng-core-hmac-sha256-counter-mode.md:399]` — the story correctly claims the suite is untouched and `git status` confirms `supabase/**` is byte-unchanged, and 25 test files are present; but the notes paste no pgTAP output, and re-measuring needs a live local stack. Every other gate in the table was re-measured and matched exactly. **Reason deferred:** the claim is structurally sound (this story adds no migration and no test file) and verifying it costs a full stack bring-up for a suite nothing in the diff can reach. **Intended home: the next story that touches `supabase/**` and has the stack up anyway.** [auditor]

## Deferred from: code review of 6-4a-stage-2-deterministic-winner-and-tie-detection (2026-08-04)

_Baseline `3f7c98e`; 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 5) over the working-tree diff (~3,620 lines reviewed; `stage2-resolve.json`'s 3,654 lines of generated data read from the tree rather than inlined). 25 findings after dedup: 3 decision-needed, 17 patch, 2 deferred, 3 dismissed. Gates re-verified against the tree and all match the author's claims (Vitest 854/39, Go `awards` 206, `--check` OK x3, scope byte-untouched, `0025` free). AC1 and AC2 MET; AC3 PARTIAL (the vector's coverage guards are weaker than they read, and the anchor's refusal surface is smaller than both runtimes'); AC4 MET but its evidence is not re-derivable from the tree. Three decision-needed items and seventeen patch items are tracked in the story's Review Findings, NOT here. The two items below are real but not actionable in this slice._

- [x] **Nothing automatically runs `generate_vectors.py --check`.** `[roulette/vectors/generate_vectors.py:1180]` — both suites read `stage2-resolve.json` off disk; neither invokes the generator, and the repo has no `.github/workflows` and no `npm` script for it, so the third-implementation anchor is enforced only by a human remembering to run it. Failure mode: someone whose implementation fails a case edits `expected.steamid64` in the JSON instead of fixing the code — `go test`, `vitest`, `lint` and `build` all go green and the committed vector silently becomes a transcript of a buggy implementation, which is exactly the property the README says committing the generator exists to prevent. **Reason deferred:** the project has no CI at all and gates are run by hand per story; 6.3 shipped its two vector files under the identical condition, so this is a project-wide gap rather than something 6-4a introduced. **Intended home: 6.11 — it owns the vector suite and is the natural place for a committed, gated regeneration run (it already inherits `deferred-work.md:288` for the same reason).** [blind]
- [x] **The Python anchor's independence is weaker than AC3 claims.** `[roulette/vectors/generate_vectors.py:459-497]` — AC3's whole justification is that expected values come from a third implementation written from spec text alone, "so the vector proves conformance to the spec rather than mutual agreement of two possibly-identical mistakes". The generator carries prose VERBATIM identical to both shipped implementations, including implementation commentary rather than spec transcription: `# Unreachable: with non-negative denominators the comparator is a total preorder` and the `"empty best set — the comparator is not a total preorder"` error string appear word-for-word in all three; so do `_cmp`'s "a silent filter is an argmax by another name" and `_eligible`'s "the deciding magnitudes are validated where they are read". Several rules the vector pins appear in NO spec document and are identical in all three only because they were authored together in this story: the negative-denominator and negative-numerator refusals, the negative-volume refusal, the duplicate-`steamid64` refusal, and DECISION E. For those rows the "external anchor" is three co-authored implementations agreeing — the property AC3 exists to exclude. Note this is a weakening, not a falsification: the arithmetic core (floors, byte-lex order, cross-multiplication, best-as-set) IS derivable from SOLUTION-DESIGN §9.3 and AD-14, and the shared error prose is evidence about the comments, not about the expected values. **Reason deferred:** unfixable within this story — an anchor written in the same slice by the same author cannot be made retroactively independent, and rewriting it now would only relaunder the same reasoning. **Intended home: 6.11 — when the end-to-end ceremony vector lands, re-derive the Stage-2 expectations from the spec independently (ideally by a different agent or model) and diff them against the committed file; a clean diff converts this from a claim into a measurement.** [auditor]

## Deferred from: code review of 6-5-fr-29-tie-ladder (2026-08-04)

_Baseline `ad8b649`; **GROUP 1 of 3 only — the logic layer.** 3 parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 5) over `worker/awards/ladder.go` and `lib/roulette/ladder.ts` in full plus the working-tree diff of `stage1.*`, `stage2.*` and `lib/awards/catalog.ts` — ~2,430 lines. 13 findings after dedup: 2 decision-needed, 6 patch, 5 deferred, 4 dismissed. **Group 2 (the test suites and the two pinning files) and Group 3 (`generate_vectors.py`, `ladder-resolve.json`, `stage1-pick.json`, `stage2-resolve.json`, the vectors README) are NOT yet reviewed.** The decision-needed and patch items are tracked in the story's Review Findings, NOT here. The five items below are real but not actionable in this slice._

- [x] **The catalog can no longer express a NULL rung key, so the L2 skip is unrepresentable by its only producer.** `[lib/awards/catalog.ts:101,113,114]` — `SeedAward.secondaryStat` / `effNumKey` / `effDenKey` are typed `StatKey`, not `StatKey | null`, and `CurateAwardPayload` mirrors that. `0023`'s columns are nullable, `0024`'s DECISION B left them NULL deliberately, and both ladders implement absent-is-a-skip as a first-class published contract — but nothing upstream can now produce it. Not a defect today (all twelve are filled, and the vector exercises the branch), and arguably the type honestly encodes "our catalog always fills them". **Reason deferred:** whether the catalog should ever ship a NULL is a product call bound up with the rung-2 efficiency-pair decision item in the story's Review Findings, and the fix should be made once, with it. **Intended home: whichever option is picked for that decision.** [blind]
- [x] **`LadderExitStep` spells "none" two different ways across the seam, and Go's field is an unconstrained `int`.** `[worker/awards/stage2.go:312, lib/roulette/stage2.ts:293]` — Go carries `LadderExitStep int` with `LadderExitNone = 0` on every outcome; TS omits the key entirely and constrains it to `1|2|3|4` on `winner` and `5` on `shared`. Both are internally coherent and each is idiomatic for its language, but any serialization comparison must special-case the two spellings of NULL, and Go's `int` will happily carry `7` where the TS union will not compile. **Reason deferred:** nothing in this story serializes an `Outcome`; the normalisation belongs where the field is canonicalized and persisted. **Intended home: 6.8 (the nullable `award_result.tie_ladder_exit_step` column) and 6.9 (RFC-8785 canonicalization).** [blind] **🟢 CLOSED by Story 6.9a (2026-08-09).** DECISION J picks ONE spelling for the bundle and states it at every site: **NULL is spelled ABSENT** — the key is omitted, never `0` and never `null`, because JCS hashes `0`, `null` and absent as three different documents. `applyLadderExitStep` was extracted so the rule has one implementation, the hash difference was measured, and mutant M27r kills it. ⚠ 6.9b CONSUMES this spelling; it does not re-pick it.
- [x] **A negative shelf index panics in Go and returns `undefined as number` in TypeScript.** `[worker/awards/stage1.go:470, lib/roulette/stage1.ts:463]` — 6-4b's W8 clamp is one-sided (`if index > tableMax`), so a negative shelf entry, or an empty `table` making `tableMax = -1`, indexes out of range: a loud panic in Go, a silent `undefined` cast to `number` poisoning a byte-accounted draw in TS. Opposite failure kinds for one input, in a pair of runtimes whose whole contract is identical behaviour. **Reason deferred:** pre-existing in 6-4b's clamp and not reachable through this story's new path — the minimum over co-winners' shelves is a minimum of non-negative counts. **Intended home: the next story that touches the `luck_weight_table` indexing (6.6 owns its VALUES).** [blind] — ✅ **CLOSED BY STORY 6.6 (2026-08-07).** All three implementations now index the table at ONE site (`weightAt` / `weightAt` / `_weight_at`), guarded on BOTH ends, which REFUSES rather than silently clamping to `table[0]` — reaching it means an upstream guard was removed, and `table[0]` is the heaviest weight, i.e. the plausible-but-wrong answer. Typed `shelf` / `weight_table` details, a positive control in each suite, and the non-representability of both arms declared rather than faked (a vector row is a set of INPUTS and every input that could reach them is refused by a guard that runs first). Mutation-verified: removing either arm reddens in both languages.
- [x] **`ladder.ts` transcribes `stage2.ts`'s private `validateAward` instead of calling it, so the two can drift silently.** `[lib/roulette/ladder.ts:714]` — Go's ladder **calls** the real `validateAward` (`ladder.go:623`); TypeScript re-implements it as `validateStage2AwardSurface`. The site comment states the trade honestly (exporting a resolver's internals for one caller is its own hazard) and claims *"the four clauses below are the same four"* — **verified true at review time**: all four clauses are byte-verbatim identical, differing only in one curly apostrophe. But the asymmetry is one-directional (Go can never drift, TS can), and the single vector row named as the guard exercises one of the four clauses, leaving three of them and their ordering unpinned. **Reason deferred:** no current defect, and the architectural objection to exporting `validateAward` is legitimate; the durable fix is a vector row per clause, which is the vector group's territory. **Intended home: Group 3 of this review, or 6.11's vector consolidation.** [blind]
- [x] **`decidingValue` was made optional — a type widening the spec did not name — and the "`deciding_value` XOR `ladder_exit_step`" invariant is enforced only by construction.** `[lib/roulette/stage2.ts]` — Task 2 authorised only *"the `winner` arm gains an optional `ladder_exit_step`"*; the diff also made `decidingValue` optional, which the story's own transcribed algorithm (`winner(S1[0], exit_step = 1)`) genuinely forces. The **behaviour** is correct and was verified in both languages: the ladder never sets a deciding value, and the pure stage never sets an exit step. The **invariant** is not expressible in either type system as written, so `{kind:'winner', steamid64}` carrying neither field, and one carrying both, are type-valid. No production consumer outside `stage2.*`/`ladder.*` reads `decidingValue`, so nothing broke. **Reason deferred:** a discriminated pair would make the violation unrepresentable, but 6.8 is the story that persists both columns and should own the shape — the Completion Notes already hand it the related question of deriving a `deciding_value` for a ladder-resolved award. **Intended home: 6.8.** [auditor]

## Deferred from: code review of 6-5-fr-29-tie-ladder, Groups 2 & 3 (2026-08-04)

_Baseline `ad8b649`; the test suites and the vector seam. Six parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor over each group, all Opus 5). One decision-needed and nine patch themes are tracked in the story's Review Findings, NOT here — including the headline, which is that the Group-1 patches left `generate_vectors.py` behind and the anchor now RESOLVES inputs both runtimes REFUSE. AC4's mandatory-row scorecard came back 31 of 32; `--check` is OK on all five; `stage2-resolve.json` really did change only `outcome_kinds`. The four items below are real but not actionable in this slice._

- [x] **`stage2-resolve.json` declares an outcome kind its own stage cannot produce, with no unreachable marker.** `[roulette/vectors/stage2-resolve.json, generate_vectors.py:356-361]` — `"shared"` was appended to `outcome_kinds`, and the generator's own comment concedes "the PURE stage can never produce it". Both runtimes' Stage-2 exhaustiveness checks are therefore widened to accept a value their Stage-2 code should reject, and no case in that file emits it. The ladder's sibling problem is handled properly — `internal` is separated out by `ROW_REPRESENTABLE_LADDER_DETAILS` and flagged in the README — but `outcome_kinds` carries no such split. **Reason deferred:** AC3 explicitly required this widening and required `stage2-resolve.json` to regenerate with `outcome_kinds` as its only changed bytes, which it did; adding a marker now would change those bytes again. The right home is where the kind vocabulary is canonicalized. **Intended home: 6.9.** [blind] **🟢 CLOSED by Story 6.9a's CODE REVIEW (2026-08-09), not by 6.9a's first pass.** `unreachable_outcome_kinds: ["shared"]` is now emitted into `stage2-resolve.json` from `UNREACHABLE_STAGE2_OUTCOME_KINDS`, in the same `unreachable_*` marker form `antisweep-resolve.json` uses, so both suites can assert ZERO cases carry it instead of inferring the absence from prose. ⛔ The first pass ticked this subtask, wrote "NOT done" in its own Completion Notes, and told 6.9b it was "shipped" — three artifacts, three answers, no code. The review found the gap and wrote the marker.
- [x] **The W8 "clamp AFTER the minimum" comment asserts a distinction with no observable difference.** `[worker/awards/stage1.go:449-453, lib/roulette/stage1.ts:461-463, generate_vectors.py:437-439]` — `min(x, tableMax)` is monotone non-decreasing, so `clamp(min(S))` and `min(clamp(S))` are equal for all inputs. No fixture can distinguish them and no implementation can get it wrong. The comment reads as a pinned invariant and is not one, which devalues the genuinely load-bearing `min`-vs-`max` rule three lines above it. **Reason deferred:** the comment is 6-4b's, pre-existing, and the co-winner arm that inherited it is correct; rewriting it touches a shipped file for a documentation-only gain. **Intended home: the next story that touches the weight table (6.6 owns its VALUES).** [blind] — ✅ **CLOSED BY STORY 6.6 (2026-08-07)**, and closed by making the question unaskable rather than by rewording it: with exactly ONE clamp at exactly ONE indexing site (`weightAt`), "clamp before or after the minimum" is not a rule anybody can violate. The comment now says that, in all three implementations, instead of asserting a distinction with no observable difference next to the genuinely load-bearing `min`-vs-`max` rule. ⚠ Its stale `generate_vectors.py:437-439` citation is moot — the site moved into `_weight_at`.
- [x] **The anchor's independence is weaker than the Completion Notes claim, though the evidence now cuts both ways.** `[roulette/vectors/generate_vectors.py]` — eight distinctive error phrases are verbatim-shared across all three implementations (*"an absent key is a refusal, never a zero"*, *"is not one of the 21 vocabulary keys"*, *"half-configured rung"*, *"has no matching snapshot row"*, *"strictly ascending byte-lex"*, *"below the published absent sentinel"*, *"eff_num_key and eff_den_key are both-or-neither"*, *"the comparator is broken"*), and four rows are labelled in-file as added in response to a measured mutation survival rather than derived from spec text. The latter is what the story MANDATES ("fix the vector, not the test"), so it is not a violation — the Notes' unqualified *"not from either implementation"* should be read as "the algorithm was; four rows' selection was not". ⭐ Note the counter-evidence, which is strong: the anchor LACKS two guards both runtimes have, which is hard to explain if it had been derived from either. **Reason deferred:** unfixable within this story for the same reason 6-4a's twin was — an anchor written in the same slice by the same author cannot be made retroactively independent. **Intended home: 6.11, alongside `deferred-work.md:300`, which this extends to `ladder-resolve.json`.** [blind+auditor]
- [x] **The 21-key stat vocabulary is pinned across three artifacts but never linked to `0023`, which is the actual source of truth.** `[roulette/vectors/generate_vectors.py:194-198]` — shipping the list in the vector ties the generator and the two suites together, but nothing compares it to `0023`'s `award_deciding_stat_valid` / `award_secondary_stat_valid` / `award_eff_*_key_valid` CHECKs or to `award_stat_vocabulary()`. A key added to the database and to Go alone still passes if the anchor is updated to match. The pin is also by exact equality over a hand-written, non-alphabetical order, so a purely cosmetic reordering reddens two runtimes. **Reason deferred:** closing it means reading `supabase/**` from a test, which the no-dependency-edge rule and this story's byte-untouched constraint both forbid; it needs a deliberate decision about where the vocabulary's single definition site lives. **Intended home: 6.11, or whichever story next revisits the catalog vocabulary.** [blind]

## Deferred from: code review of 6-5b-fr-29-ladder-vector-and-suite-debt (2026-08-06)

_Baseline `4c9cf36`; the completion pass on 6.5's ladder. Three parallel layers (Blind Hunter + Edge-Case Hunter + Acceptance Auditor, all Opus 5) over the full working-tree diff in ONE pass (~9,810 lines; the two regenerated vectors read as evidence rather than line by line). 25 findings after dedup: 2 decision-needed, 15 patch, 4 deferred, 4 dismissed. ⭐ Every runnable gate was re-derived by the reviewer and every one MATCHED the author's printed numbers — lint 0, Vitest 1183/41, build 0, Go clean, gofmt empty, `--check` OK x5, 37/29 and 17/19 rows, 0 CRLF x5, all ten excluded paths byte-untouched, `0025` free, `qa65b` absent. AC1/AC2/AC3/AC5/AC8/AC9 MET; AC4/AC6/AC10 PARTIAL; AC7's guard MET but its measurement unverifiable (harness deleted by design). The headline finding — that the pass closed the vacuous-guard defect in the ANCHOR while re-introducing it in the two CONSUMER suites, making this its fourth occurrence rather than its third — and the two decision-needed items are tracked in the story's Review Findings, NOT here. The four items below are real but not actionable in this slice._

- [x] **An absent `secondary` or `efficiency` container is never READ by a configured rung.** `[lib/roulette/ladder.ts:325,361, worker/awards/ladder.go:290,333]` — the new absent-container row omits all three blocks from one player, but its award has `secondary_stat`, `eff_num_key` and `eff_den_key` all null, so rungs 1 and 2 SKIP and only `h2h` is dereferenced. `container(byId.get(sid)?.secondary)` and Go's nil-`Secondary` map read remain entered by zero rows. The unexercised path is the one the rule is about: an absent block WITH the rung configured must refuse `player` ("an absent key is a refusal, never a zero"), and a TS implementation writing `byId.get(sid)!.secondary[key]` throws a bare `TypeError` on that input with nothing reddening. **Reason deferred:** the fix is a new case (a second absent-container row whose award CONFIGURES the omitted rung), which is the same "add a row and re-run the mutation gate" cost as the six boundaries in the story's decision-needed item, and it should be decided with them. **Intended home: 6.11's vector consolidation, or whichever option is picked for that decision.** [edge]
- [x] **`players` supplied as a non-array refuses under a different `detail` in TypeScript than in Go and Python.** `[lib/roulette/ladder.ts:851-853 vs worker/awards/ladder.go:782-796, roulette/vectors/generate_vectors.py:3245]` — `resolveLadder(award, tied, null)` refuses `player` in TS ("players must be an array"); Go's nil slice and Python's `players or []` both yield an empty index and refuse `tied` ("tied member has no matching snapshot row"). One input, two labels, at a PUBLIC entry point that 6.6 drives directly. Not expressible as a vector row — Go's parameter is typed, so the input cannot exist there — and the TS module does flag the split as deliberate. **Reason deferred:** unrepresentable in the shared vector by construction, so closing it means either a language-local test asserting the divergence is intentional, or recording it in the six-group `spec` string the way the other TS-only guards are. Both are documentation decisions about the contract surface rather than defects. **Intended home: 6.11, alongside the other contract-surface reconciliations.** [edge]
- [x] **DECISION B(2) landed wider than it was declared.** `[roulette/vectors/generate_vectors.py:1683]` — the authorised edit was "a `None`/nil guard on `stage1_weight`'s **`shared`-branch** `shelf` dereference"; what shipped is `shelf = {} if shelf is None else shelf` at the TOP of the function, normalising BOTH arms. It is defensible (it makes the anchor agree with Go's nil-map-is-empty semantics), it is unreachable through `stage1_weights`/`stage1_pick` which normalise first, and no test in any language calls the function that way. But it is a wider edit than the one authorised, and the Completion Notes describe it only as "normalises a `None` shelf". The Blind Hunter reached the same line independently and objected on different grounds: if publicness is the argument, `table`, `players` and `ladder` get no equivalent treatment in the same function. **Reason deferred:** behaviourally inert and inside the anchor rather than shipped source; narrowing it now would churn the generator for no observable gain. **Intended home: whichever story next revisits `stage1_weight` — 6.6 owns the weight table.** [auditor+blind]
- [x] **Rung-4 `tsOf` asymmetry between the two test loaders.** `[worker/awards/ladder_test.go, lib/roulette/ladder.test.ts]` — Go's helper returns `false` if any tied member's `achievement_ts` is missing; TypeScript's returns `undefined` and the filter simply does not count it as a sentinel. Different behaviour on a malformed regeneration, in the guards whose purpose is to detect exactly that. No current row reaches it. **Reason deferred:** reachable only through a hand-edited or corrupted vector, and the two guards agree on every row in the committed file; levelling them is part of the broader claim-strengthening pass the story's patch items already cover. **Intended home: fold into whichever pass addresses the weak-claim patch items.** [blind]
## Deferred from: code review of 6-6-anti-sweep-and-luck-meter (2026-08-07)

_Baseline: the working tree over `26dc674` (6.6's changes are entirely uncommitted). Three parallel blind layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor, all Opus 5) over 5,679 reviewed lines plus the 10,989-line generated vector, interrogated structurally rather than line by line. 46 raw findings -> 38 after dedup: 4 decision-needed, 21 patch, 10 deferred, 3 dismissed. Every runnable gate was re-derived by the reviewer and every one MATCHED the author's printed numbers -- lint 0, Vitest 1302/42, build 0 with every viewer route still dynamic and no `roulette` route, Go `awards` 602 PASS / 0 FAIL, gofmt empty, `--check` OK x6 with the five pre-existing vector files byte-identical, all ten excluded scope paths byte-untouched, `supabase/**` carrying only `0025` + its pgTAP file. pgTAP itself was NOT re-run (it needs `supabase db reset`), so 1218/26 stands unverified. AC1/AC2/AC4/AC5 DISCHARGED; AC3 and AC6 PARTIAL. Two layers converged independently on five findings. The headline items -- the injected `Ladder` port's unguarded winner, the four harness-dependent measurements that can no longer be re-derived, and the false premise in the equivalent-mutant proof -- are tracked in the story's Review Findings, NOT here. The ten below are real but not actionable in this slice._

- [x] **The `is_shared` assertion trigger is `security invoker` under `FORCE ROW LEVEL SECURITY` with zero policies, so it fails OPEN rather than closed.** `[supabase/migrations/0025_ceremony_results.sql:243-245]` -- the `if not found then return null` branch conflates "the parent was deleted in this transaction" with "the parent is invisible to the invoking role under RLS", and `count(*)` over an RLS-filtered child set returns a filtered count. It works today only because the sole writer is `service_role`, which has BYPASSRLS. **Reason deferred:** DECISION B homes the grant AND the reveal-gated policy to 6.8, and they must be decided together; a `security definer` change now would be a policy decision taken by the wrong story. **Intended home: 6.8, with the grant and the policy.** [blind]
- [x] **`award_result_winner_result_key` is subsumed by the anti-sweep UNIQUE and can never be the constraint that refuses anything.** `[supabase/migrations/0025_ceremony_results.sql:230]` -- DECISION D's composite FK forces every child's `spin_id` to equal its parent's, so two rows sharing `award_result_id` necessarily share `spin_id`, and `unique (spin_id, winner_entry_id)` fires first. Its only coverage is `has_index`, which does not assert the index is UNIQUE -- nor do the other three `has_index` calls. **Reason deferred:** harmless redundancy, and removing a declared constraint from a shipped migration is a wider call than a review patch. **Intended home: whichever story next revisits `0025` -- 6.8 widens it.** [blind]
- [x] **TypeScript's `.sort()` is UTF-16 code-unit order, not byte-lex, and the comment states the equivalence as a definition.** `[lib/roulette/sweep.ts, the `sweptOut.sort()` / `assigned` sort sites]` -- Go's `sort.Strings` and Python's `sorted()` agree with each other and disagree with JS for code points in U+E000-U+FFFF versus the supplementary planes. Latent only: steamid64s are ASCII digits, so nothing diverges on any reachable input. **Reason deferred:** unreachable today, and the fix (an explicit byte-lex comparator) touches the ordering of a shipped result field. **Intended home: 6.9, when `lib/roulette` ships to the browser and the byte-lex claim becomes load-bearing for the verifier.** [blind] **🟢 CLOSED by Story 6.9a (DECISION K).** `STEAMID64_RE` is carried into `sweep.*` in all three runtimes, which makes the byte-lex claim true BY CONSTRUCTION rather than by comment. Pulled forward out of 6.9b because it is outcome-affecting for `bundle_sha256`. ⚠ The 6.9a code review additionally fixed the two refusal MESSAGES here, which still said "empty" / "no steamid64" after the guard had moved to a regex — so a present-but-non-numeric id reported a missing field.
- [x] **`lib/roulette/prng.test.ts > real-seed/spin1/i255` times out at 5000 ms under full-suite load.** `[lib/roulette/prng.test.ts:118]` -- it passes in 3.28 s when the file runs alone (201/201) and fails the 5 s per-test timeout when all 42 files run in parallel, so `npm test` is not reliably green on this machine. **PRE-EXISTING and not 6.6's** -- it is a PRNG conformance test over a byte-identical `prng-block.json`. **Reason deferred:** it is a test-infrastructure timeout, not a correctness defect, and raising `testTimeout` is a project-wide config decision. **Intended home: whichever story next touches `vitest.config.ts`.** [reviewer, measured this session]
- [x] **Migration `0025` re-uses DECISION letters A-D with meanings different from the story's DECISIONS A-D.** `[supabase/migrations/0025_ceremony_results.sql]` -- `0025`'s "DECISION B" is "no RLS policy" (the story's B is "anti-sweep is a pure producer-side pass"), its "DECISION C" is "`is_shared` is kept and asserted" (the story's C is the untouched FR-21 floors), its "DECISION D" is the declarative FK-consistency check (the story's D is the full Stage-2 re-run). A reader arriving from the story reads the opposite subject. **Reason deferred:** documentation coherence only, in a story whose record is otherwise dense with correct decision-letter cross-references. **Intended home: 6.8, which widens `0025` and can renumber in the same edit.** [auditor]
- [x] **`array_length(t, 1) < 1` is unreachable in `luck_weight_table_valid`.** `[supabase/migrations/0025_ceremony_results.sql, section G]` -- every empty array has `array_ndims` NULL and is already rejected by the first branch; anything reaching the third has `ndims = 1` and `lower = 1`, hence `length >= 1`. Harmless, but it is presented as one of three load-bearing guards and the pgTAP suite's `array[]::int[]` case credits it with a refusal it does not perform. **Reason deferred:** dead-but-defensive, and it reads as intentional depth; the decision is whether to say so or delete it. **Intended home: fold into whichever pass addresses the untested-CHECK-branch patch item.** [blind]
- [x] **No vector case carries an empty or single-player roster.** `[roulette/vectors/antisweep-resolve.json]` -- measured player counts across the 16 cases are `3` x14 and `2` x1. So `players: []` -- every award `no_eligible_players`, every `swept_out` empty, `assigned` empty -- is never resolved through the shared seam, even though "an absent container IS the empty container" is a named decision restated in all three implementations and is exactly where the `players: null` divergence lives. A single-player roster is likewise absent; the exhaustion row covers 2. **Reason deferred:** adding rows means regenerating the vector and re-running the mutation gate, which is the same cost as the story's decision-needed vector items and should be decided with them. **Intended home: 6.11's vector consolidation.** [edge]
- [x] **`TestResolveSpinDoesNotMutateItsInputs` verifies reordering only, not mutation.** `[worker/awards/sweep_test.go:3703-3739, lib/roulette/sweep.test.ts:4639-4651]` -- both capture only the SEQUENCE OF IDENTIFIERS before and after, so a pass that mutated `players[i].Volume`, zeroed `IdleDq` or rewrote `live[i].Award.Direction` in place passes both suites. The genuine risk it guards (an in-place `sort` of the caller's slice) IS covered; the property asserted is narrower than the property named. **Reason deferred:** the real hazard is closed, and a deep-equality snapshot is a test-strengthening pass rather than a defect fix. **Intended home: fold into whichever pass addresses the weak-claim patch items.** [blind]
- [x] **`pg_get_expr` string-pins a deparsed expression eight lines after the file argues against string pins.** `[supabase/tests/0025_ceremony_results_test.sql:461-465]` -- the rendered form depends on `search_path` (whether the function is schema-qualified) and on the server version's deparse rules, and the very next assertion carries a comment explaining why the sibling `column_default` is asserted SEMANTICALLY rather than by its rendered string. The four `throws_ok` cases already provide the semantic equivalent. **Reason deferred:** brittle rather than wrong, and it currently passes; removing an assertion changes the plan count. **Intended home: fold into whichever pass addresses the pgTAP patch items.** [blind]
- [x] **Task 2's mandatory row "a spin where the HIGHEST-PRIORITY award is the one that overflows" is unsatisfiable as written, and the substitution was silent.** `[_bmad-output/implementation-artifacts/6-6-anti-sweep-and-luck-meter.md, Task 2]` -- higher priority is the LOWER priority number, so the highest-priority award is processed FIRST with `assigned` still empty and can never overflow. The shipped `the-award-with-the-LOWER-PRIORITY-NUMBER-KEEPS-the-trophy` (where `aw-07`, the lowest-priority award, overflows) is the correct reading and pins the sort direction. **Reason deferred:** record-keeping only -- the vector is right; the checkbox was ticked without noting that the mandatory-row list was itself wrong. **Intended home: no code change; note it if the row list is ever reused as a template.** [auditor]
## Deferred from: code review of 6-7-pity-roulette (2026-08-07)

_Baseline: the working tree over `ba894d1` (6.6 IS committed; 6.7 is entirely uncommitted). Three parallel layers (Blind Hunter + Edge Case Hunter + Acceptance Auditor, all Opus 5) over ~3,400 reviewed lines of code plus the 2,534-line generated vector, interrogated structurally rather than line by line. 28 raw findings -> 22 after dedup and verification: 2 decision-needed, 16 patch, 4 deferred, 6 dismissed. Every runnable gate was re-derived by the reviewer and MATCHED the author's printed numbers EXCEPT the Vitest baseline -- lint 0, Vitest 1365/43 (after) but baseline 1322/42 not the claimed 1318/42, build 0 with every viewer route still dynamic and no `roulette` route, Go `awards` 641 PASS / 0 FAIL, gofmt empty, `--check` OK x7 with the six pre-existing vector files byte-identical, all excluded scope paths byte-untouched, `supabase/**` carrying only `0026` + its pgTAP file. pgTAP itself was NOT re-run (it needs `supabase db reset`), so 1239/27 stands unverified. AC1/AC2/AC3/AC4/AC5/AC7 MET; AC6 PARTIAL. Two layers converged independently on the `.sort()` finding and on the unwrapped `uniform_int` error. The headline items -- the duck-typed stream guard, the two coverage guards that cannot fail, and the untyped refusal surface in TS/Python -- are tracked in the story's Review Findings, NOT here. The four below are real but not actionable in this slice._

- [x] **AC6's 19-id set equality is printed at `live_count = 1` only, where AC6 asked for all four widths.** `[_bmad-output/implementation-artifacts/6-7-pity-roulette.md:982-990]` -- the winless CARDINALITY (19/28) and the shelf distributions ARE recorded at widths 1, 2, 3 and 4 and are identical across them, which makes the id sets near-certainly identical, but the AC asked for the printed SET EQUALITY at four widths and it is there for one. **Reason deferred:** the qa67 harness is deleted, so re-printing costs a full 14-demo corpus rebuild for a check whose answer is already implied by the recorded distributions. **Intended home: 6.11's end-to-end vector, which rebuilds the corpus anyway.** [auditor]
- [x] **`rejections` uses float division in TypeScript and integer division in Go and Python, and no vector row has `k != 1`.** `[lib/roulette/pity.ts:292, worker/awards/pity.go:371, roulette/vectors/generate_vectors.py:7499]` -- TS computes `consumed / k - 1`, Go computes `int(consumed/uint64(k)) - 1`, Python computes `(consumed // k) - 1`. Unreachable today on two independent grounds: `consumed` is always an exact multiple of `k` by `uniform_int`'s contract (every attempt reads exactly k bytes), so the float result is always integral; and `k > 1` needs `n >= 257`, i.e. a 257-member winless set, which a 28-player tournament cannot produce. All 11 draws across all 13 cases have `k = 1`, so the `k`-multiplication in `bytes_consumed = sum(k * (1 + rejections))` is never exercised at anything but 1. **Reason deferred:** no reachable divergence, and forcing `k > 1` means a synthetic 257-player fixture whose only purpose is arithmetic coverage. **Intended home: 6.9, when the browser verifier's arithmetic becomes load-bearing for `bundle_sha256`.** [blind+edge] **🟢 CLOSED by Story 6.9a (DECISION K) and COMPLETED by its code review.** TypeScript's float division became integer division with an exactness assertion. ⛔ The review found that assertion had been added to **TypeScript only** — Go and Python still truncated silently, and Go is the PRODUCER, so a partial-byte rejection would have written a wrong `rejections` into the published `draws[]` and been caught only in the browser, after the commitment was immutable. The guard now exists in all three runtimes.
- [x] **`ROW_REPRESENTABLE_PITY_DETAILS = PITY_REFUSAL_DETAILS` is an alias, so the equality both suites assert as data cannot redden.** `[roulette/vectors/generate_vectors.py:371]` -- both suites assert `row_representable_refusal_details == refusal_details` and explain at length that this makes the absence of an `internal` label a decision rather than an oversight, but the generator emits the same tuple object under both keys, so the two arrays can never differ except by hand-editing a generated file. The same aliasing makes the generator's own `if detail not in ROW_REPRESENTABLE_PITY_DETAILS` guard redundant with the check three lines above it. **Reason deferred:** the equality IS the intended design and the assertion correctly documents it; splitting the constants to make it falsifiable would be inventing a difference in order to test for it. **Intended home: whichever story first needs a non-row-representable pity detail -- if none ever does, the assertion is correct as documentation.** [blind]
- [x] **The doubly-malformed refusal rows' SECOND defect is re-derived only by the anchor; both runtime suites read `defects` as self-declared data.** `[worker/awards/pity_test.go:689-707, lib/roulette/pity.test.ts:3145-3153]` -- both boundary tests read `r.Defects[0]` / `r.Defects[1]` straight out of the JSON and check the pair is present, so a row declaring `"defects": ["players","shelf"]` while carrying a valid shelf would pass both suites while pinning no validation order at all. **Mitigating:** `build_pity_file` at `generate_vectors.py:8103-8127` DOES independently prove each second defect refuses on its own under a different label, so the property is genuinely verified -- just only by the generator. **Reason deferred:** nothing runs `--check` automatically (`deferred-work.md:299`, already homed to 6.11), so this is the same manual-gate exposure rather than a new one. **Intended home: 6.11, with the `--check` automation.** [blind]

### Resolved from decision-needed during the 6-7 code review (2026-08-07)

_Cuatro delegated both decisions to the reviewer; both resolved to DEFER. Recorded here because each is now owned by a LATER story rather than by 6.7._

- [x] **⚠ SECOND OCCURRENCE — TypeScript's `.sort()` is UTF-16 code-unit order, not byte-lex, in `pity.ts` as well as `sweep.ts`.** `[lib/roulette/pity.ts:241-246]` and the already-recorded `[lib/roulette/sweep.ts]` entry above. Go's `sort.Strings` (UTF-8 bytes) and Python's `sorted` (code points) agree with each other and disagree with JS for supplementary-plane characters. ⭐ **What makes 6.7's instance sharper than 6.6's:** `pity.ts` is the module 6.9 ships to the BROWSER to re-verify externally-supplied bundle JSON, and a divergent `winless` yields a divergent `reveal_order` while `draws` and `bytes_consumed` stay IDENTICAL -- so the byte-accounting gate this epic was built on structurally cannot see it. `validate_pity_players` requires only a NON-EMPTY STRING in all three runtimes, where `stage2.ts:657` guards the same field with `STEAMID64_RE = /^[0-9]+$/` and makes the weaker, CORRECT claim (`stage2.ts:707-716`: "identical to byte order FOR ASCII DIGITS"). Unreachable through the shipped DB (`player.steamid64 check (~ '^[0-9]{17}$')`, `0001:39`). **Reason deferred:** 6.7 is forbidden from touching `sweep.ts` (its Scope boundaries exclude 6.6's anti-sweep pass and result shape), so fixing only `pity.ts` would leave two sibling modules in one directory making DIFFERENT guarantees about the same field -- a split 6.9 would have to reconcile anyway. The comment was corrected in this slice so it no longer states a falsehood; the GUARD was not added. **Intended home: 6.9, BOTH modules together. ⭐ The concrete fix: carry `stage2.ts`'s `STEAMID64_RE` into `pity.*` and `sweep.*` in all three runtimes, which makes the byte-lex claim true by construction without inventing a non-ASCII vector row for an input the DB forbids.** [blind+edge, converged independently] **🟢 CLOSED by Story 6.9a (DECISION K)** — the same `STEAMID64_RE` fix as `:335`, applied to `pity.*` in all three runtimes. ⚠ This is the occurrence the byte-accounting gate structurally could not see (a divergent `winless` yields a divergent `reveal_order` while `draws` and `bytes_consumed` stay identical), which is why `publish_bundle` now ALSO checks that `reveal_order` is a permutation of `winless` — added by the 6.9a code review.
- [x] **`is_pity = true` on a `kind = 'main'` spin became representable when `0026` dropped `award_id`'s NOT NULL, and nothing constrains it.** `[supabase/migrations/0026_pity_award_result.sql:80-92]` -- nothing links `award_result.is_pity` to `spin.kind`; 0025 carries only `spin_kind_valid check (kind in ('main','pity'))` (`0025:119`) and `is_pity boolean not null default false` (`0025:153`). ⭐ **The second-order consequence is the real one:** NULLs are DISTINCT in a UNIQUE, so `award_result_spin_award_key` (`0025:178`) no longer bounds a main spin's results AT ALL -- unlimited award-less "pity" rows can accumulate inside one `kind='main'` spin. 0026's own thesis is that the schema should make "which award was it?" UNREPRESENTABLE rather than answered wrong; for a main-spin row it is now UNANSWERABLE on the say-so of an unconstrained flag. The pgTAP suite's Section D proves the NULL-distinct behaviour is WANTED for pity spins without checking it is CONFINED to them, and no row attempts `is_pity = true` on `main_spin`. **Reason deferred:** `award_result` is UNWRITABLE until 6.8 (0025's own table comment says nothing writes a row until then, and 6.7 adds no writer), so there is no live exposure; Cuatro authorised exactly one migration for this slice; and 6.8 writes both `spin.kind = 'pity'` and `is_pity`, so it is the only story that can decide the pairing with the real write shape in front of it. ⛔ A pgTAP row BLESSING the state as legal was deliberately NOT added -- it would pin behaviour 6.8 is likely to constrain and would then have to be deleted. **Intended home: 6.8, alongside the pity write. Mechanism to consider: a `check (not is_pity or spin.kind = 'pity')` enforced by trigger or by a denormalised `spin.kind` column, decided together with the writer.** [edge]

### Deferred from: code review of 6-8a-ceremony-run-and-persistence (2026-08-08)

- [x] **`ceremony.luck_weight_table` stays freely mutable after the ceremony is frozen, so a persisted run can be made unreproducible while every freeze column still looks intact.** `[supabase/migrations/0025_ceremony_results.sql]`, surfaced against `[supabase/migrations/0027_ceremony_run.sql:427-511]` -- `0027`'s `assert_ceremony_transition` freezes `snapshot_id` and `seed_demo_sha256` write-once and pins `tournament_id`, but `luck_weight_table` is left writable at every state including `spinning` and `complete`. It is an INPUT every drawn byte is a pure function of: Stage 1's weight lookup reads it (`stage1.go:213-229`), so editing it after a run silently changes what a re-derivation produces while `snapshot_id` and `seed_demo_sha256` -- the two things a verifier checks -- both still match. ⭐ This is the one thing the strict column-allowlist reading of Task 3 would have caught that DECISION 4's transition trigger does not; DECISION 4 is nonetheless correct to reject the allowlist, because `lock_ceremony` sets four columns in one statement (`0024:815-819`) and an allowlist would break the only shipped ceremony writer. **Reason deferred:** not caused by this change (the column and its grant are `0025`'s), AC6 is met exactly as written, and 6.8a authorised one migration which is already at 934 lines. **Intended home: 6.9, which canonicalizes and hashes these bytes and therefore owns the question of what the bundle must be reproducible from.** [auditor] **🟢 CLOSED by Story 6.9a, AC10.1 (migration 0029).** `assert_ceremony_transition` is `create or replace`d with a state-conditional frozen-column block raising `IC910`. ⚠ The 6.9a code review then found the predicate keyed on `old.state` ALONE, leaving one statement's worth of daylight — `update ceremony set state='locked', luck_weight_table=…` passed both the state machine and the freeze. It now reads BOTH `old.state` and `new.state`.
- [x] **Ceremony-WIDE award uniqueness is enforced by nothing — the same trophy can be persisted in two spins of one ceremony.** `[supabase/migrations/0025_ceremony_results.sql:178, supabase/migrations/0027_ceremony_run.sql]` -- `award_result_spin_award_key` is `unique (spin_id, award_id)`, i.e. PER SPIN. Nothing enforces "each catalog award is awarded at most once per CEREMONY", and nothing checks that a result's `award_id` is in its own spin's `live_award_ids`. `RunCeremony`'s `withoutAwards` prevents both producer-side, but `persist_ceremony`'s stated thesis is "THE PAYLOAD IS VALIDATED, NOT TRUSTED". **Partially closed in 6.8a:** the code review added `unknown_plan_award`, so `spin_plan[].pool` ids are now resolved against the frozen catalog like every other award id -- the field that was published verbatim no longer is. **Reason deferred:** the remaining half is cross-spin state rather than per-row shape, and 6.9 canonicalizes and hashes these bytes, so a ceremony that handed the same trophy twice would be caught in the bundle by a check that has to exist there anyway. **Intended home: 6.9.** [edge+blind, converged independently] **🟢 CLOSED by Story 6.9a, AC4.** `publish_bundle` refuses `award_revealed_twice`: the ceremony-wide cross-check `0027:138-140` homed to "6.9, which hashes these bytes and would catch it in the bundle" is now made, and it CATCHES rather than notes. ⭐ The 6.9a code review widened the surrounding validation from cardinality to IDENTITY — `awards`, `players` and `spin_plan` entries are now joined back to the database, not merely counted.
- [x] **`spin.label` and `spin.bytes_consumed` are built, shipped over the wire, and persisted nowhere.** `[worker/ceremony/ceremony.go:314-315, supabase/migrations/0027_ceremony_run.sql]` -- `worker/awards` calls the label "the single most load-bearing fact in a provably-fair ceremony" and says "6.9's verifier re-opens it by name", and `Consumed` is documented as MEASURED FROM THE STREAM; the RPC's spin insert names neither. **Closed in 6.8a to the extent 6.8a can:** Cuatro's DECISION 6 keeps them on the wire as provenance and documents the non-persistence at the write loop rather than leaving it an unexplained omission, and the code review fixed the related defect that every pity spin carried the whole pity stream's byte TOTAL (so `sum(bytes_consumed)` reported 28 x 27 = 756 for a 27-byte stream) -- the stream is now attributed to the spin that opened it, pinned by `TestBuildPayloadAttributesThePityStreamExactlyOnce`. **Reason deferred:** adding two `spin` columns now would pre-empt the shape 6.9's verifier actually needs, for data nothing yet reads. **Intended home: 6.9's `verification_bundle`.** [blind] **🟢 CLOSED by Story 6.9a, AC10.3 / DECISION F (migration 0029).** `spin` gains `label text` and `bytes_consumed bigint`, and `persist_ceremony` is `create or replace`d to write them; the body diff is pasted in the story's Completion Notes. ⚠ The 6.9a code review recorded that both columns are anon-readable through `0028`'s TABLE-WIDE grant on `spin` — intended, but it happened by default rather than by decision, and the pgTAP suite now pins that surface.

### Deferred from: code review of 6-8b-reveal-gating-and-commitment (2026-08-08)

_Baseline: the working tree over `e7a5aa0` (6.8a IS committed; 6.8b is entirely uncommitted). Three parallel layers (Blind Hunter -- diff only, no project access; Edge Case Hunter -- diff + project; Acceptance Auditor -- diff + spec + context docs, all Opus 5) over 3,270 reviewed lines across 10 files. 28 findings after dedup and verification: 3 decision-needed, 22 patch, 2 deferred, 1 dismissed. The reviewer independently re-derived the plan/test arithmetic: **the Vitest and pgTAP baselines reconcile exactly as claimed** (44 files / 1403; 29 / 1426 = 1344 + 76 + 2 + 3 + 1; retarget deltas 111->113, 116->119, 68->69, 103->103 all check out) -- the first story in this epic whose printed gate numbers survived re-measurement unchanged. AC7's three replaced bodies were mechanically re-diffed against `0015`/`0016` and contain exactly the claimed delta and nothing else. The headline items -- the two viewer policies joining on an UNQUALIFIED `spin_id`, `anon_counts` missing the exception handler its own file calls load-bearing, the `Set<string>` cast that defeats the route's "load-bearing" exhaustiveness, and Section E proving role scoping rather than policy separation -- are tracked in the story's Review Findings, NOT here. The two below are real but not actionable in this slice._

- [x] **The zero-winner feed card renders "Se revela en la ceremonia" TWICE, and it is the COMMON case rather than the pity case DECISION H records.** `[lib/feed/model.ts:188, app/(viewer)/components/TimelineFeed.tsx:107]` -- a `no_eligible_players` main spin leaves `v_subtitles` NULL, so `reveal_spin` omits the `subtitle` key by design; `toCardModel` substitutes `es.award.revealAtCeremony` AND `AwardRevealBody` unconditionally renders the same string as a lockpill beside it, so an award that was just revealed reads "Se revela en la ceremonia / [Se revela en la ceremonia]". ⭐ **What makes this sharper than DECISION H's recorded gap:** DECISION H names the *pity* branch and the fallbacks' pre-ceremony origin, but not the DOUBLING and not that 12/12 main spins over the measured corpus take this branch -- 0/28 players clear the FR-21 floors, so it is the ceremony's dominant card, not an edge. This diff's own new Vitest case asserts exactly the state that produces it. **Reason deferred:** DECISION H forbids this story from touching `app/(viewer)/**` or adding an i18n key, and AD-24 keeps every string in one module -- fixing the pill without the copy, or the copy without the pill, leaves the card wrong either way. **Intended home: 6.10, which owns the ceremony strings AND that card. DECISION H's carried-forward entry should be corrected to name the doubling and the zero-winner branch.** [edge]
- [x] **⛔ `tournament.fair_seed` is ALREADY readable by `anon` and has been since `0002`, so DECISION C's "closes the hole STRUCTURALLY" was false.** `[supabase/migrations/0002_rls.sql:50, :70]` -- `create policy tournament_read … using (true)` plus a TABLE-WIDE `grant select on public.tournament to anon, authenticated`, which covers every column including `fair_seed` (`0001:33`). No migration `0001`-`0028` revokes it; the only `revoke` statements in the whole tree are `revoke execute on function`. ⚠ **Read this precisely, because it is easy to over- or under-state.** It is NOT a secrecy leak: `fair_seed` is `= SHA-256(final demo)`, the SAME value `lock_ceremony` copies into `ceremony.seed_demo_sha256` (`0024:198`), and publishing that hash is the POINT of AC1. It IS a broken justification: 6.2 loosened `IC908` to permit `value -> NULL` on the strength of *"6.2 publishes the seed to nobody before it"*, and that premise was ALREADY false when written -- so a `rollback_match` before the ceremony locks makes the anon-visible `tournament.fair_seed` go NULL while `ceremony.seed_demo_sha256` holds. Two public columns, one of which can be un-published. **Reason deferred (Cuatro, 2026-08-08):** column-scoping or revoking `tournament`'s grant changes `0002`'s posture for a table 6.8b does not own, for no secrecy gain; 6.8b's own surface is the binding one and that is what AC1 asked for. The false claim at `0028:1211-1221` was corrected in place rather than left standing. **Intended home: 6.9, which builds the verifier and therefore owns which column a verifier reads. The fix is a column-scoped grant on `tournament`, NOT a change to 0028.** [auditor] **🟢 CLOSED by Story 6.9a, AC10.2 (migration 0029).** The tree's first `revoke … on table`: `tournament`'s table-wide grant is revoked and re-granted column-by-column over all NINE columns minus `fair_seed`. ⚠ `grace_period_seconds` (added at `0015:75-76`, AFTER `0002`'s grant) is explicitly enumerated — omitting it would have silently killed the 4.5 grace timer and the 5.7 surfaces.
- [x] **AC10's non-vacuity denominator (`27/27`) does not reconcile with the `28 roster` anchor the same section claims to have reproduced exactly.** `[_bmad-output/implementation-artifacts/6-8b-reveal-gating-and-commitment.md:549-551 vs :506-507]` -- AC10 requires the corpus-name hit count *"the way 6.8a's re-run did … 28 distinct player names each"*, and the grep table reports 27 at every `k` with no explanation of the missing name. **Mitigating:** the non-vacuity CONCLUSION is unaffected -- 27 corpus names appearing on a rendered page proves the page is live just as well as 28 would -- so nothing measured is in doubt; what is wrong is the figure's derivability, which is the entire reason AC10 demands it be printed. **Reason deferred:** the qa68b harness and `_qa68b/` are deleted, so re-measuring means a full 14-demo corpus rebuild for a number whose conclusion does not change. **Intended home: 6.11's end-to-end vector, which rebuilds the corpus anyway** -- the same home 6.7's equivalent measurement gap was given. [auditor]
- [x] **`ceremony_locked`'s `<> 'not_started'` predicate keeps `begin_match_grace` / `resume_match` / `bind_match_demo` locked out PERMANENTLY once the ceremony completes.** `[supabase/migrations/0028_reveal_gating.sql:877, :954, :1056]` -- `reveal_spin` sets `state = 'complete'` (DECISION E) and `complete <> 'not_started'`, so the guard keeps firing forever; `ceremony.state` is forward-only through `assert_ceremony_transition` and `complete` is terminal, so no RPC in the tree can clear it. Every justification written at the three sites is temporal ("while the audience is watching the ceremony") and none states the permanence. **Mitigating -- this is inherited, not introduced:** the predicate is byte-identical to `mark_walkover`'s 6.2 form (`0024:2248`) and to the same guard in `approve_match` / `rollback_match` / `manual_resolve_match`, and AC7 explicitly mandated copying that form, so the code is correct as specified. ⭐ **What is new is `bind_match_demo`**: binding a demo is a data-completeness action rather than a bracket mutation, so a demo uploaded the morning after the ceremony can never be bound -- the first time this guard blocks something that does not move the bracket. **Reason deferred:** changing the predicate would change four already-shipped RPCs' behaviour in a story whose AC mandated the opposite, and whether a completed ceremony should re-open the non-mutating commands is a product decision. **Intended home: Epic 7, or 6.9 if the verification bundle turns out to need late-bound demos. Mechanism to consider: gate on `state in ('locked','spinning')` rather than `<> 'not_started'`, applied to all four RPCs together.** [blind]

## Deferred from: code review of 6-9a-verification-bundle-and-the-published-commitment (2026-08-09)

_Baseline: `c85f1ed` -> working tree (commit `fe50626` plus the uncommitted T3-T10 work). Three parallel layers (Blind Hunter -- diff only, no project access; Edge Case Hunter -- diff + project; Acceptance Auditor -- diff + spec + context docs, all Opus 5) over 28 files / 9,760 diff lines. 46 raw findings -> 34 after dedup: 5 decision-needed, 28 patch, 1 deferred, 1 dismissed. ⚠ **This story's own AC9/AC10 obligation to CLOSE the nine debts it inherits is unmet -- `deferred-work.md` was byte-untouched by 6.9a, so `:307, 316, 335, 348, 356, 361, 362, 363, 370` still read as open; that is tracked as a patch finding in the story, and this section is not a substitute for it.** The headline items -- a published commitment surviving `persist_ceremony(p_replace => true)`, `publish_bundle` validating `awards`/`players`/`spin_plan` by cardinality rather than identity, three independent TS/Go/Python canonicalizer divergences (map cycles unguarded in Go, `ParseFloat` range errors, the document-global U+FFFD detector), the last consolation winner being derivable as `winless` minus `reveal_order`, and `plan(93)` not reconciling with its section banners -- are tracked in the story's Review Findings, NOT here. The one below is real but not actionable in this slice._

- [x] **A canonical payload containing `\u0000` is rejected by `p_payload::jsonb` and misdiagnosed as `payload_shape / 'the payload is not valid JSON'`.** `[supabase/migrations/0029_verification_bundle.sql:750]` -- all three canonicalizers emit the six-character escape `\u0000` for U+0000 (`canonical.ts` `if (code < 0x20)`, `bundle.go` `fmt.Fprintf(b, "\\u%04x", r)`, `generate_vectors.py` `f"\\u{code:04x}"`) and the shipped `string-escaping` vector case pins exactly that output. Such a payload passes the ASCII check (the escape is six ASCII bytes) and the SHA-256 re-derivation, and then `p_payload::jsonb` raises 22P05 (*"unsupported Unicode escape sequence"*); the `exception when others` handler hard-codes one detail string, so a document that is valid RFC-8785 **and** valid RFC-8259 is reported as malformed JSON. **Reason deferred:** unreachable in production -- PostgreSQL `text` cannot hold NUL, so no value read out of the database can carry U+0000 into the builder, and no producer path can construct one. It is a divergence between the vector's stated contract and the storage layer rather than a live break, and closing it means either narrowing the `when others` handler to distinguish 22P05 from 22P02 or declaring U+0000 out of the bundle's alphabet -- both of which want the browser verifier (which will canonicalize untrusted served bytes) to exist first. **Intended home: 6.9b, alongside the verifier's own refusal taxonomy.** [edge]
- [x] **AC3's completeness test -- *"the browser verifier is proven to run with EVERY non-bundle read stubbed to throw"* -- was neither executed nor owned.** `[_bmad-output/implementation-artifacts/6-9a-verification-bundle-and-the-published-commitment.md, AC3]` -- no stub-everything harness appears anywhere in the diff, and unlike every other thing this story could not reach, the clause is absent from the six-item DEFERRALS list, so it belonged to nobody. ⛔ AC3 is **not** part of the AC5-AC9 carve-out, and 6.9b's scope names AC5-AC9 / AC12(TS) / AC13(browser) without ever mentioning AC3 -- which is exactly the "spec bug" outcome `0025:8-10` describes (*"the JS verifier needs NOTHING outside the bundle; if it does, the bundle is incomplete"*). **Reason deferred (Cuatro, 2026-08-09):** untestable until the verifier exists -- the test proves the browser verifier needs nothing outside the bundle, and that verifier is 6.9b's deliverable. **Intended home: 6.9b, folded into AC5's reproduction harness (see the AC3 entry below for the same home)** -- AC5 already re-derives the whole ceremony from the bundle alone, so the completeness test is that same run with non-bundle reads stubbed to throw. ⚠ 6.9b's hand-off table must gain the row; as shipped it does not carry AC3 at all. [auditor]
- [x] **⛔ THE ZERO-CALLER GAP WIDENED, AND STORY 6.9a WAS EXPLICITLY TOLD TO RE-RECORD IT AND DID NOT.** `[worker/ceremony/ceremony.go (Run), worker/ceremony/bundle.go (BuildBundle / BuildCanonicalBundle), supabase/migrations/0029_verification_bundle.sql (publish_bundle)]` -- the story's own **answered Question 5** reads: *"`worker/ceremony.Run`'s zero callers -- stays open, Epic 7's. `publish_bundle` and the bundle builder join it: the QA harness is the only caller and it gets deleted. ⛔ No production entry point is built here (DECISION I). **Re-record it in `deferred-work.md`, do not silently inherit it.**"* `deferred-work.md` was byte-untouched by 6.9a, so the instruction went unfollowed and the widening went unrecorded -- which is the precise outcome the word "silently" was warning about. Recording it now, as the 6.9a code review (2026-08-09). **The state of the world:** the tree holds a published, hashed, reveal-gated verification bundle and **three** production-callerless entry points that build and publish it; the only thing that ever called them was a throwaway QA harness that is deleted by convention. **Reason deferred:** unchanged from Question 5 -- 6.9a deliberately builds no production entry point (DECISION I), and 6.9b's browser reads the bundle but does not produce one either. **Intended home: Epic 7**, alongside `Run`'s original entry-point gap; they are one piece of work, not three. [auditor]