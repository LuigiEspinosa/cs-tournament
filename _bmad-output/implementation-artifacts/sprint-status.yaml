# generated: 2026-06-30
# last_updated: 2026-07-01 (1-3 → done via code-review: 3 test-hardening patches applied, 0 code defects; supabase test db now 78/78 [0001 30 + 0002 48])
# project: cs-tournament
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#   - deferred: Story is blocked on a later epic's prerequisite and has been re-sequenced; NOT auto-picked as "next" (see the story's inline note / deferred-work.md)
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-06-30
last_updated: 2026-07-06 (Story 3.1 CODE-REVIEW DONE -> review→in-progress via bmad-code-review [3 adversarial Opus-4.8 layers — Blind Hunter / Edge Case Hunter / Acceptance Auditor; none failed]. ALL 7 ACs PASS (Acceptance Auditor: DDL exact vs SOLUTION-DESIGN §3, plan(38)=38, module boundary holds, no scope creep, demoinfocs NOT imported; RLS/FORCE/grants [admin-only dormant SELECT, select/insert/update NO-delete, no anon/authenticated grant] independently re-verified). Severity calibrated to the private-tournament threat model — every ingest endpoint is shared-secret or requireAdmin gated, no public surface. Triage: 1 decision-needed→RESOLVED (1 patch applied) + 4 defers + 10 dismissed. ⭐ The lone decision (Cuatro→Option 1 apply-now): ingest size/decompression-bomb caps — the MatchZy receiver streamed the (gzip/zip/raw) body to R2 with NO http.MaxBytesReader + no decompressed ceiling + an uncapped zip-spool io.Copy. FIX: http.MaxBytesReader(w,r.Body,maxDemoBytes) on handleMatchZy + decompressToDem wraps its output in newLimitedReadCloser(300 MiB) — an ERRORING ceiling on the DECOMPRESSED stream (io.LimitReader would silently truncate + store a truncated demo; this FAILS→aborts a bomb) + regression test TestLimitedReadCloserErrorsPastCap. GREEN: gofmt/go build/go vet clean, go test ./... 28→29 PASS. 4 defers→deferred-work.md: [W1] /ingest/presign shares WORKER_MATCHZY_SHARED_SECRET across two trust boundaries (MatchZy server-to-server vs admin browser)→Epic 5 admin-upload UI (app-side requireAdmin proxy or a separate presign secret); [W2] /api/ingest/register trusts an unverified client-supplied storage_key (orphan/mismatch; browser-PUT-then-never-register orphan)→Epic 5 (needs an R2 HEAD [app has no R2 creds by design] or key↔presign binding); [W3] shared secret carried in ?token= query is logged→Epic 7 deploy hardening (prefer the Authorization header carrier, rotate); [W4] MatchZy gzip/zip paths don't re-validate the decompressed PBDEMS2 magic the CLI enforces→optional (3.2 hashing/3.3 parsing catch non-demos). 10 dismissed (documented-intentional/fail-closed/downstream-handled: store-before-record orphan+retry-dup [3.2 reconciles], size_bytes 0→NULL [nullable by design], Authorization-vs-?token= precedence [fails CLOSED], NewStorageKey 128-bit collision [UNIQUE deferred to 3.2], Delete() guard unwired [GC is Epic 7], MatchZy-FileName/MapNumber ignored [no column], Put ignores advisory size [intentional], et al.). Impl + this review patch STILL UNCOMMITTED (worktree). ⛔ Task 9 live-QA sign-off (Cuatro — real R2_* creds + a real Source-2 .dem + a seeded season+tournament row; the agent cannot run it) STILL PENDING → then done; mirrors the 2.1–2.5 rhythm. Epic 3 (CAP-3) 0/8 done, 3-1 in-progress. PREVIOUSLY: Story 3.1 dev-story DONE -> review via bmad-dev-story [baseline 1915c62]: built the repo's FIRST Go code -- top-level worker/ module (go 1.26.4; aws-sdk-go-v2 v1.42.1 + jackc/pgx/v5 v5.10.0; NO demoinfocs yet) -- and migration 0005_demo.sql. Acquiring node ONLY (store the canonical .dem in R2 + record a partial demo row; demo_sha256/parser_version NULL shells); NO hash/dedup (3.2), NO parse/stat_row (3.3), NO async queue (3.8). Tasks 1-8 complete + green; Task 9 (live-QA sign-off) is a PENDING human gate (needs real R2 creds + a real .dem + seeded season/tournament; live Railway/MatchZy deferred to Epic 7). All 3 resolved decisions built: 0005 with nullable shells / all 3 paths built+locally-verifiable+live-deferred / DemoStore R2+fake only. AC1 MatchZy receiver POST /ingest/matchzy (constant-time shared secret via Authorization-or-?token=, framing auto-detect gzip/zip-spooled-to-tempfile/raw PBDEMS2, stream to R2, source=matchzy; live CS2 E2E deferred). AC2 admin path: worker POST /ingest/presign mints a single presigned PUT (worker holds R2 creds, app does not) + app/api/ingest/register/route.ts (requireAdmin-gated thin route mirroring admin/roster) -> lib/ingest.ts records source=manual_upload. AC3 CLI worker ingest <path> --match <id> (PBDEMS2 guard). AC4 DemoStore interface + R2 impl + map-backed fake (AD-1 round-trip proven). AC5 bypass invariant (50-170MB never hits a Next route; no NEXT_PUBLIC secret; lib/env.ts UNTOUCHED, no R2 getters). AC6 0005_demo.sql per SOLUTION-DESIGN §3 (shells) + ENABLE/FORCE RLS + dormant demo_admin_read (mirrors audit_log) + grant select/insert/update service_role NO-delete + pgTAP plan(38); NO UNIQUE(match_id,sha256), NO match FK. AC7 single-writer service-role (pgTAP fail-closed matrix) + worker<->app NO code-import boundary + server-only secrets. AD-2 nuance flagged for review: lib/ingest.recordManualUpload is the one app-side demo write (acquisition metadata only, never stat_row). Retention guard AD-16: Go-side Delete() refuses permanent_seed unless force (object-lock hardening = Epic 7). GREEN: Go 28 tests (5 pkgs) + go vet + gofmt clean; supabase test db Files=6 Tests=209 (0005 plan(38)); Vitest 104->110 (+6 lib/ingest.test.ts); next build clean (/api/ingest/register = ƒ); eslint clean; 0 NEXT_PUBLIC leaks. Impl NOT yet committed. Next: commit -> code-review (different LLM -- focus 0005 RLS/grants/shells + pgTAP, DemoStore round-trip + shared-secret constant-time auth + zip/gzip framing, register requireAdmin + the AD-2 nuance, bypass-Vercel invariant, worker<->app no-code-edge, DB DSN derivation) -> Cuatro clears live-QA -> done; then Story 3.2 (hash/dedup/write-once). Epic 3 now 0/8 done, 3-1 review. PREVIOUSLY: Story 3.1 (demo acquisition that bypasses Vercel) HYDRATED -> ready-for-dev via bmad-create-story [baseline 1915c62, HEAD/clean; 3 research subagents -- architecture-spine/solution-design, PRD/tech-research/PoC, repo-state/UX/migrations -- + a live MatchZy-docs fetch]. Epic 3 (CAP-3) STARTED: epic-3 backlog->in-progress (3-1 is its first story). THE BIG PIVOT -- the repo's FIRST Go code (top-level worker/ module, go 1.26.x), FIRST Cloudflare R2 integration, FIRST multi-language layout; NONE of Epic 2's TS/auth/RLS carries into the worker. Scope = the ingest state machine's `Acquiring` node ONLY (get 50-170MB demo bytes durable in R2 bypassing the ~4.5MB Vercel cap), stopping BEFORE hashing/dedup (3.2), parse->stat_row (3.3), and the async job queue (3.8). 3 CENTRAL DECISIONS resolved by Cuatro at creation: [1] CREATE migration 0005_demo NOW with nullable shells -- demo_sha256 + parser_version nullable; UNIQUE(match_id,demo_sha256)+sha256 NOT NULL DEFERRED to Story 3.2; parser_version NOT NULL to Story 3.3; demo_match_fk to Epic 4 (the `match` table does not exist yet -> match_id is a plain bigint not null, no forward FK, mirroring tournament.final_match_id); the register/CLI/MatchZy paths insert a PARTIAL acquisition row. [2] BUILD all 3 ingest paths (MatchZy shared-secret HTTP receiver / admin R2-presigned upload + a thin requireAdmin /api/ingest/register Next route / local Go CLI), verify LOCALLY vs the real R2 bucket, DEFER live Railway deploy + a rented MatchZy CS2 server to an operational gate (Epic 7) -- mirrors 2.1's prod-domain defer. [3] DemoStore interface + R2 impl + in-memory fake (for `go test`) ONLY; DEFER the Supabase-Storage backend (YAGNI; R2 primary). Env ALREADY stubbed in .env.example (R2_ACCOUNT_ID/ACCESS_KEY_ID/SECRET_ACCESS_KEY/BUCKET/S3_ENDPOINT + WORKER_MATCHZY_SHARED_SECRET + SUPABASE_DB_PASSWORD 'direct worker DB access') -> NO net-new env vars; worker-side only (no lib/env.ts getters). demo is admin/worker-only (mirror audit_log 0003: admin-only dormant read policy + service_role select/insert/UPDATE [update for the 3.2/3.3 backfills + re-parse gen bump] NO delete; FORCE RLS auto-covered by the 0003 catalog guard). AD-2 single-writer NUANCE flagged for code-review: the manual-path register route is the one spot the app writes a `demo` row (service-role, admin-gated, acquisition metadata only -- never stat_row). MatchZy wire contract CONFIRMED from live docs (shobhit-pathak.github.io/MatchZy/gotv): HTTP POST, body = zipped .dem (worker must decompress+stream to the canonical .dem for AD-1 re-hash), metadata headers MatchZy-MatchId/FileName/MapNumber, cvar matchzy_demo_upload_url -- NO auth-header cvar in the snapshot, so pin how the shared secret is carried against the deployed MatchZy build. Traces FR-11 . AD-16, AD-25 . SPEC Constraint 6. NO new deps beyond aws-sdk-go-v2 + pgx/v5 (demoinfocs NOT added until 3.3). Live-QA prereq: seeded season+tournament + a real Source-2 (.dem, PBDEMS2) file. Next: bmad-dev-story -> code-review (different LLM: focus 0005 RLS/grants/shells + pgTAP plan(N), DemoStore round-trip + shared-secret constant-time auth, register requireAdmin + the AD-2 nuance, the bypass-Vercel invariant, worker<->app no-code-edge boundary) -> Cuatro clears live-QA -> done; then Story 3.2 (hash/dedup/write-once). Epic 3 (CAP-3) now 0/8 done, 3-1 ready-for-dev. PREVIOUS: Epic 2 RETROSPECTIVE DONE via bmad-retrospective [party-mode facilitation; reviewed 2.1-2.5, all done; 2.6 deferred to Epic 3]. Verdict: Epic 2 identity/roster substrate COMPLETE for all buildable scope; all 5 Epic-1 retro action items applied/maintained [accountability loop closed]; NO significant discovery -> Epic 3 plan STANDS. Themes: adversarial review caught a CRITICAL 2.1 Steam identity-spoof [HTTP param pollution: body.set last-wins vs params.get first-wins] PRE-SHIP; live-QA was the irreducible gate for version-sensitive Supabase Auth [why Epic 2 felt 'a little harder' than pure-SQL Epic 1]; 'one named decision per story resolved before dev' paid off [2.5's 5 decisions -> 0 review patches]; 4 architecture invariants realized in code [AD-12 instant-revoke Option-A / AD-7 two-policy RLS first-realized in roster_entry / AD-4 canonical-key pgTAP catalog guard / AD-8 requireAdmin gate primitive]. 4 NEW action items [epic:2]: (1) fold Story 2.6 into Story 3.4 hydration; (2) close the roster-lock TOCTOU in the Epic-4 bracket slice [unexploitable until a concurrent bracket_live writer exists]; (3) Epic-3 infra prep [Go-worker host + R2 + MatchZy] -> decide at Epic 3 planning [Cuatro]; (4) seed a standing season+tournament fixture. Epic-1 action items #2 [ES256/JWKS], #3 [0004 grants], #5 [plan(N)] -> done. Cuatro sign-off: Go-worker pivot = comfortable stretch [poc-cs2-demo-parse de-risks parsing]; prod deploy stays Epic-7-deferred; infra decided at Epic 3. Retro doc: epic-2-retro-2026-07-06.md. epic-2-retrospective optional -> done. PREVIOUS: Story 2.6 create-story -> DEFERRED & re-sequenced to Epic 3 [build AFTER Story 3.4] by Cuatro: exhaustive create-story analysis [3 research subagents -- architecture / PRD-UX / codebase] established the unreconciled-SteamID64 list is an AD-4 derived LEFT-JOIN [ARCHITECTURE-SPINE:98], not a stored table, over `stat_row` -- which is NOT created until Story 3.3 and only populated/flagged by Story 3.4 [Epic 3, all backlog; stat_row is the Go worker's single-writer table per AD-2]. No data source exists in the current schema; a SQL view over a missing relation is impossible; reconciliation is mechanical [add a SteamID64 to the roster -> orphaned stats auto-attach via the deliberately-FK-less join key, largely already delivered by 2.5's adminAddPlayer]. Story 3.4 already names 2.6 by number as its destination [epics:535]. Set `2-6` status -> `deferred` [documented in STATUS DEFINITIONS] so it is NOT auto-picked next -> next actionable = Epic 3 Story `3-1`. NO story file written, NO migration, NO code change. epic-2 stays in-progress [5/6 buildable stories done: 2.1-2.5; 2.6 deferred to Epic 3] -- the Epic 2 retrospective [epic-2-retrospective: optional] can run now since all buildable Epic-2 work is complete. Full rationale + the rider [2.3's placeholder-display_name deferral, whose home was 2.6] in deferred-work.md. PREVIOUS: Story 2.5 DONE — live-QA SIGN-OFF CLEARED by Cuatro: all 7 ACs verified end-to-end (enroll→roster_entry active; close→self-enroll 409s while admin-add still works; remove→status=removed, gone from anon read + audit_log rows present; non-admin 403; a player cannot enroll a different SteamID64; forced state=bracket_live→enroll/add/remove all 409 locked). Impl 3a58121 + code-review artifacts 0dc778a on remote main. Status in-progress→done. Epic 2 (CAP-2) now 5/6 done (2.1/2.2/2.3/2.4/2.5); next = Story 2.6 (unreconciled SteamID64 list, roster-side — the parsed-ID-absent-from-roster check 2.5's roster is the reference set for). PREVIOUS: Story 2.5 CODE-REVIEW DONE via bmad-code-review [3 adversarial Opus-4.8 layers — Blind Hunter / Edge Case Hunter / Acceptance Auditor, none failed; diff a0dc435..3a58121, 9 code files +1237] → status review→in-progress. AC map: AC1/AC2/AC4/AC5/AC6 VERIFIED, AC3/AC7 PARTIAL (roster add/remove + self-enroll lock is a read-then-write state gate, not the WHERE-clause DB-write guard AC7 literally names — deferred W1). Triage: 2 decisions + 0 patches + 2 defers + 8 dismissed. ⭐ Both decisions Cuatro-KEPT (→ dismissed, NO code change): D1 self-enroll reactivates an admin-removed row while registration_open → KEEP open-join model (prd.md:146 no-approval; admin finalizes by closing registration); D2 writeAudit throws → 500 after the committed primary write → KEEP as-is (consistent with 2.4 healRoleMirror→500 precedent: fail-closed/don't-swallow/idempotent-retry). Verification KILLED the sole raised High (Blind: writeAudit audit_log columns 'unproven/drift') as a FALSE PREMISE — the four inserted columns tournament_id/actor_steamid64/action/detail match 0003_audit_snapshot.sql:21-29 exactly; also dismissed a false 'STATUS_FOR fail-open 200' (TS errors at the index site on a new reason, not a runtime 200) + multi-open-wedge/actor-FK-500/leading-zero-id/tournament_id≤0/is_admin-gating (all fail-closed-handled). 2 defers → deferred-work.md: W1 the read-then-write lock guard on the roster writes (extend to enrollSelf) → Epic-4 bracket slice, unexploitable until a concurrent bracket_live writer exists; W2 pgTAP has no removed→active reactivation round-trip (covered across unit+grant layers) → optional test-hardening. Code UNCHANGED + green (Vitest 104/104, next build clean, supabase test db 171 PASS, plan(38) exact). ⛔ Task 9 live-QA SIGN-OFF still PENDING (human gate — mirrors 2.1/2.2/2.3/2.4 which each went code-review→in-progress→Cuatro clears live-QA→done). Next: Cuatro runs Task 9 live-QA (seed a season+tournament row; enroll/close/add/remove/lock-at-bracket_live end-to-end) → done; then hydrate 2.6 (unreconciled list). Epic 2 (CAP-2) 4/6 done (2.1/2.2/2.3/2.4), 2.5 in-progress (code-review clean, live-QA pending). PREVIOUS: Story 2.5 dev-story DONE → review via bmad-dev-story [baseline a0dc435; impl NOT yet committed] — NEW migration 0004_roster.sql (roster_entry + FORCE RLS + the AD-7 two-policy read realized-for-the-first-time + explicit grants NO-delete = soft-delete ceiling; first migration since Epic 1) + pgTAP 0004_roster_test.sql plan(38) exact + lib/auth/user-guard.ts (requireUser standalone/regression-safe, fail-closed 401) + lib/roster.ts (setRegistrationOpen/resolveOpenTournament/enrollSelf/adminAddPlayer/removePlayer + private writeAudit) + 3 routes (roster/enroll no-body requireUser self-enroll; admin/registration + admin/roster requireAdmin) + Vitest user-guard(+5)/roster(+21). GREEN — Vitest 78→104, next build clean (3 routes ƒ, 0 NEXT_PUBLIC_), supabase db reset 0001→0004 clean, supabase test db Files=5 Tests=171 PASS (0004 plan(38); 0001/0002/0003/canonical unaffected — the 0003 generic FORCE-guard now also covers roster_entry). Decisions built as resolved — audit-in-2.5 D4, soft-delete D2, per-action state-gate lock D3, backend-first UI-deferred D5, standalone requireUser D1b. Scope held (no bracket/bracket_seed, no UI, no unreconciled 2.6, no per-season, no tournament-create route); env.ts/session.ts/roles.ts/admin-guard.ts/callback interfaces untouched. NO seed.sql shipped (would collide with 0001-0003 pgTAP 'Season 1' fixtures) — season+tournament seed is an operational one-time step, seed SQL provided in Task 9. ⛔ Task 9 live-QA SIGN-OFF pending (human — real Steam logins + a seeded tournament row; mirrors 2.1/2.2/2.3/2.4). Next — code-review (different LLM: focus 0004 RLS/grant/two-policy + pgTAP, requireUser fail-closed, state-gate lock + soft-delete, self-enroll can't-target-others, audit rows) → Cuatro clears live-QA → done; then 2.6. Epic 2 (CAP-2) 4/6 done (2.1/2.2/2.3/2.4), 2.5 review. PREVIOUS — Story 2.5 HYDRATED → ready-for-dev via bmad-create-story [baseline a0dc435, HEAD/clean; 3 research subagents: architecture, PRD, UX]. Scope = the roster-membership + registration-window slice; the FIRST migration since Epic 1. ⭐ tournament.state closed-set enum ALREADY EXISTS [0001:29-30] — AC1 open/close is a service-role state UPDATE, NOT a migration; the new migration slice is roster_entry ONLY [0004_roster.sql per SOLUTION-DESIGN.md:83-91: surrogate id PK; tournament_id FK→tournament ON DELETE CASCADE; steamid64 FK→player ON DELETE RESTRICT; bracket_seed nullable SHELL (Epic-4 populates, like 0001's final_match_id/fair_seed); status ('active','removed') soft-delete; registered_at; UNIQUE(tournament_id, steamid64)] + FORCE RLS + the AD-7 TWO-POLICY read roster_entry is the FIRST table to REALIZE (0002:87-98 forecast it: viewer USING(status='active') + admin USING(is_admin()), never OR'd) + explicit grants (always-revoked default, retro action #3: anon/authenticated SELECT; service_role SELECT/INSERT/UPDATE, NO DELETE = soft-delete ceiling). NEW pgTAP 0004_roster_test.sql with explicit plan(N) [retro #5]; note the generic catalog FORCE-guard in 0003's test (54-60) now covers roster_entry so FORCE is non-negotiable. Enrollment model RESOLVED by PRD (prd.md:149-151) = HYBRID: players self-enroll while registration_open [NEW app/api/roster/enroll + NEW requireUser gate in lib/auth/user-guard.ts — the non-admin sibling of 2.4's requireAdmin, verified getUser→own steamid64, a player can only enroll THEMSELVES not a body id]; admin adds-while-closed + removes-pre-bracket [NEW app/api/admin/roster + app/api/admin/registration, reuse requireAdmin verbatim]; ALL writes service-role via server routes (no authenticated write policy — SPINE:295). Lock = tournament.state gate (SPINE:235) reconciled: self-enroll→registration_open only; admin add/remove→state IN (open,closed) i.e. before bracket_live; all locked at bracket_live+ (enforced by route check AND the write's WHERE state-guard = no silent no-op). NEW lib/roster.ts (setRegistrationOpen/resolveOpenTournament/enrollSelf/adminAddPlayer/removePlayer/writeAudit, injectable, typed refusals, upsert onConflict:'tournament_id,steamid64' reactivate). 5 ⭐ CENTRAL DECISIONS all RESOLVED by Cuatro at end of creation (each took the RECOMMENDED option; tasks already reflect them): [1] enrollment model = hybrid self-enroll+admin-manage (+1b requireUser standalone vs DRY-refactor requireAdmin — recommend standalone, regression-safe); [2] removal = SOFT-delete via status='removed' (follow SD DDL) vs hard DELETE — recommend soft; [3] lock boundary = per-action state gates — recommend; [4] audit_log write in THIS story (tournament-scoped → NO 2.4 grant_role blocker; audit_log+grant exist 0003, action is uncapped text) vs defer to 4.9 — recommend audit-now (FR-33); [5] UI scope = BACKEND-FIRST, defer the designed-but-unbuilt Admin Roster console + viewer 'Inscripciones abiertas' state (EXPERIENCE.md:43,110) to Epic 5 / a UI slice — recommend defer (no app shell yet; matches 2.1-2.4 rhythm; UX docs DO specify it unlike 2.4, so it's an ACKNOWLEDGED deferral not 'docs silent'). NO new env/deps; NO bracket generation or bracket_seed writes (Epic 4); NO unreconciled list (2.6); NO per-season scoping (AD-18); NO tournament/season CREATE route (seeded prerequisite — state defaults registration_open). Traces FR-3 · AD-18. Prereq for live-QA: seed a season+tournament row. Next: dev-story (build for the confirmed resolutions) → code-review (different LLM — focus the 0004 RLS/grant/two-policy + pgTAP, requireUser fail-closed, the state-gate lock + soft-delete, self-enroll can't-target-others, audit rows) → live-QA sign-off → done; then 2.6 (unreconciled list). Epic 2 (CAP-2) now 4/6 done (2.1/2.2/2.3/2.4), 2.5 ready-for-dev. PREVIOUS: Story 2.4 DONE — live-QA SIGN-OFF CLEARED by Cuatro: Task 6 verified end-to-end on real Steam logins + live Supabase — 2nd player promoted [app_role→admin, granted_by=Cuatro] → is_admin() true after refresh; revoked → INSTANT 403 on their next admin call pre-refresh [Option-A re-read] → post-refresh JWT role:viewer → is_admin() false [AC2]; re-login did NOT re-promote [durable]; fail-closed edges live [non-admin 403 / self-target rejected / never-logged-in refused]. All 7 ACs met. Impl 30ddfbd + review patches ad96d4d on remote main. Retro action item #4 [granted_by admin-only / no-self-grant in the 2.4 route] → done. Epic 2 (CAP-2) now 4/6 done (2.1/2.2/2.3/2.4); next = 2.5 (registration window + roster_entry, migration 0004, retro action #3). PREVIOUS: Story 2.4 CODE-REVIEW DONE via bmad-code-review [3 adversarial Opus-4.8 layers — blind/edge/auditor, none failed]. Triage: 1 decision + 2 patch + 1 defer + 18 dismissed. Auditor AC map: AC1/AC3/AC4/AC5/AC6/AC7 VERIFIED, AC2 PARTIAL (literal "forced refresh" has no active signOut — reconciled by the Option-A Central Decision; enforcement is instant-by-re-read + mirror-heal on refresh; intent met). Verification KILLED the 3 loudest flags as false premises: (a) UNIQUE_VIOLATION "dead constant" → USED at roles.ts:84 (resolveRole race re-read); (b) granted_by FK→no_such_player ambiguity → NOT reachable (app_role.steamid64 is itself FK→player 0001:46, so any admin who passed requireAdmin already has a player row); (c) CSRF Critical → not exploitable under default SameSite=Lax (blocks cross-site fetch POST) + the JSON-body requirement (HTML forms can't send application/json). Decision RESOLVED (Cuatro → Option 1, keep-as-is/dismissed): healRoleMirror infra-fail→500 AFTER the durable app_role write is working-as-intended — Blind's "stale-admin-bypass" Critical was a FALSE premise (Option-A requireAdmin re-read blocks a revoked admin instantly regardless of the JWT mirror); residual is response-semantics only; fail-closed + idempotent + documented. 2 patches APPLIED + verified (78/78 Vitest [+1 listUsers-error test, was 77], next build clean, /api/admin/roles=ƒ): P1 wrapped the FULL POST handler in the controlled try/catch (client-build + requireAdmin gate now INSIDE → a transport throw yields {error:internal_error} JSON, not Next's default HTML 500; inner request.json() 400 preserved); P2 added the listUsers-error fail-closed test (symmetry vs the existing updateUserById-error test; retro "explicit assertion-accounting" action). 1 defer → deferred-work.md (findAuthUserIdByEmail silent no-op past the 10k-user listUsers pagination cap — orders beyond this private event's scale). Review patches UNCOMMITTED (working tree). Status review → in-progress: code-review is CLEAN, but ⛔ Task 6 live-QA SIGN-OFF still PENDING (human gate — needs a 2nd real Steam player + live admin Supabase; mirrors 2.1/2.2/2.3 which each went code-review → in-progress → Cuatro clears live-QA → done). NEXT: commit the review patches, Cuatro clears live-QA → done, then hydrate 2.5. PREVIOUS: Story 2.4 dev-story DONE → review via bmad-dev-story [baseline 0cd8164]: built OPTION A (requireAdmin re-reads the authoritative app_role per request = INSTANT revoke). NEW lib/auth/admin-guard.ts (requireAdmin: verified getUser → re-read app_role → admit only role='admin' NOW; fail-closed 401 [no session/getUser error] / 403 [bad-or-missing claim, viewer, no row, hard read error — logged]; typed denial, never throws; server-only; both clients injected). NEW app/api/admin/roles/route.ts (FIRST file under AD-8 app/api/admin/ tree; runtime=nodejs + dynamic=force-dynamic; thin: requireAdmin gate [non-admin→403 no write] → body validation [400 invalid_json / invalid_body] → setRole reason→status map [self_target 409 / bad_target 400 / no_such_player 404 / write_failed 500] → healRoleMirror on success → {ok:true}; try/catch → controlled 500 internal_error, never a raw 500). EXTENDED lib/auth/roles.ts: setRole (pre-write guards BEFORE any from() — non-17-digit target→bad_target, actingAdmin===target→self_target [no-self-grant: blocks self-demote lockout/self-promote loop + guarantees granted_by is a DIFFERENT admin]; upsert onConflict:'steamid64' stamping granted_by=actingAdmin + explicit granted_at [on-conflict UPDATE won't re-apply the column default, so explicit keeps granted_by/granted_at a coherent pair]; NEVER deletes on revoke = durable viewer row keeps resolveRole app_role-first from re-promoting on re-login/allowlist; 23503 FK→no_such_player [target never logged in — app_role.steamid64→player(steamid64) verified 0001:46], else write_failed) + healRoleMirror (re-mint app_metadata via updateUserById BOTH keys [Admin API replaces not merges — 2.2 D1 gotcha]; resolve auth id by paging listUsers [perPage 200, MAX_PAGES 50] matching steamEmail(target) since NO getUserByEmail exists; DELIBERATE no-op when target has no auth user yet [resolveRole binds on next login]; THROWS on a real listUsers/updateUserById error — an error is not 'not found', fails closed per 2.2 P1/P2 don't-swallow). Vitest 59→77 (+8 admin-guard [allow + 7 fail-closed incl. the ⭐Option-A instant-revoke proof: JWT claim said admin but app_role now viewer → 403], +10 roles [6 setRole + 4 healRoleMirror]); next build clean + TS pass + /api/admin/roles renders ƒ (Node,dynamic); NO migration (supabase/ untouched — git status -- supabase/ empty), NO new table, NO audit_log write (DEFERRED to Story 4.9 — audit_log.tournament_id NOT NULL vs event-global role per AD-18; granted_by/granted_at is the interim durable record; documented in a route comment only); 0 NEXT_PUBLIC_ additions; env.ts/session.ts/login-flow.ts/players.ts/callback public interfaces untouched; scope HELD (no Epic-4 command routes, no admin UI, no roster_entry, no per-season logic). Central decision Option A built as RESOLVED (Option B — trust JWT claim, accept ≤TTL window — declined). Retro action item #4 (granted_by admin-only/no-self-grant in the 2.4 route) now IMPLEMENTED — stays open until 2.4 done. ⛔ Task 6 live-QA SIGN-OFF GATE PENDING (human, Cuatro; dev-story cannot run it — needs a SECOND real Steam player + live admin Supabase): promote a second player→they refresh→is_admin() true; revoke→instant 403 on their NEXT admin call even pre-refresh→after refresh JWT carries role:viewer→is_admin() false (AC2); re-login does NOT re-promote (durable even if their id is in ADMIN_STEAMIDS); fail-closed edges live (non-admin 403 no write / self-target rejected / never-logged-in target refused cleanly). Impl NOT yet committed. NEXT: commit 2.4 impl → code-review (different LLM — focus the requireAdmin fail-closed matrix + Option-A instant-revoke re-read, setRole granted_by/no-self-grant + 23503 mapping, healRoleMirror don't-swallow + both-keys mirror, route status mapping + try/catch), Cuatro clears live-QA → done; then hydrate 2.5 (registration window + roster_entry, migration 0004). PREVIOUS (2.4 hydration) [baseline 0cd8164, HEAD/clean; 3 research subagents: epics/PRD, architecture, UX]. Scope = the ENFORCEMENT + REVOKE half of the role system (2.2 BOUND it; session.ts header defers enforcement/revoke here). Primary ACs verbatim (epics.md#Story 2.4 L381-398): AC1 server-enforced admin gating (FR-4, not merely hidden), AC2 revoke-invalidates-session (AD-12: app_role updated AND session invalidated/forced-refresh AND a stale JWT can't pass is_admin() after refresh). Derived ACs (authoring elaboration — granted_by/no-self-grant is NOT verbatim epic text; grounded in AD-8/AD-12/AD-17 + Story 4.9's enumerated 'grant role' route): AC3 NEW lib/auth/admin-guard.ts requireAdmin primitive (verified getUser() → authorize, fail-closed 401/403, the reusable gate Epic-4 command routes consume per AD-8); AC4 NEW app/api/admin/roles/route.ts (first file under AD-8 api/admin/ tree; requireAdmin-gated POST, service-role app_role write, Node runtime); AC5 granted_by=acting-admin + no-self-grant enforced IN THE ROUTE (migration 0003 header EXPLICITLY forbids a DB CHECK/trigger here — RLS can't gate a BYPASSRLS writer); AC6 durable app_role UPDATE (never delete → resolveRole app_role-first keeps revoke durable vs re-login/allowlist re-promote) + updateUserById mirror re-mint (BOTH app_metadata keys, the 2.2 replace-not-merge gotcha) so post-refresh JWT flips is_admin(); AC7 NO migration / NO new table (app_role+is_admin+grants+audit_log all exist 0001/0002/0003), audit_log grant_role row DEFERRED to Story 4.9 (audit_log.tournament_id NOT NULL vs event-global role per AD-18 — granted_by/granted_at is the interim record), Vitest green. ⭐ CENTRAL DECISION RESOLVED → OPTION A (Cuatro, 2026-07-05): requireAdmin re-reads the authoritative app_role per request → INSTANT revoke (closes the stale-token window, cheap PK SELECT); OPTION B (trust JWT claim, accept ≤access-token-TTL ~1h window per AC2's literal 'after the refresh') DECLINED. Both keep updateUserById mirror-heal; A ADDS the app_role re-read. Tasks/ACs already written for A — no rework. Verified installed @supabase/auth-js (supabase-js 2.110.0): signOut(jwt) needs the live JWT (NOT usable for an admin-initiated by-id revoke) + NO getUserByEmail (heal via listUsers→email match→updateUserById) — this is WHY A is the robust instant path. DB-level app_role write-deny + is_admin() fail-closed matrix ALREADY proven in 0002_rls_test.sql → new pgTAP OPTIONAL. NO migration (app-code only: admin-guard.ts new, roles.ts +setRole/+healRoleMirror, api/admin/roles/route.ts new, admin-guard.test.ts new, roles.test.ts extended). Retro action item #4 (granted_by admin-only/no-self-grant in the 2.4 route) now hydrated into the story (item stays open until 2.4 done). NO UX surface (UX docs define server-gate + hidden-not-disabled UX-DR4 but NO role-management/403 page — backend-only; route returns JSON). Next: dev-story (build for Option A) → code-review (different LLM) → live-QA sign-off gate (mirrors 2.1/2.2/2.3). PREVIOUS: Story 2.3 DONE — ✅ Task 5 live-QA SIGN-OFF CLEARED (Cuatro): local real Steam login → renamed persona → re-login showed NEW display_name with SAME steamid64 PK + created_at + app_role still attached; AC6 transient-outage preserve also confirmed (blanked STEAM_API_KEY → good name not clobbered). All 7 ACs met end-to-end. Impl + review patches committed c830e8a + pushed to remote main (85b293a..c830e8a). Status in-progress → done. Epic 2 (CAP-2) now 3/6 stories done (2.1, 2.2, 2.3); next = 2.4 (roles admin gating + revoke-invalidates-session + granted_by invariant, retro action #4). PREVIOUS: Story 2.3 CODE-REVIEW DONE → status back to in-progress via bmad-code-review [3 adversarial Opus-4.8 layers — blind/edge/auditor, all converged on the AC6 partial-profile gap]. Triage: 1 decision + 2 patch + 3 defer + 6 dismissed. Auditor AC map: 6/7 VERIFIED, AC6 was PARTIAL. Decision RESOLVED (Cuatro) → Option A: fetchSteamProfile now returns null on a blank/absent persona (was an id-fallback that took the UPDATE path and clobbered a stored good name) → routes to ON CONFLICT DO NOTHING preserve; avatar-only partial-200 case deferred to Epic 5. Patches applied+verified: P0 (Option A + softened doc comments + revised Vitest case blank-persona→null); P1 (NEW DB-level AC6 behavioral proof — good row → placeholder upsert on-conflict-do-nothing → display_name/avatar_url unchanged, + brand-new placeholder still inserts); P2 (AC4 catalog guard extended to EXCLUDE contype='x'; expression/partial unique-index blind spot noted as Epic-5 residual). GREEN: Vitest 58→59, supabase test db 130→133 [plan(10)→plan(13)], next build clean, /auth/steam/* stay ƒ, no migration. 4 defers → deferred-work.md. ⛔ Task 5 live-QA SIGN-OFF GATE still PENDING (human: real Steam login → rename persona → re-login shows new name, same PK/created_at, app_role attached; mirrors 2.1/2.2 done-after-QA). Impl + review patches still uncommitted. PREVIOUS: Story 2.3 dev-story DONE → review via bmad-dev-story [baseline 3006dba]. AC6 placeholder-clobber guard shipped: fetchSteamProfile → SteamProfile|null (explicit unhydrated signal on no-key/non-200/throw/empty-players), upsertPlayer gains an ignoreDuplicates branch = ON CONFLICT DO NOTHING so a transient Steam outage never clobbers a stored good display_name/avatar with the 17-digit placeholder (brand-new player still gets a placeholder row); added injectable HttpGetJson seam (mirrors 2.1's HttpPost) for unit-testability. Proof-only otherwise, NO migration: NEW pgTAP canonical_steamid64_invariant_test.sql plan(10) [AC3 rename tolerance — sentinel created_at + FK'd app_role + non-FK stat_snapshot_row all survive a display_name UPDATE, exactly one row, PK/created_at unchanged; AC4 catalog guard — player PK exactly {steamid64} + no PK/UNIQUE/FK constraint or unique index in public keys on display_name, bites for future roster_entry(2.5)/stat_row(E3)] + NEW lib/players.test.ts (15 tests: onConflict:steamid64 upsert, rename routes to same key, 17-digit guard pre-DB, AC6 ignoreDuplicates, fetchSteamProfile success/url/null-fallbacks). Vitest 43→58, pgTAP 120→130 (Files=4 PASS), next build clean + /auth/steam/* stay ƒ + no NEXT_PUBLIC + supabase/migrations unchanged. login-flow.ts public interface untouched; app_metadata/role/session untouched (as 2.2 left them); display_name length/charset cap stays Epic-5-deferred. ⛔ Task 5 live-QA SIGN-OFF GATE PENDING (human: real Steam login → rename persona → re-login shows new name, same PK/created_at, app_role attached; mirrors 2.1/2.2 cleared post-code-review). Impl commit pending. PREVIOUS: Story 2.3 hydrated via bmad-create-story → ready-for-dev [baseline 3006dba, HEAD/clean]. Scope: PROVE the canonical-key invariant + rename tolerance — the mechanical rename tolerance already ships in 2.1's upsertPlayer [.upsert(onConflict:'steamid64') updates display_name/avatar, keeps PK]; 2.3 adds proof + one robustness guard, NOT new machinery. ACs: AC1/AC2 verbatim [join by steamid64 never display_name; re-login updates display_name cosmetically, link preserved] + derived AC3 pgTAP rename-tolerance [UPDATE display_name preserves steamid64 PK + created_at + FK'd app_role row], AC4 pgTAP catalog guard [no PK/UNIQUE/FK anywhere references display_name — bites for future roster_entry(2.5)/stat_row(E3), mirrors Epic-1 generic-catalog-guard precedent], AC5 NEW lib/players.test.ts [onConflict:'steamid64', rename routes to same key, 17-digit guard, fetchSteamProfile fallbacks — no players test existed], AC6 RECOMMENDED-decision placeholder-clobber guard [transient Steam API failure on re-login must NOT overwrite a good display_name/avatar with the steamid64 placeholder → ignoreDuplicates:true = ON CONFLICT DO NOTHING for the unhydrated path], AC7 NO migration/scope held [no roster_entry/stat_row/unreconciled/role; display_name length cap stays Epic-5-deferred; explicit plan(N)]. NO migration (only supabase/tests/*.sql + Vitest, + small players.ts/callback edit if AC6 in scope). ⭐ Judgment call RESOLVED (Cuatro 2026-07-03): AC6 CONFIRMED IN SCOPE (build the placeholder-clobber guard; length/charset cap stays Epic-5-deferred). Live-QA sign-off gate (mirrors 2.1/2.2): real Steam login → rename → re-login shows new display_name, same PK/created_at, app_role still attached. PREVIOUS: 2026-07-02 (Story 2.1 code-review DONE → back to in-progress; 3 parallel adversarial layers [blind/edge/auditor]; triage 1 decision + 4 patch + 6 defer + 7 dismissed. FIXED: P1 CRITICAL HTTP-param-pollution Steam identity-spoof [duplicated openid.claimed_id: .get()=first vs Steam-validated last → auth as any SteamID64] now fails closed + reads claimed_id from validated set +2 regression tests; P2 callback try/catch → fail-closed redirect not 500; P3 AbortSignal.timeout on both Steam fetches; P4 Secure cookie from parsed protocol. 30/30 Vitest green, next build clean, NO migration. verifyOtp 'email'→'magiclink' flip WITHDRAWN [auditor misread; code matches its own doc comment]. ✅ SIGN-OFF GATE CLEARED → 2-1 DONE (live QA 2026-07-02: real Steam login → player upserted [cuatro/76561198388441171] + auth.users app_metadata.steamid64 present, no role, sb-* session cookie minted; verifyOtp 'email' CONFIRMED working vs supabase-js@2.110.0/ssr@0.12.0 — no magiclink flip). 6 deferrals logged to deferred-work.md. THEN Story 2.2 (bind identity+role into app_metadata) hydrated via bmad-create-story → ready-for-dev [baseline 3b0f161]: net-new server-only ADMIN_STEAMIDS allowlist; extend session.ts ensureAuthUser (+role on create AND a new updateUserById backfill branch, before the mint); new lib/auth/roles.ts resolveRole reads app_role-first then allowlist-bootstraps (default viewer, fail-closed); NO migration (app_role + is_admin/jwt_steamid64 + service_role grant all exist 0001/0002); ES256/JWKS = NO app-code work (Supabase signs). decision RESOLVED → write both). THEN Story 2.2 dev-story DONE → review (2026-07-02, 12 files): new lib/auth/roles.ts (resolveRole app_role-first + revoke-durable + parseAdminAllowlist); extended session.ts (ensureAuthUser/establishSession take role, updateUserById backfill using generateLink's user.id BEFORE verifyOtp so first admin JWT resolves is_admin()); env.ts ADMIN_STEAMIDS getter + forbidden-mirror; callback route resolves+passes role; vitest server-only stub (unblocks testing server-only modules — closes 2.1's session.ts zero-tests gap). 42/42 Vitest (+roles.test 9 +session.test 3), next build clean, NO migration (app_role + is_admin/jwt_steamid64 + grants pre-exist), ES256/JWKS = no app-code. app_metadata carries {steamid64, role}; user_metadata never touched. ⛔ LIVE-QA SIGN-OFF GATE pending (mirrors 2.1): live login must confirm role:admin in raw_app_meta_data + app_role row (id,admin,null) + is_admin() true / viewer false + backfill of Cuatro's pre-existing 2.1 user). THEN 2.2 code-review DONE → back to in-progress (2026-07-02, 3 adversarial layers): STRONG — 0 confirmed crit/high, all 8 ACs met, scope held. 3 patches applied: P1 (blind+edge converged) session.ts fail-closed when generateLink returns no user.id (was silent-skip → role-less JWT) +regression test; P2 narrowed isAlreadyExists (dropped over-broad status===422); P3 documented ADMIN_STEAMIDS one-time-bootstrap gotcha in .env.example. 43/43 Vitest, next build clean. 3 defers (as-Role cast [DB-CHECK-guarded], 23505 re-read swallows error [near-impossible race], malformed-allowlist-silence) → deferred-work.md; 6 dismissed. ✅ D1 LIVE-QA GATE CLEARED → 2-2 DONE (2026-07-02): Cuatro's login [pre-existing 2.1 user] → raw_app_meta_data role:admin + steamid64 kept; temp /api/whoami RPC returned is_admin:true + jwt_app_metadata.role:admin + jwt_steamid64 resolves [end-to-end proof the backfill landed role in the FIRST JWT — generateLink→updateUserById→verifyOtp ordering confirmed, verifyOtp re-reads current app_metadata]; app_role row (id,admin,null) inserted; temp route deleted. Epic 2 now 2/6 stories done [2.1, 2.2])
project: cs-tournament
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # --- Epic 1: Foundation & Schema (CAP-1) ---
  epic-1: in-progress
  1-1-provision-the-single-environment-and-server-only-secrets: done
  1-2-identity-and-scope-schema-migration-0001: done
  1-3-fail-closed-rls-framework-and-helper-functions-migration-0002: done
  1-4-append-only-audit-and-snapshot-convention: done
  epic-1-retrospective: done

  # --- Epic 2: Identity & Roster (CAP-2) ---
  epic-2: in-progress
  2-1-steam-openid-2-0-server-side-login: done
  2-2-bind-identity-and-role-into-app-metadata: done
  2-3-canonical-steamid64-capture-and-display-name-tolerance: done
  2-4-roles-admin-gating-and-revoke-invalidates-session: done
  2-5-registration-window-and-roster-management: done
  2-6-unreconciled-steamid64-list-roster-side: deferred  # -> Epic 3, build AFTER Story 3.4 (Cuatro 2026-07-06): the unreconciled list is an AD-4 left-join over stat_row, which is not created until Story 3.3 (Epic 3) -- no data source exists yet; 'deferred' so it is not auto-picked next. See deferred-work.md.
  epic-2-retrospective: done

  # --- Epic 3: Demo Ingestion Worker (CAP-3) ---
  epic-3: in-progress
  3-1-demo-acquisition-that-bypasses-vercel: in-progress
  3-2-hash-dedup-and-write-once-retention: backlog
  3-3-parse-the-demo-to-stat-rows-as-the-single-writer: backlog
  3-4-validation-and-anomaly-gate: backlog
  3-5-stage-stats-to-pending-ingest-side-of-fr-13: backlog
  3-6-re-parse-a-retained-demo-ingest-side-of-fr-14: backlog
  3-7-demo-derived-score-from-ingest-ingest-side-of-fr-16: backlog
  3-8-bounded-async-ingest-job-meeting-the-latency-budget: backlog
  epic-3-retrospective: optional

  # --- Epic 4: Bracket & Admin Command Routes (CAP-4) ---
  epic-4: backlog
  4-1-random-seeded-double-elimination-bracket-generation: backlog
  4-2-pre-declared-match-format-and-tie-policy: backlog
  4-3-idempotent-conditional-advance: backlog
  4-4-grand-final-reset-as-two-ordered-match-rows: backlog
  4-5-bye-and-forfeit-walkover-with-grace-timer: backlog
  4-6-aprobar-atomic-publish-publish-side-of-fr-13: backlog
  4-7-rollback-an-approved-match-rollback-side-of-fr-14: backlog
  4-8-manual-score-override-manual-side-of-fr-16: backlog
  4-9-server-gated-audited-command-routes: backlog
  epic-4-retrospective: optional

  # --- Epic 5: Stats, Leaderboards & Realtime Viewer Surfaces (CAP-5) ---
  epic-5: backlog
  5-1-core-stat-derivation: backlog
  5-2-weird-demo-only-stat-derivation: backlog
  5-3-derived-stat-derivation: backlog
  5-4-anti-farm-floors-and-afk-idle-dq: backlog
  5-5-single-normalized-leaderboard-view-rpc: backlog
  5-6-timeline-feed-read-read-side-of-fr-31: backlog
  5-7-spanish-mobile-first-read-only-viewer-surfaces: backlog
  5-8-realtime-nudge-and-reconnect-reconcile: backlog
  epic-5-retrospective: optional

  # --- Epic 6: Awards Roulette — Producer & Verifier (CAP-6) ---
  epic-6: backlog
  6-1-award-catalog-and-buckets: backlog
  6-2-fair-seed-freeze-and-immutable-snapshot-capture: backlog
  6-3-prng-core-hmac-sha256-counter-mode: backlog
  6-4-two-stage-draw: backlog
  6-5-fr-29-tie-ladder: backlog
  6-6-anti-sweep-and-luck-meter: backlog
  6-7-pity-roulette: backlog
  6-8-reveal-gating-and-commitment: backlog
  6-9-verification-bundle-and-verificar-la-ceremonia: backlog
  6-10-ceremony-reveal-ui-with-reduced-motion-parity: backlog
  6-11-cross-language-golden-vector-suite: backlog
  epic-6-retrospective: optional

  # --- Epic 7: Hardening & Operations (CAP-7) ---
  epic-7: backlog
  7-1-demo-durability-object-lock-delete-guard: backlog
  7-2-cost-guards-billing-alerts: backlog
  7-3-dr-runbook: backlog
  7-4-ingest-recovery-hardening-ops-slice: backlog
  7-5-build-handoff-checklist-gate: backlog
  epic-7-retrospective: optional

# Action items recorded during retrospectives (sprint-status surfaces open ones).
action_items:
  - epic: 1
    action: "Obtain Steam Web API key + register production domain before Story 2.1 (deferred from Story 1.1 Task 2). RESOLVED 2026-07-02: Steam Web API key obtained + set in .env.local + local realm wired (localhost) → local dev unblocked; production domain decided = inclusivcup.cuatro.dev (Cuatro owns cuatro.dev via Squarespace Domains + Cloudflare DNS). Remaining is deploy-time infra only (Vercel Production env vars STEAM_REALM/STEAM_RETURN_URL=https://inclusivcup.cuatro.dev[/auth/steam/callback] + Vercel custom-domain + Cloudflare CNAME) — tracked for first prod deploy (Epic 7), not a Story 2.1 blocker."
    owner: "Cuatro"
    status: done
  - epic: 1
    action: "Bind identity/role using the ES256 + JWKS scheme recorded in 1.1 (not legacy HS256) in Story 2.2. DONE 2026-07-06 (via Epic 2 retro): Story 2.2 binds role through Supabase's ES256/JWKS session signing (no HS256 shared secret); is_admin()/jwt_steamid64() resolve from the minted app_metadata, live-QA confirmed."
    owner: "Amelia"
    status: done
  - epic: 1
    action: "roster_entry migration 0004 adds its own explicit GRANTs + ENABLE/FORCE RLS (always-revoked default). DONE 2026-07-06 (via Epic 2 retro): Story 2.5 shipped 0004_roster.sql with explicit anon/authenticated SELECT + service_role SELECT/INSERT/UPDATE (NO DELETE = soft-delete ceiling) + FORCE RLS; pgTAP 0004_roster_test.sql plan(38); the 0003 generic catalog FORCE-guard auto-covers roster_entry."
    owner: "Amelia"
    status: done
  - epic: 1
    action: "Enforce granted_by admin-only / no-self-grant invariant in the Story 2.4 grant/revoke route. DONE 2026-07-05: implemented in setRole (actingAdmin===target → self_target, rejected before any DB write, guaranteeing granted_by is always a different admin) + stamped granted_by=actingAdmin on the app_role upsert; enforced in app code (0003 header forbids a DB CHECK — service-role writes bypass RLS); code-reviewed + live-QA'd as part of Story 2.4 → done."
    owner: "Amelia / Cuatro"
    status: done
  - epic: 1
    action: "Keep explicit plan(N) assertion-accounting for every new pgTAP suite. MAINTAINED through Epic 2 (2.3 plan(10)->(13), 2.5 plan(38) exact, no drift) -> now a standing convention; marked done for Epic 2, carries forward as an ongoing habit."
    owner: "Amelia"
    status: done
  - epic: 2
    action: "Fold Story 2.6 (unreconciled-SteamID64 list) into the Story 3.4 hydration -- 2.6 has no data source until stat_row exists (created 3.3, populated/flagged 3.4) and epics.md:535 already names it by number. Move 2.6 from deferred to built as part of / adjacent to Story 3.4."
    owner: "Amelia"
    status: open
  - epic: 2
    action: "Close the roster-lock TOCTOU (deferred W1) in the Epic 4 bracket-generation slice: add the write-side state predicate (WHERE-clause / RPC guard) to adminAddPlayer/removePlayer/enrollSelf BEFORE a concurrent bracket_live writer exists. Unexploitable until then; must not be forgotten when Epic 4 starts."
    owner: "Amelia"
    status: open
  - epic: 2
    action: "Epic 3 infra prep (decide at Epic 3 planning, per Cuatro): choose the Go-worker deploy host (must bypass Vercel), provision the Cloudflare R2 bucket + MatchZy shared-secret path. poc-cs2-demo-parse de-risks parsing; hosting/integration is the open call."
    owner: "Cuatro"
    status: open
  - epic: 2
    action: "Seed a standing season + tournament row as the local/prod fixture for all Epic 3 ingest live-QA (was also the 2.5 live-QA prereq). Not shipped as seed.sql -- would collide with the 0001-0003 pgTAP 'Season 1' fixtures -- so it is an operational one-time SQL step."
    owner: "Cuatro"
    status: open
