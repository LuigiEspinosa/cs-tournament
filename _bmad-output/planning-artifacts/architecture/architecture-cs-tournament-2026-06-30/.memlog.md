---
scope: InclusivCup CS2 tournament web app — v1 architecture covering demo ingest, double-elim bracket, provably-fair Awards Roulette, timeline feed, admin and viewer surfaces.
purpose: build-substrate
altitude: feature
updated: 2026-06-30T10:05
---

- (event) Architecture run started -- Phase 4, greenfield. Inputs: PRD+addendum, UX EXPERIENCE+DESIGN, brief, research spike/PoC. Plan approved by user.
- (direction) Fast-path draft with the load-bearing forks surfaced to the user as explicit choices; coaching compressed given a fully-specified PRD/UX and a casual one-off scope.
- (decision) OQ-3 parser fork -> demoinfocs-golang (Go) v5.2.0. Binds the worker. Prevents betting the stat keystone on a less-proven parser. Chosen over demoparser2 (Node) for FACEIT-grade reliability, fastest Valve-format-churn tracking, per-tick positions for FR-21, single static binary.
- (decision) OQ-2 worker hosting -> hosted Go worker on Railway Hobby (~$5-10/mo) with MatchZy auto-ingest LIVE at v1; same binary keeps a local-CLI manual fallback (FR-11). User choice.
- (decision) OQ-8 retention -> all raw demos in Cloudflare R2 indefinitely, final/seed demo permanently; Supabase stays free-tier. User choice (R2 + Supabase free).
- (decision) AD-1 Demo = immutable source of truth: R2 write-once, identified by (match_id, sha256), re-hashable, never deleted by re-parse/rollback. Binds ingest/fairness/disputes. FR-15/FR-27.
- (decision) AD-2 Single-writer derivation: the Go worker via service-role is the only writer of stat_row/demo; no client role has a stat-write policy. Prevents two-writer races/dupes. NFR security.
- (decision) AD-3 Idempotent ingestion: demo unique (match_id, sha256); stat_row unique (match_id, steamid64); re-parse = ON CONFLICT DO UPDATE + delete-missing in one txn, forces status=pending. FR-12/FR-14.
- (decision) AD-4 SteamID64 sole canonical join key (Postgres text + CHECK ^[0-9]{17}$); display_name mutable/cosmetic, never a join key; unrostered IDs surface in an unreconciled list, never dropped. FR-2.
- (decision) AD-5 One score source per match: score_source in {demo_derived, admin_manual}; demo-derived is default of record when a demo exists; manual override needs an explicit flag + audit row. Prevents two owners of score. FR-16.
- (decision) AD-6 Aprobar is ONE transaction (publish stats + advance bracket + post feed + recompute leaderboards); realtime emit is post-commit only. Prevents partial publish/flicker. FR-13/FR-31.
- (decision) AD-7 Viewers read approved-only at the row level: two SEPARATE RLS policies (viewer USING status=approved; admin USING is_admin()); FORCE RLS; fails closed. Prevents Pending leak. FR-13/FR-34.
- (decision) AD-8 Idempotent, server-gated, admin-only mutations: advance is a consequence of a result (not a button racing publish), conditional no-double-route; rollback cascades dependent advances and flags independently-approved downstream; routes re-verify admin then use service-role. FR-4/FR-7/FR-14/FR-33.
- (decision) AD-9 Forfeit/Bye produce zero stat rows; leaderboards normalize over completed non-forfeit matches. Prevents phantom/denominator poisoning. FR-8/FR-9/FR-17.
- (decision) AD-10 Match format/tie policy declared and locked BEFORE match start; later changes are audited overrides. FR-10.
- (decision) AD-11 Realtime Broadcast is a nudge, not truth: every viewer surface is reconstructable from a published read; reconnect re-fetches the snapshot, never replays. Channels tournament:{id}, ceremony:{id}, admin:{id}. Meets ~2s NFR.
- (decision) AD-12 Steam OpenID 2.0 verified server-side -> identity+role bound in Supabase app_metadata (service-role-only, never user_metadata); RLS reads role/steamid64 from auth.jwt(); role lives in app_role with no client write. Prevents client role escalation. FR-1/FR-4/FR-33.
- (decision) AD-13 Provably-fair seed = SHA-256(final demo), frozen once the final demo is approved; the bracket-generation seed is a DISTINCT field. Prevents seed conflation/re-roll. FR-5/FR-27.
- (decision) AD-14 Deterministic client-reproducible draw engine: HMAC-SHA256 counter-mode PRNG keyed by raw 32-byte seed, integer-only/cross-multiply arithmetic (no floats/locale, explicit ordering), two-stage draw + FR-29 tie ladder + anti-sweep <=1/spin + pity; RFC-8785 canonical verification bundle; Go produces / JS re-verifies; cross-language conformance vectors gate the build. FR-24..FR-30, SM-3.
- (decision) AD-15 Ceremony decides winners from an immutable frozen snapshot captured at ceremony-lock, never live tables. Prevents mid-ceremony winner drift from re-parse/approve. FR-25/FR-27.
- (decision) AD-16 Demo bodies never transit a Vercel function (4.5MB cap): MatchZy->worker HTTP->R2 / admin web->R2 presigned multipart->notify / CLI->R2 server-side. DemoStore abstraction (R2 primary, Supabase-Storage swappable); DB stores opaque (backend,key)+hash; seed demo delete-proof; worker ingest endpoint shared-secret authed. FR-11/FR-15.
- (decision) AD-17 Append-only audit log of every event-mutating admin action (actor + timestamp + before/after); enforced by the ABSENCE of any UPDATE/DELETE policy. FR-33.
- (decision) AD-18 Seasons-aware schema, no season features built; everything scopes by tournament_id under a season. Constraint.
- (version) Next.js 16.2.x App Router, Node 20.9+ baseline -- web-verified current mid-2026.
- (version) demoinfocs-golang v5.2.0 (Go 1.26.x), pinned; full CS2/Source-2 support; ~30% faster entity parsing in recent releases -- web-verified.
- (version) Supabase free tier (Postgres 15+, Realtime, Auth). Broadcast <50ms / 224k msg/s; Postgres Changes throughput-bottlenecked under RLS -- web-verified; Broadcast chosen for the hot path.
- (version) Cloudflare R2 -- S3-compatible, FREE egress, multipart (no native TUS), ~$0.015/GB-mo -- web-verified.
- (version) Railway Hobby ~$5-10/mo for a small always-on Go worker (low usage) -- web-verified; Fly.io now ~$14-20.
- (version) PRNG primitive HMAC-SHA256 (Go crypto/hmac+crypto/sha256; browser SubtleCrypto), byte-identical; chosen over raw SHA256(seed||ctr) (length-extension/domain-sep) and NIST HMAC_DRBG (heavier state) -- web-verified.
- (constraint) Demo files 50-170MB exceed the Vercel ~4.5MB body cap AND the Supabase free-tier 50MB file cap -- drives direct-to-R2 + worker-owns-demo. PRD section 9.
- (constraint) Spanish UI invariant; load-bearing exact strings: Verificar la ceremonia / Verificado desde el demo / Sembrado por el demo final reproducible / Pendiente / Aprobado. Spine prose itself is English (document_output_language).
- (constraint) Reduced-motion ceremony must resolve identical winners in identical seeded order -- determinism is independent of animation. UX accessibility floor.
- (note) OQ-4 match formats -> match.format + match.tie_policy columns (locked per match) + tournament.format_default. Organizer config, not a code blocker.
- (note) OQ-5 thresholds -> award.eligibility_floor_rounds/_kills (24/20), AFK idle threshold+epsilon, grace timer (10min default), luck weight_table, spin_plan -- all published in the verification bundle so the ceremony stays reproducible when tuned.
- (note) OQ-6 dispute flow -> evidence-only confirmed (no in-app raise button); app exposes demo hash + re-parse + seed snapshot; disputes settled in Discord. Matches UX spine.
- (note) OQ-7 ceremony pacing -> a published spin_plan parameterizes how the 12-award catalog maps to Spins; the draw algorithm is parameterized, never hardcoded.
- (insight) Adversarial design pre-closed the known divergence holes: two score owners (AD-5), re-parse duplication (AD-3), co-winner ambiguity (1..N award_result_winner child table), role escalation (AD-12 app_metadata), Pending RLS leak (AD-7 split policies + FORCE RLS), seed conflation (AD-13 distinct fields), forfeit stat poisoning (AD-9), dropped-realtime divergence (AD-11), Go<->JS PRNG drift (AD-14 integer-only + conformance vectors).
- (event) Reviewer gate run: lint_spine.py (clean after {id}->/<id/> fix) + 5 parallel lenses (tech-currency, adversarial, good-spine rubric, input-reconcile, data-integrity). All CONCERNS, additive; tech-currency verified every bound version current (Next.js 16.2 2026-03-18, Go 1.26.x, demoinfocs v5.2.0 2026-04-21, Supabase 200-concurrent/50MB, R2 clauses, HMAC byte-identity).
- (decision) AD-19 (gate) Snapshot is the verifier complete integer-form contract: stat_snapshot_row holds exactly the integer-form fields Stage-2 + FR-29 consume; a golden vector must project a REAL captured snapshot. Closes adversarial H1. FR-25/27, SM-3.
- (decision) AD-20 (gate) Leaderboards have one owner and one normalization site (single SQL view/RPC over approved rows; FR-21 floors + rate/volume applied once). Closes adversarial H3. FR-17/21/22.
- (decision) AD-21 (gate) Grand-final reset = two ordered match rows; champion resolves from the last GF row; champion slot never overwritten in place. Closes adversarial H4. FR-6.
- (decision) AD-22 (gate) Reveal-gating + commit-then-publish: reveal-state is a distinct RLS axis; catalog/live-sets/award_result reveal-gated on spin.revealed_at; seed+bundle_hash committed up front, full bundle released progressively/at completion so verify confirms but never spoils. Closes reconcile-#1 + data-integrity-#2 + adversarial H7. FR-24/27/30.
- (decision) AD-23 (gate) One terminal match state, single-writer precedence: a late demo for a committed Forfeit/Bye is archived for evidence but produces no stat_row; Resolved and Forfeit cannot coexist. Closes adversarial H8. FR-9/16/17.
- (decision) AD-24 (gate) Spanish-UI invariant promoted: all viewer copy Spanish from one i18n module, no inline English; exact load-bearing strings + status->label pin (pending->Pendiente, approved->Aprobado); reduced-motion uses identical resolution path + identical published spin order. Closes reconcile-#4/#5. FR-30.
- (decision) AD-25 (gate) Single-environment v1; secrets server-only (never NEXT_PUBLIC); ParseFailed/anomalies alert + log; DR stance = R2 demos are durable truth (object-lock on permanent_seed) + DB re-derivable by re-parse + Supabase managed backups. Closes rubric-#1 (silent operational envelope). NFR security, FR-15.
- (decision) AD-26 (gate) Ingestion is an idempotent, bounded, async job meeting SM-5: off-trigger async (never inline), pinned parser, bounded retriable ParseFailed + backoff, bounded concurrency; SM-5 P50<5min/P95<15min is the end-to-end target. Closes rubric-#2. FR-11/12, SM-5.
- (decision) Gate tightenings: AD-3 re-parse-of-Approved runs as one revert->reparse->republish txn (no observable Pending window), guarded on consumed downstream edges; AD-7 per-table fail-closed RLS (audit_log/snapshot admin-only; reveal tables per AD-22); AD-12 is_admin()=COALESCE(jwt app_metadata role, false) + revoke invalidates session; AD-14 equal value/cross-product = tie -> ladder (vectors include ties); AD-15 snapshot write-once + ceremony-lock SERIALIZABLE; AD-16 permanent_seed delete-proof at storage layer (R2 object-lock) + Supabase-paid caveat; AD-5 OR-precedence parenthesized; AD-2 accept-anomaly is an app-side admin action.
- (constraint) Railway Hobby cost honesty (tech-currency): ~$5-10/mo at low utilization but bills actual CPU/RAM with ~$20-30/mo ceiling for a 24/7 worker -- set a billing alert. Supabase-Storage DemoStore backend needs a PAID tier for 50-170MB files (free 50MB cap), so it is not a drop-in free failover; R2 is primary.
- (note) ERD hardened: tournament.state gates roster writes (FR-3); tournament.fair_seed write-once; stat_row.demo_id provenance; STAT_SNAPSHOT_ROW integer-form block; award.priority UNIQUE; AWARD_RESULT_WINNER.spin_id for UNIQUE(spin_id,winner_entry_id) anti-sweep; ON DELETE convention; closed-set columns CHECK-enums. app_role is event-global in v1 (per-season scoping deferred with AD-18).
- (event) Spine revised post-gate (AD-19..26 added, 8 AD tightenings, ERD/conventions/stack/deferred/provably-fair updated); status set to final. spine finalized.
